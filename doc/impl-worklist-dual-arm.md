# Implementation worklist (wf_eace1f00-350)

## Verification conditions

- [The custody invariant HOLDS under deletion of psingleMintWithCredential, and the identical-policy-ID premise is verified against the actual offchain derivation.

Derivation check (the crux the task asked to confirm): in programmableLogicMintingScript (Scripts.hs 123-137) the unapplied issuance script is Scripts.tryCompile Production (mkProgrammableLogicMinting # base # directoryNodeCS), then applyArguments [mintingLogicHash]. The compile target is hardcoded Production and the ONLY applied datum is the minting-logic script hash. No one-shot TxIn, nonce, version byte, or any other varying input enters the applied issuance script. (The one-shot TxIn/nonce live only in the SEPARATE protocolParams NFT (line 50) and issuanceCborHex NFT (line 64), which are not arguments to the issuance policy.) In the two-parameter redesign, base+directoryNodeCS collapse into protocolParamsCS, which is itself instance-unique because it is the one-shot protocol-params NFT symbol. Therefore the issuance policy ID = blake2b(prefix ++ enc(mintingLogicHash) ++ postfix) is a pure deterministic function of (protocolParamsCS, mintingLogicHash). Within one instance the map mintingLogicHash -> policyID is injective.

Why deletion is safe: psingleMintWithCredential is an authorization-uniqueness scan (exactly one minting redeemer equal to the policy's own minting-logic credential), NOT a custody check. It can only ever constrain two mint purposes that carry the SAME minting-logic-credential redeemer. But each policy's own authorization tie (own-redeemer == its compile-time mintingLogicHash in the current code; withdrawal at mintingLogicWdrlIdx == ScriptCredential(mintingLogicHash) in the redesign) binds every minted policy to its OWN unique hash, and injective derivation means distinct policy IDs have distinct hashes. So two mint purposes in one tx can never legitimately share the logic-hash redeemer -> the scan protects nothing that can exist within one instance. Custody itself is enforced independently: current code forces all minted tokens into output 0 at the base credential; the redesign uses the Local full-scan (every non-base output holds zero of ownCS) or Delegate scoping. Neither path reads the deleted scan.

Cross-deployment logic reuse (attacker deploys own instance sharing the hash) produces a DIFFERENT policy ID (different protocolParamsCS) that is not registered in the victim directory, hence not a registered policy of the victim instance and outside the per-instance custody invariant; this is the documented logic-author contract (spec 10.4), matching Aiken which has no isolation scan.

Note: the claim's stated code state ("two-parameter policy, psingleMintWithCredential deleted") is the SPEC target; the current on-chain Issuance.hs is still the three-parameter POC that RETAINS and uses psingleMintWithCredential (lines 191, 199, 204-252). The verification concerns the redesign's soundness, which holds./Custody invariant vs. deletion of the minting-logic isolation scan (psingleMintWithCredential), verified via the injectivity of the offchain policy-ID derivation.] Each minted policy MUST keep its authorization check bound to its OWN compile-time mintingLogicHash constant (spec 7 item 2). If that check were ever made non-policy-specific (e.g. accepting any withdrawal rather than ScriptCredential(mintingLogicHash)), cross-authorization of multiple policies by one withdrawal reopens.
- [The custody invariant HOLDS under deletion of psingleMintWithCredential, and the identical-policy-ID premise is verified against the actual offchain derivation.

Derivation check (the crux the task asked to confirm): in programmableLogicMintingScript (Scripts.hs 123-137) the unapplied issuance script is Scripts.tryCompile Production (mkProgrammableLogicMinting # base # directoryNodeCS), then applyArguments [mintingLogicHash]. The compile target is hardcoded Production and the ONLY applied datum is the minting-logic script hash. No one-shot TxIn, nonce, version byte, or any other varying input enters the applied issuance script. (The one-shot TxIn/nonce live only in the SEPARATE protocolParams NFT (line 50) and issuanceCborHex NFT (line 64), which are not arguments to the issuance policy.) In the two-parameter redesign, base+directoryNodeCS collapse into protocolParamsCS, which is itself instance-unique because it is the one-shot protocol-params NFT symbol. Therefore the issuance policy ID = blake2b(prefix ++ enc(mintingLogicHash) ++ postfix) is a pure deterministic function of (protocolParamsCS, mintingLogicHash). Within one instance the map mintingLogicHash -> policyID is injective.

Why deletion is safe: psingleMintWithCredential is an authorization-uniqueness scan (exactly one minting redeemer equal to the policy's own minting-logic credential), NOT a custody check. It can only ever constrain two mint purposes that carry the SAME minting-logic-credential redeemer. But each policy's own authorization tie (own-redeemer == its compile-time mintingLogicHash in the current code; withdrawal at mintingLogicWdrlIdx == ScriptCredential(mintingLogicHash) in the redesign) binds every minted policy to its OWN unique hash, and injective derivation means distinct policy IDs have distinct hashes. So two mint purposes in one tx can never legitimately share the logic-hash redeemer -> the scan protects nothing that can exist within one instance. Custody itself is enforced independently: current code forces all minted tokens into output 0 at the base credential; the redesign uses the Local full-scan (every non-base output holds zero of ownCS) or Delegate scoping. Neither path reads the deleted scan.

Cross-deployment logic reuse (attacker deploys own instance sharing the hash) produces a DIFFERENT policy ID (different protocolParamsCS) that is not registered in the victim directory, hence not a registered policy of the victim instance and outside the per-instance custody invariant; this is the documented logic-author contract (spec 10.4), matching Aiken which has no isolation scan.

Note: the claim's stated code state ("two-parameter policy, psingleMintWithCredential deleted") is the SPEC target; the current on-chain Issuance.hs is still the three-parameter POC that RETAINS and uses psingleMintWithCredential (lines 191, 199, 204-252). The verification concerns the redesign's soundness, which holds./Custody invariant vs. deletion of the minting-logic isolation scan (psingleMintWithCredential), verified via the injectivity of the offchain policy-ID derivation.] protocolParamsCS MUST be derived from a one-shot NFT so that 'one protocol instance' is well-defined and instance-unique; confirmed by protocolParamsMintingScript's one-shot TxIn (Scripts.hs 48-50).
- [The custody invariant HOLDS under deletion of psingleMintWithCredential, and the identical-policy-ID premise is verified against the actual offchain derivation.

Derivation check (the crux the task asked to confirm): in programmableLogicMintingScript (Scripts.hs 123-137) the unapplied issuance script is Scripts.tryCompile Production (mkProgrammableLogicMinting # base # directoryNodeCS), then applyArguments [mintingLogicHash]. The compile target is hardcoded Production and the ONLY applied datum is the minting-logic script hash. No one-shot TxIn, nonce, version byte, or any other varying input enters the applied issuance script. (The one-shot TxIn/nonce live only in the SEPARATE protocolParams NFT (line 50) and issuanceCborHex NFT (line 64), which are not arguments to the issuance policy.) In the two-parameter redesign, base+directoryNodeCS collapse into protocolParamsCS, which is itself instance-unique because it is the one-shot protocol-params NFT symbol. Therefore the issuance policy ID = blake2b(prefix ++ enc(mintingLogicHash) ++ postfix) is a pure deterministic function of (protocolParamsCS, mintingLogicHash). Within one instance the map mintingLogicHash -> policyID is injective.

Why deletion is safe: psingleMintWithCredential is an authorization-uniqueness scan (exactly one minting redeemer equal to the policy's own minting-logic credential), NOT a custody check. It can only ever constrain two mint purposes that carry the SAME minting-logic-credential redeemer. But each policy's own authorization tie (own-redeemer == its compile-time mintingLogicHash in the current code; withdrawal at mintingLogicWdrlIdx == ScriptCredential(mintingLogicHash) in the redesign) binds every minted policy to its OWN unique hash, and injective derivation means distinct policy IDs have distinct hashes. So two mint purposes in one tx can never legitimately share the logic-hash redeemer -> the scan protects nothing that can exist within one instance. Custody itself is enforced independently: current code forces all minted tokens into output 0 at the base credential; the redesign uses the Local full-scan (every non-base output holds zero of ownCS) or Delegate scoping. Neither path reads the deleted scan.

Cross-deployment logic reuse (attacker deploys own instance sharing the hash) produces a DIFFERENT policy ID (different protocolParamsCS) that is not registered in the victim directory, hence not a registered policy of the victim instance and outside the per-instance custody invariant; this is the documented logic-author contract (spec 10.4), matching Aiken which has no isolation scan.

Note: the claim's stated code state ("two-parameter policy, psingleMintWithCredential deleted") is the SPEC target; the current on-chain Issuance.hs is still the three-parameter POC that RETAINS and uses psingleMintWithCredential (lines 191, 199, 204-252). The verification concerns the redesign's soundness, which holds./Custody invariant vs. deletion of the minting-logic isolation scan (psingleMintWithCredential), verified via the injectivity of the offchain policy-ID derivation.] Custody enforcement moves entirely to the Local full-scan / Delegate-scoping arm; its own soundness is assumed here, not re-verified under this lens — the deletion of psingleMintWithCredential is safe only insofar as custody is fully carried by that arm.
- [The custody invariant HOLDS under deletion of psingleMintWithCredential, and the identical-policy-ID premise is verified against the actual offchain derivation.

Derivation check (the crux the task asked to confirm): in programmableLogicMintingScript (Scripts.hs 123-137) the unapplied issuance script is Scripts.tryCompile Production (mkProgrammableLogicMinting # base # directoryNodeCS), then applyArguments [mintingLogicHash]. The compile target is hardcoded Production and the ONLY applied datum is the minting-logic script hash. No one-shot TxIn, nonce, version byte, or any other varying input enters the applied issuance script. (The one-shot TxIn/nonce live only in the SEPARATE protocolParams NFT (line 50) and issuanceCborHex NFT (line 64), which are not arguments to the issuance policy.) In the two-parameter redesign, base+directoryNodeCS collapse into protocolParamsCS, which is itself instance-unique because it is the one-shot protocol-params NFT symbol. Therefore the issuance policy ID = blake2b(prefix ++ enc(mintingLogicHash) ++ postfix) is a pure deterministic function of (protocolParamsCS, mintingLogicHash). Within one instance the map mintingLogicHash -> policyID is injective.

Why deletion is safe: psingleMintWithCredential is an authorization-uniqueness scan (exactly one minting redeemer equal to the policy's own minting-logic credential), NOT a custody check. It can only ever constrain two mint purposes that carry the SAME minting-logic-credential redeemer. But each policy's own authorization tie (own-redeemer == its compile-time mintingLogicHash in the current code; withdrawal at mintingLogicWdrlIdx == ScriptCredential(mintingLogicHash) in the redesign) binds every minted policy to its OWN unique hash, and injective derivation means distinct policy IDs have distinct hashes. So two mint purposes in one tx can never legitimately share the logic-hash redeemer -> the scan protects nothing that can exist within one instance. Custody itself is enforced independently: current code forces all minted tokens into output 0 at the base credential; the redesign uses the Local full-scan (every non-base output holds zero of ownCS) or Delegate scoping. Neither path reads the deleted scan.

Cross-deployment logic reuse (attacker deploys own instance sharing the hash) produces a DIFFERENT policy ID (different protocolParamsCS) that is not registered in the victim directory, hence not a registered policy of the victim instance and outside the per-instance custody invariant; this is the documented logic-author contract (spec 10.4), matching Aiken which has no isolation scan.

Note: the claim's stated code state ("two-parameter policy, psingleMintWithCredential deleted") is the SPEC target; the current on-chain Issuance.hs is still the three-parameter POC that RETAINS and uses psingleMintWithCredential (lines 191, 199, 204-252). The verification concerns the redesign's soundness, which holds./Custody invariant vs. deletion of the minting-logic isolation scan (psingleMintWithCredential), verified via the injectivity of the offchain policy-ID derivation.] The minting-logic author's cross-deployment-reuse contract (spec 10.4) MUST remain documented, since reuse across separate instances is outside the policy's protection scope (as in Aiken).
- [The Local custody claim is sound. The scan forces: for every txInfo.output, payment credential == progLogicCred OR the output's ownCS token map is empty. Since output token quantities are non-negative and Conway value maps in txInfo are ledger-canonicalized (attacker cannot supply an unsorted/hidden entry), "empty ownCS at every non-base output" means total ownCS at non-base outputs = 0 post-transaction. Non-base quantity is therefore driven to zero regardless of what inputs carried — it cannot increase from any starting state. This is delta-agnostic and input-agnostic, so it holds for positive mint, mixed mint/burn, register-and-mint, and cross-policy seize alike. The §3.3 subtract-walk was unsound because preserved input supply masked the mint; the full scan is immune because it never reads inputs or the mint delta — it inspects only output values. The interaction claim holds because the Local scan on A is fully independent of the seize validator: in a seize-of-B transaction that mints A positively, A cannot select any non-scanning witness — BurnOnly requires empty positiveMint (A is minted), DelegateTransfer requires the global/TransferAct withdrawal which is absent in a seize tx (and if present would itself force A into containment via nodeRefIdx making a NonMember proof for A unsatisfiable), and DelegateSeize requires SeizeAct.directoryNodeIdx == nodeRefIdx pointing at A's node, but the real seize redeemer's directoryNodeIdx points at B's node (mkProgrammableSeize seizes directoryNodeDatumFKey at that index, line 1241) — a contradiction. So A is forced to Local, its scan pins all A to base, and whatever the seize does with B or shuffles among base outputs cannot move A to a non-base output without the scan rejecting./ledger-level value-conservation + witness-forcing analysis of the universal full-output-scan custody proof] The Local arm (spec §8) is not yet implemented in Issuance.hs (still the old POC 'mint-into-output-0' policy); soundness is contingent on the implementation scanning the FULL txInfo.outputs list, never a witness-selected subset
- [The Local custody claim is sound. The scan forces: for every txInfo.output, payment credential == progLogicCred OR the output's ownCS token map is empty. Since output token quantities are non-negative and Conway value maps in txInfo are ledger-canonicalized (attacker cannot supply an unsorted/hidden entry), "empty ownCS at every non-base output" means total ownCS at non-base outputs = 0 post-transaction. Non-base quantity is therefore driven to zero regardless of what inputs carried — it cannot increase from any starting state. This is delta-agnostic and input-agnostic, so it holds for positive mint, mixed mint/burn, register-and-mint, and cross-policy seize alike. The §3.3 subtract-walk was unsound because preserved input supply masked the mint; the full scan is immune because it never reads inputs or the mint delta — it inspects only output values. The interaction claim holds because the Local scan on A is fully independent of the seize validator: in a seize-of-B transaction that mints A positively, A cannot select any non-scanning witness — BurnOnly requires empty positiveMint (A is minted), DelegateTransfer requires the global/TransferAct withdrawal which is absent in a seize tx (and if present would itself force A into containment via nodeRefIdx making a NonMember proof for A unsatisfiable), and DelegateSeize requires SeizeAct.directoryNodeIdx == nodeRefIdx pointing at A's node, but the real seize redeemer's directoryNodeIdx points at B's node (mkProgrammableSeize seizes directoryNodeDatumFKey at that index, line 1241) — a contradiction. So A is forced to Local, its scan pins all A to base, and whatever the seize does with B or shuffles among base outputs cannot move A to a non-base output without the scan rejecting./ledger-level value-conservation + witness-forcing analysis of the universal full-output-scan custody proof] The per-output custody check must test the ENTIRE ownCS token map empty (all token names), not a single name, or a per-name escape reopens
- [The Local custody claim is sound. The scan forces: for every txInfo.output, payment credential == progLogicCred OR the output's ownCS token map is empty. Since output token quantities are non-negative and Conway value maps in txInfo are ledger-canonicalized (attacker cannot supply an unsorted/hidden entry), "empty ownCS at every non-base output" means total ownCS at non-base outputs = 0 post-transaction. Non-base quantity is therefore driven to zero regardless of what inputs carried — it cannot increase from any starting state. This is delta-agnostic and input-agnostic, so it holds for positive mint, mixed mint/burn, register-and-mint, and cross-policy seize alike. The §3.3 subtract-walk was unsound because preserved input supply masked the mint; the full scan is immune because it never reads inputs or the mint delta — it inspects only output values. The interaction claim holds because the Local scan on A is fully independent of the seize validator: in a seize-of-B transaction that mints A positively, A cannot select any non-scanning witness — BurnOnly requires empty positiveMint (A is minted), DelegateTransfer requires the global/TransferAct withdrawal which is absent in a seize tx (and if present would itself force A into containment via nodeRefIdx making a NonMember proof for A unsatisfiable), and DelegateSeize requires SeizeAct.directoryNodeIdx == nodeRefIdx pointing at A's node, but the real seize redeemer's directoryNodeIdx points at B's node (mkProgrammableSeize seizes directoryNodeDatumFKey at that index, line 1241) — a contradiction. So A is forced to Local, its scan pins all A to base, and whatever the seize does with B or shuffles among base outputs cannot move A to a non-base output without the scan rejecting./ledger-level value-conservation + witness-forcing analysis of the universal full-output-scan custody proof] progLogicCred must be read from the params datum authenticated by the one-shot params NFT (phasCSH), never from a redeemer-supplied or unauthenticated source
- [The Local custody claim is sound. The scan forces: for every txInfo.output, payment credential == progLogicCred OR the output's ownCS token map is empty. Since output token quantities are non-negative and Conway value maps in txInfo are ledger-canonicalized (attacker cannot supply an unsorted/hidden entry), "empty ownCS at every non-base output" means total ownCS at non-base outputs = 0 post-transaction. Non-base quantity is therefore driven to zero regardless of what inputs carried — it cannot increase from any starting state. This is delta-agnostic and input-agnostic, so it holds for positive mint, mixed mint/burn, register-and-mint, and cross-policy seize alike. The §3.3 subtract-walk was unsound because preserved input supply masked the mint; the full scan is immune because it never reads inputs or the mint delta — it inspects only output values. The interaction claim holds because the Local scan on A is fully independent of the seize validator: in a seize-of-B transaction that mints A positively, A cannot select any non-scanning witness — BurnOnly requires empty positiveMint (A is minted), DelegateTransfer requires the global/TransferAct withdrawal which is absent in a seize tx (and if present would itself force A into containment via nodeRefIdx making a NonMember proof for A unsatisfiable), and DelegateSeize requires SeizeAct.directoryNodeIdx == nodeRefIdx pointing at A's node, but the real seize redeemer's directoryNodeIdx points at B's node (mkProgrammableSeize seizes directoryNodeDatumFKey at that index, line 1241) — a contradiction. So A is forced to Local, its scan pins all A to base, and whatever the seize does with B or shuffles among base outputs cannot move A to a non-base output without the scan rejecting./ledger-level value-conservation + witness-forcing analysis of the universal full-output-scan custody proof] The token-in-value lookup must correctly detect any nonzero ownCS entry; this relies on ledger-canonical sorting of output Values (the attacker cannot inject an unsorted/hidden entry)
- [The Local custody claim is sound. The scan forces: for every txInfo.output, payment credential == progLogicCred OR the output's ownCS token map is empty. Since output token quantities are non-negative and Conway value maps in txInfo are ledger-canonicalized (attacker cannot supply an unsorted/hidden entry), "empty ownCS at every non-base output" means total ownCS at non-base outputs = 0 post-transaction. Non-base quantity is therefore driven to zero regardless of what inputs carried — it cannot increase from any starting state. This is delta-agnostic and input-agnostic, so it holds for positive mint, mixed mint/burn, register-and-mint, and cross-policy seize alike. The §3.3 subtract-walk was unsound because preserved input supply masked the mint; the full scan is immune because it never reads inputs or the mint delta — it inspects only output values. The interaction claim holds because the Local scan on A is fully independent of the seize validator: in a seize-of-B transaction that mints A positively, A cannot select any non-scanning witness — BurnOnly requires empty positiveMint (A is minted), DelegateTransfer requires the global/TransferAct withdrawal which is absent in a seize tx (and if present would itself force A into containment via nodeRefIdx making a NonMember proof for A unsatisfiable), and DelegateSeize requires SeizeAct.directoryNodeIdx == nodeRefIdx pointing at A's node, but the real seize redeemer's directoryNodeIdx points at B's node (mkProgrammableSeize seizes directoryNodeDatumFKey at that index, line 1241) — a contradiction. So A is forced to Local, its scan pins all A to base, and whatever the seize does with B or shuffles among base outputs cannot move A to a non-base output without the scan rejecting./ledger-level value-conservation + witness-forcing analysis of the universal full-output-scan custody proof] The witness set must make Local the only valid witness for a positive mint whenever neither the global (TransferAct) nor a same-policy seize is running — i.e. DelegateTransfer/DelegateSeize/BurnOnly must fail closed in that case
- [The Local custody claim is sound. The scan forces: for every txInfo.output, payment credential == progLogicCred OR the output's ownCS token map is empty. Since output token quantities are non-negative and Conway value maps in txInfo are ledger-canonicalized (attacker cannot supply an unsorted/hidden entry), "empty ownCS at every non-base output" means total ownCS at non-base outputs = 0 post-transaction. Non-base quantity is therefore driven to zero regardless of what inputs carried — it cannot increase from any starting state. This is delta-agnostic and input-agnostic, so it holds for positive mint, mixed mint/burn, register-and-mint, and cross-policy seize alike. The §3.3 subtract-walk was unsound because preserved input supply masked the mint; the full scan is immune because it never reads inputs or the mint delta — it inspects only output values. The interaction claim holds because the Local scan on A is fully independent of the seize validator: in a seize-of-B transaction that mints A positively, A cannot select any non-scanning witness — BurnOnly requires empty positiveMint (A is minted), DelegateTransfer requires the global/TransferAct withdrawal which is absent in a seize tx (and if present would itself force A into containment via nodeRefIdx making a NonMember proof for A unsatisfiable), and DelegateSeize requires SeizeAct.directoryNodeIdx == nodeRefIdx pointing at A's node, but the real seize redeemer's directoryNodeIdx points at B's node (mkProgrammableSeize seizes directoryNodeDatumFKey at that index, line 1241) — a contradiction. So A is forced to Local, its scan pins all A to base, and whatever the seize does with B or shuffles among base outputs cannot move A to a non-base output without the scan rejecting./ledger-level value-conservation + witness-forcing analysis of the universal full-output-scan custody proof] Movement of pre-existing tokens with no mint of the policy is outside the scan and relies on the base->global/seize coupling (§3.5); the 'sound for EVERY shape' wording depends on that coupling remaining intact
- [The claim holds. The custody invariant for registered policies survives the Member/NonMember mint-walk redesign because the escape-critical accounting is driven by TWO retained, fully-authenticated aggregates that feed one aggregate containment check, and value conservation then pins non-base quantity.

Mechanics (ProgrammableLogicBase.hs): the global TransferAct builds expectedProgrammableOutputValue = filterPositive( union( totalProgTokenValue_ , programmableMintValue ) ) (lines 1142-1174) and requires it at the base credential via poutputsContainExpectedValueAtCred (line 1179, def 512-697), a per-(cs,tn) LOWER-BOUND check. For any registered policy A / token tn let I = A.tn summed over base inputs (produced by the transfer walk pcheckTransferLogicAndGetProgrammableValue, def 837-901, still fully authenticated), M = A.tn signed mint delta (from the real mint field). Containment forces base_out(A.tn) >= I + M. Ledger conservation gives base_out + nonbase_out = base_in + nonbase_in + M = I + N + M, so nonbase_out <= N. Non-base quantity cannot increase. Custody holds.

The redesign only affects M's provenance, and it can only make expected LARGER or equal for a registered policy, never smaller:
- Registered A can never obtain a NonMember tag: NonMember retains nodeKey < cs < nodeNext + phasCSH directory-NFT auth (current code 961-966; retained by claim). In an insert-only, sorted, unique-key directory the registered key cs is its own node and its predecessor's next-pointer equals cs, so no authentic node's (key,next) interval strictly contains cs. Registered -> NonMember is unsatisfiable, so A's positive mint can never be excluded from base.
- Member conses the real signed mint-field entry unmodified into programmableMintValue (self-penalizing: it only ADDS M to the base requirement). The attacker cannot inflate/deflate M (ledger-truth) nor skip it (lockstep proof consumption, "mint proof missing"/"extra mint proof", lines 973,979).
- The input side (I) is untouched and equally protected: transfer-walk member branch still requires nodeKey==currCS + phasCSH (837-889), so a registered base input cannot be dropped via a covering-node NonMember either.

Cross-policy / cross-name masking is impossible: pcurrencyPairsUnionFast (194-235) merges only equal currency symbols, ptokenPairsUnionFast (150-178) only equal token names, and pfilterPositiveCurrencyPairs (257-292) drops non-positive entries per (cs,tn). Distinct policy IDs never interact and policy IDs cannot collide (adversary cannot forge a script hash), so a false Member for an unregistered/negative policy cannot reduce any registered policy's required base amount. The §3.3 seize-masking shape (preserved input supply hiding a mint) does not apply here because the global uses the AGGREGATE I+M, not a per-UTxO correspondence, so masking fails containment.

Every constructed escape died on a concrete code fact, so I cannot produce a Conway-valid tx that increases a registered policy's out-of-base quantity while the global accepts./Conway-ledger adversarial construction of a custody-violating transaction against the redesigned Member/NonMember global mint walk] Directory integrity holds as trusted in spec §3.5: insert-only, sorted, unique keys, next-pointer linkage maintained on insert, node NFTs unforgeable via one-shot root, NFT-name==datum-key. If a wide-interval covering node keyed to exclude a registered cs could be forged/authenticated, a registered policy could obtain NonMember and escape.
- [The claim holds. The custody invariant for registered policies survives the Member/NonMember mint-walk redesign because the escape-critical accounting is driven by TWO retained, fully-authenticated aggregates that feed one aggregate containment check, and value conservation then pins non-base quantity.

Mechanics (ProgrammableLogicBase.hs): the global TransferAct builds expectedProgrammableOutputValue = filterPositive( union( totalProgTokenValue_ , programmableMintValue ) ) (lines 1142-1174) and requires it at the base credential via poutputsContainExpectedValueAtCred (line 1179, def 512-697), a per-(cs,tn) LOWER-BOUND check. For any registered policy A / token tn let I = A.tn summed over base inputs (produced by the transfer walk pcheckTransferLogicAndGetProgrammableValue, def 837-901, still fully authenticated), M = A.tn signed mint delta (from the real mint field). Containment forces base_out(A.tn) >= I + M. Ledger conservation gives base_out + nonbase_out = base_in + nonbase_in + M = I + N + M, so nonbase_out <= N. Non-base quantity cannot increase. Custody holds.

The redesign only affects M's provenance, and it can only make expected LARGER or equal for a registered policy, never smaller:
- Registered A can never obtain a NonMember tag: NonMember retains nodeKey < cs < nodeNext + phasCSH directory-NFT auth (current code 961-966; retained by claim). In an insert-only, sorted, unique-key directory the registered key cs is its own node and its predecessor's next-pointer equals cs, so no authentic node's (key,next) interval strictly contains cs. Registered -> NonMember is unsatisfiable, so A's positive mint can never be excluded from base.
- Member conses the real signed mint-field entry unmodified into programmableMintValue (self-penalizing: it only ADDS M to the base requirement). The attacker cannot inflate/deflate M (ledger-truth) nor skip it (lockstep proof consumption, "mint proof missing"/"extra mint proof", lines 973,979).
- The input side (I) is untouched and equally protected: transfer-walk member branch still requires nodeKey==currCS + phasCSH (837-889), so a registered base input cannot be dropped via a covering-node NonMember either.

Cross-policy / cross-name masking is impossible: pcurrencyPairsUnionFast (194-235) merges only equal currency symbols, ptokenPairsUnionFast (150-178) only equal token names, and pfilterPositiveCurrencyPairs (257-292) drops non-positive entries per (cs,tn). Distinct policy IDs never interact and policy IDs cannot collide (adversary cannot forge a script hash), so a false Member for an unregistered/negative policy cannot reduce any registered policy's required base amount. The §3.3 seize-masking shape (preserved input supply hiding a mint) does not apply here because the global uses the AGGREGATE I+M, not a per-UTxO correspondence, so masking fails containment.

Every constructed escape died on a concrete code fact, so I cannot produce a Conway-valid tx that increases a registered policy's out-of-base quantity while the global accepts./Conway-ledger adversarial construction of a custody-violating transaction against the redesigned Member/NonMember global mint walk] phasCSH first-non-Ada node authentication is sufficient only because directory NFTs are unforgeable and node datums are trustworthy; a spendable/re-datumable directory node would break the covering-interval guarantee.
- [The claim holds. The custody invariant for registered policies survives the Member/NonMember mint-walk redesign because the escape-critical accounting is driven by TWO retained, fully-authenticated aggregates that feed one aggregate containment check, and value conservation then pins non-base quantity.

Mechanics (ProgrammableLogicBase.hs): the global TransferAct builds expectedProgrammableOutputValue = filterPositive( union( totalProgTokenValue_ , programmableMintValue ) ) (lines 1142-1174) and requires it at the base credential via poutputsContainExpectedValueAtCred (line 1179, def 512-697), a per-(cs,tn) LOWER-BOUND check. For any registered policy A / token tn let I = A.tn summed over base inputs (produced by the transfer walk pcheckTransferLogicAndGetProgrammableValue, def 837-901, still fully authenticated), M = A.tn signed mint delta (from the real mint field). Containment forces base_out(A.tn) >= I + M. Ledger conservation gives base_out + nonbase_out = base_in + nonbase_in + M = I + N + M, so nonbase_out <= N. Non-base quantity cannot increase. Custody holds.

The redesign only affects M's provenance, and it can only make expected LARGER or equal for a registered policy, never smaller:
- Registered A can never obtain a NonMember tag: NonMember retains nodeKey < cs < nodeNext + phasCSH directory-NFT auth (current code 961-966; retained by claim). In an insert-only, sorted, unique-key directory the registered key cs is its own node and its predecessor's next-pointer equals cs, so no authentic node's (key,next) interval strictly contains cs. Registered -> NonMember is unsatisfiable, so A's positive mint can never be excluded from base.
- Member conses the real signed mint-field entry unmodified into programmableMintValue (self-penalizing: it only ADDS M to the base requirement). The attacker cannot inflate/deflate M (ledger-truth) nor skip it (lockstep proof consumption, "mint proof missing"/"extra mint proof", lines 973,979).
- The input side (I) is untouched and equally protected: transfer-walk member branch still requires nodeKey==currCS + phasCSH (837-889), so a registered base input cannot be dropped via a covering-node NonMember either.

Cross-policy / cross-name masking is impossible: pcurrencyPairsUnionFast (194-235) merges only equal currency symbols, ptokenPairsUnionFast (150-178) only equal token names, and pfilterPositiveCurrencyPairs (257-292) drops non-positive entries per (cs,tn). Distinct policy IDs never interact and policy IDs cannot collide (adversary cannot forge a script hash), so a false Member for an unregistered/negative policy cannot reduce any registered policy's required base amount. The §3.3 seize-masking shape (preserved input supply hiding a mint) does not apply here because the global uses the AGGREGATE I+M, not a per-UTxO correspondence, so masking fails containment.

Every constructed escape died on a concrete code fact, so I cannot produce a Conway-valid tx that increases a registered policy's out-of-base quantity while the global accepts./Conway-ledger adversarial construction of a custody-violating transaction against the redesigned Member/NonMember global mint walk] Input-side transfer walk (pcheckTransferLogicAndGetProgrammableValue) MUST retain full member+covering authentication (unchanged as spec §11.3 states); the deletion is sound only on the mint side.
- [The claim holds. The custody invariant for registered policies survives the Member/NonMember mint-walk redesign because the escape-critical accounting is driven by TWO retained, fully-authenticated aggregates that feed one aggregate containment check, and value conservation then pins non-base quantity.

Mechanics (ProgrammableLogicBase.hs): the global TransferAct builds expectedProgrammableOutputValue = filterPositive( union( totalProgTokenValue_ , programmableMintValue ) ) (lines 1142-1174) and requires it at the base credential via poutputsContainExpectedValueAtCred (line 1179, def 512-697), a per-(cs,tn) LOWER-BOUND check. For any registered policy A / token tn let I = A.tn summed over base inputs (produced by the transfer walk pcheckTransferLogicAndGetProgrammableValue, def 837-901, still fully authenticated), M = A.tn signed mint delta (from the real mint field). Containment forces base_out(A.tn) >= I + M. Ledger conservation gives base_out + nonbase_out = base_in + nonbase_in + M = I + N + M, so nonbase_out <= N. Non-base quantity cannot increase. Custody holds.

The redesign only affects M's provenance, and it can only make expected LARGER or equal for a registered policy, never smaller:
- Registered A can never obtain a NonMember tag: NonMember retains nodeKey < cs < nodeNext + phasCSH directory-NFT auth (current code 961-966; retained by claim). In an insert-only, sorted, unique-key directory the registered key cs is its own node and its predecessor's next-pointer equals cs, so no authentic node's (key,next) interval strictly contains cs. Registered -> NonMember is unsatisfiable, so A's positive mint can never be excluded from base.
- Member conses the real signed mint-field entry unmodified into programmableMintValue (self-penalizing: it only ADDS M to the base requirement). The attacker cannot inflate/deflate M (ledger-truth) nor skip it (lockstep proof consumption, "mint proof missing"/"extra mint proof", lines 973,979).
- The input side (I) is untouched and equally protected: transfer-walk member branch still requires nodeKey==currCS + phasCSH (837-889), so a registered base input cannot be dropped via a covering-node NonMember either.

Cross-policy / cross-name masking is impossible: pcurrencyPairsUnionFast (194-235) merges only equal currency symbols, ptokenPairsUnionFast (150-178) only equal token names, and pfilterPositiveCurrencyPairs (257-292) drops non-positive entries per (cs,tn). Distinct policy IDs never interact and policy IDs cannot collide (adversary cannot forge a script hash), so a false Member for an unregistered/negative policy cannot reduce any registered policy's required base amount. The §3.3 seize-masking shape (preserved input supply hiding a mint) does not apply here because the global uses the AGGREGATE I+M, not a per-UTxO correspondence, so masking fails containment.

Every constructed escape died on a concrete code fact, so I cannot produce a Conway-valid tx that increases a registered policy's out-of-base quantity while the global accepts./Conway-ledger adversarial construction of a custody-violating transaction against the redesigned Member/NonMember global mint walk] Base spending validator MUST continue to require the global (or seize) withdrawal so base inputs cannot be moved without the global running and enforcing containment (spec §3.5, code 714-727).
- [The claim holds. The custody invariant for registered policies survives the Member/NonMember mint-walk redesign because the escape-critical accounting is driven by TWO retained, fully-authenticated aggregates that feed one aggregate containment check, and value conservation then pins non-base quantity.

Mechanics (ProgrammableLogicBase.hs): the global TransferAct builds expectedProgrammableOutputValue = filterPositive( union( totalProgTokenValue_ , programmableMintValue ) ) (lines 1142-1174) and requires it at the base credential via poutputsContainExpectedValueAtCred (line 1179, def 512-697), a per-(cs,tn) LOWER-BOUND check. For any registered policy A / token tn let I = A.tn summed over base inputs (produced by the transfer walk pcheckTransferLogicAndGetProgrammableValue, def 837-901, still fully authenticated), M = A.tn signed mint delta (from the real mint field). Containment forces base_out(A.tn) >= I + M. Ledger conservation gives base_out + nonbase_out = base_in + nonbase_in + M = I + N + M, so nonbase_out <= N. Non-base quantity cannot increase. Custody holds.

The redesign only affects M's provenance, and it can only make expected LARGER or equal for a registered policy, never smaller:
- Registered A can never obtain a NonMember tag: NonMember retains nodeKey < cs < nodeNext + phasCSH directory-NFT auth (current code 961-966; retained by claim). In an insert-only, sorted, unique-key directory the registered key cs is its own node and its predecessor's next-pointer equals cs, so no authentic node's (key,next) interval strictly contains cs. Registered -> NonMember is unsatisfiable, so A's positive mint can never be excluded from base.
- Member conses the real signed mint-field entry unmodified into programmableMintValue (self-penalizing: it only ADDS M to the base requirement). The attacker cannot inflate/deflate M (ledger-truth) nor skip it (lockstep proof consumption, "mint proof missing"/"extra mint proof", lines 973,979).
- The input side (I) is untouched and equally protected: transfer-walk member branch still requires nodeKey==currCS + phasCSH (837-889), so a registered base input cannot be dropped via a covering-node NonMember either.

Cross-policy / cross-name masking is impossible: pcurrencyPairsUnionFast (194-235) merges only equal currency symbols, ptokenPairsUnionFast (150-178) only equal token names, and pfilterPositiveCurrencyPairs (257-292) drops non-positive entries per (cs,tn). Distinct policy IDs never interact and policy IDs cannot collide (adversary cannot forge a script hash), so a false Member for an unregistered/negative policy cannot reduce any registered policy's required base amount. The §3.3 seize-masking shape (preserved input supply hiding a mint) does not apply here because the global uses the AGGREGATE I+M, not a per-UTxO correspondence, so masking fails containment.

Every constructed escape died on a concrete code fact, so I cannot produce a Conway-valid tx that increases a registered policy's out-of-base quantity while the global accepts./Conway-ledger adversarial construction of a custody-violating transaction against the redesigned Member/NonMember global mint walk] Mint walk MUST keep exact lockstep proof consumption (no omission, no extras) so every registered minted policy is forced through Member.
- [Deleting psingleMintWithCredential is safe because within one protocol instance the applied issuance script is a pure function of (instance-constant params, mintingLogicHash); identical logic hash gives an identical script, hence the same policy ID, so a second policy sharing one logic cannot exist in-instance. Cross-deployment reuse is the logic author's documented contract, matching Aiken./cross-validator composition: multi-policy mints, register-and-mint, cross-instance ride-alongs, registration forgery, one-shot NFT reuse, and the actual offchain/on-chain policy-ID derivation] Every applied issuance argument other than mintingLogicHash must remain an instance-unique constant derived from one-shot provenance (protocolParamsCS in the redesign; today progLogicBaseCred + directoryNodeCS). Introducing any per-token varying argument (nonce, TxIn, version tag) collapses the identical-policy-ID argument and silently reopens the free-rider hazard the scan used to block.
- [Deleting psingleMintWithCredential is safe because within one protocol instance the applied issuance script is a pure function of (instance-constant params, mintingLogicHash); identical logic hash gives an identical script, hence the same policy ID, so a second policy sharing one logic cannot exist in-instance. Cross-deployment reuse is the logic author's documented contract, matching Aiken./cross-validator composition: multi-policy mints, register-and-mint, cross-instance ride-alongs, registration forgery, one-shot NFT reuse, and the actual offchain/on-chain policy-ID derivation] The two compile sites must stay byte-identical: Scripts.hs:128 uses tryCompile Production while IssuanceCborHexRef.hs:52 uses compile NoTracing — any config divergence breaks the prefix/postfix equation that makes non-template look-alikes unregistrable (and breaks registration liveness). The redesign must preserve this exact-mirror property and the logic-hash-last argument order.
- [Deleting psingleMintWithCredential is safe because within one protocol instance the applied issuance script is a pure function of (instance-constant params, mintingLogicHash); identical logic hash gives an identical script, hence the same policy ID, so a second policy sharing one logic cannot exist in-instance. Cross-deployment reuse is the logic author's documented contract, matching Aiken./cross-validator composition: multi-policy mints, register-and-mint, cross-instance ride-alongs, registration forgery, one-shot NFT reuse, and the actual offchain/on-chain policy-ID derivation] The one-shot IssuanceCborHex anchor (or its redesign successor) must remain the sole source of prefix/postfix, and registration must keep the _papplyHashedParameter equation plus duplicate-key impossibility — these, not the deleted scan, are now what confine 'one logic -> one policy' within an instance.
- [Deleting psingleMintWithCredential is safe because within one protocol instance the applied issuance script is a pure function of (instance-constant params, mintingLogicHash); identical logic hash gives an identical script, hence the same policy ID, so a second policy sharing one logic cannot exist in-instance. Cross-deployment reuse is the logic author's documented contract, matching Aiken./cross-validator composition: multi-policy mints, register-and-mint, cross-instance ride-alongs, registration forgery, one-shot NFT reuse, and the actual offchain/on-chain policy-ID derivation] Cross-instance ride-along (one L withdrawal authorizing mints under two deployments' policies) becomes POSSIBLE and passes all validators; safety rests entirely on section 10.4 being shipped as the documented minting-logic author contract, with logic authors inspecting the mint field(s) they approve. This is Aiken parity, not an improvement over it.
- [Deleting psingleMintWithCredential is safe because within one protocol instance the applied issuance script is a pure function of (instance-constant params, mintingLogicHash); identical logic hash gives an identical script, hence the same policy ID, so a second policy sharing one logic cannot exist in-instance. Cross-deployment reuse is the logic author's documented contract, matching Aiken./cross-validator composition: multi-policy mints, register-and-mint, cross-instance ride-alongs, registration forgery, one-shot NFT reuse, and the actual offchain/on-chain policy-ID derivation] The claim's 'full coordinated redeploy, nothing deployed' premise (section 2.2) must actually hold at genesis: if any old-template issuance policy from a prior deployment were grandfathered into the new instance's directory, old/new version coexistence under one logic — the scan's historical target — returns with no on-chain defense.
- [Deleting psingleMintWithCredential (Issuance.hs:204-252) is safe because within one protocol instance an identical minting-logic hash yields an identical applied two-parameter script and therefore the same policy ID, so a "second policy sharing one logic" cannot exist in-instance; cross-deployment reuse is the logic author's documented contract (spec 2.2, 7.1)./adversarial-verification: gap-hunt between claimed and actual behavior of the issuance policy, offchain script derivation, CBOR-split registration, and directory insert path] Every positive-mint path of the redesigned policy stays registration-gated (Local carries RegistrationWitness; Delegate arms carry nodeRefIdx over a reference input) and BurnOnly rejects ANY positive entry in the ownCS mint lookup per token name — otherwise an unregistered byte-variant template sharing L could mint outside the L->policyID bijection
- [Deleting psingleMintWithCredential (Issuance.hs:204-252) is safe because within one protocol instance an identical minting-logic hash yields an identical applied two-parameter script and therefore the same policy ID, so a "second policy sharing one logic" cannot exist in-instance; cross-deployment reuse is the logic author's documented contract (spec 2.2, 7.1)./adversarial-verification: gap-hunt between claimed and actual behavior of the issuance policy, offchain script derivation, CBOR-split registration, and directory insert path] The applied script remains a pure function of (protocolParamsCS, mintingLogicHash): no nonce/version/TxIn parameter is ever added to the template, mintingLogicHash stays the LAST applied argument, and the offchain issuance pipeline and issuerPrefixPostfixBytes keep byte-identical compile configs (today both NoTracing; Scripts.hs ignores its target arg — keep it pinned)
- [Deleting psingleMintWithCredential (Issuance.hs:204-252) is safe because within one protocol instance an identical minting-logic hash yields an identical applied two-parameter script and therefore the same policy ID, so a "second policy sharing one logic" cannot exist in-instance; cross-deployment reuse is the logic author's documented contract (spec 2.2, 7.1)./adversarial-verification: gap-hunt between claimed and actual behavior of the issuance policy, offchain script derivation, CBOR-split registration, and directory insert path] The prefix/postfix issuanceCborHex NFT is regenerated for the new 2-param template at redeploy and the registration preimage check (Common.hs:304) plus the withdrawal-consent check are retained unchanged; the one-shot + always-fail lock on that NFT is preserved
- [Deleting psingleMintWithCredential (Issuance.hs:204-252) is safe because within one protocol instance an identical minting-logic hash yields an identical applied two-parameter script and therefore the same policy ID, so a "second policy sharing one logic" cannot exist in-instance; cross-deployment reuse is the logic author's documented contract (spec 2.2, 7.1)./adversarial-verification: gap-hunt between claimed and actual behavior of the issuance policy, offchain script derivation, CBOR-split registration, and directory insert path] The directory remains insert-only with strict-covering unique keys (no node update/removal redeemer is ever added)
- [Deleting psingleMintWithCredential (Issuance.hs:204-252) is safe because within one protocol instance an identical minting-logic hash yields an identical applied two-parameter script and therefore the same policy ID, so a "second policy sharing one logic" cannot exist in-instance; cross-deployment reuse is the logic author's documented contract (spec 2.2, 7.1)./adversarial-verification: gap-hunt between claimed and actual behavior of the issuance policy, offchain script derivation, CBOR-split registration, and directory insert path] The 'nothing is actively deployed' premise holds — no older-template instance reachable by the same minting logics exists on the target network at redeploy
- [Deleting psingleMintWithCredential (Issuance.hs:204-252) is safe because within one protocol instance an identical minting-logic hash yields an identical applied two-parameter script and therefore the same policy ID, so a "second policy sharing one logic" cannot exist in-instance; cross-deployment reuse is the logic author's documented contract (spec 2.2, 7.1)./adversarial-verification: gap-hunt between claimed and actual behavior of the issuance policy, offchain script derivation, CBOR-split registration, and directory insert path] sect. 10.4 minting-logic author contract (must pin its expected policy ID / inspect what it approves, because one withdrawal invocation authorizes the whole tx across protocol deployments) is actually written and shipped with the logic-author docs
- [Deleting psingleMintWithCredential (Issuance.hs:204-252) is safe because within one protocol instance an identical minting-logic hash yields an identical applied two-parameter script and therefore the same policy ID, so a "second policy sharing one logic" cannot exist in-instance; cross-deployment reuse is the logic author's documented contract (spec 2.2, 7.1)./adversarial-verification: gap-hunt between claimed and actual behavior of the issuance policy, offchain script derivation, CBOR-split registration, and directory insert path] Standard assumptions: blake2b-224 collision resistance and the ledger's exact purpose/redeemer and one-mint-entry-per-CS invariants (spec 3.1)
- [The universal full output scan (spec section 8) — every tx output either has payment credential == progLogicCred (from the authenticated params datum) or holds zero ownCS tokens — is sound as the sole Local custody mode for every transaction shape, because combined with ledger non-ADA value conservation it makes creation of any non-base ownCS output impossible regardless of inputs, mint sign, or co-running validators; in particular a seize-of-B tx that mints A locally cannot be gamed./cross-validator composition: co-occurrence of Local issuance with seize, global TransferAct, register-and-mint, multiple issuance policies, and multiple params-reading validators; one validator satisfying its invariant while creating state another misreads] Section 4.1 params-anchor hardening must actually be implemented in mkProtocolParametersMinting (one-shot input spent; exactly one NFT and nothing else under ownCS; anchor at alwaysFailScript with value exactly min-ada + NFT; four-field datum fully decoded at mint) — the runtime first-non-Ada phasCSH authentication and raw datum field access are only sound under it; the current ProtocolParams.hs has the 2-field datum and lacks these checks, so shipping the scan against the current anchor policy is not yet licensed
- [The universal full output scan (spec section 8) — every tx output either has payment credential == progLogicCred (from the authenticated params datum) or holds zero ownCS tokens — is sound as the sole Local custody mode for every transaction shape, because combined with ledger non-ADA value conservation it makes creation of any non-base ownCS output impossible regardless of inputs, mint sign, or co-running validators; in particular a seize-of-B tx that mints A locally cannot be gamed./cross-validator composition: co-occurrence of Local issuance with seize, global TransferAct, register-and-mint, multiple issuance policies, and multiple params-reading validators; one validator satisfying its invariant while creating state another misreads] Deployment consistency (trusted genesis, section 3.1/12): the params datum's progLogicCred must equal the credential of the actual base spending script that forwards to global/seize; the scan compares against the datum field, so a wrong field at genesis redirects custody wholesale
- [The universal full output scan (spec section 8) — every tx output either has payment credential == progLogicCred (from the authenticated params datum) or holds zero ownCS tokens — is sound as the sole Local custody mode for every transaction shape, because combined with ledger non-ADA value conservation it makes creation of any non-base ownCS output impossible regardless of inputs, mint sign, or co-running validators; in particular a seize-of-B tx that mints A locally cannot be gamed./cross-validator composition: co-occurrence of Local issuance with seize, global TransferAct, register-and-mint, multiple issuance policies, and multiple params-reading validators; one validator satisfying its invariant while creating state another misreads] The custody invariant is payment-credential-only (section 3.2 as defined, section 2.2 exclusion): the spec design drops the current implementation's inline-stake-credential check (Issuance.hs:165-187), so minted tokens may land at base with arbitrary, pointer, or absent stake credentials — attacker-stake or unattributable/locked base UTxOs are representable; this is ownership/attribution risk the claim explicitly excludes, not an escape, but it is a real behavioral regression vs the current code
- [The universal full output scan (spec section 8) — every tx output either has payment credential == progLogicCred (from the authenticated params datum) or holds zero ownCS tokens — is sound as the sole Local custody mode for every transaction shape, because combined with ledger non-ADA value conservation it makes creation of any non-base ownCS output impossible regardless of inputs, mint sign, or co-running validators; in particular a seize-of-B tx that mints A locally cannot be gamed./cross-validator composition: co-occurrence of Local issuance with seize, global TransferAct, register-and-mint, multiple issuance policies, and multiple params-reading validators; one validator satisfying its invariant while creating state another misreads] The implementation must iterate the full txInfo.outputs list unconditionally (no index shortcut), interpret 'zero tokens of ownCS' as the entire ownCS token-name map absent (any token name), and its sorted-value short-circuit is sound only because ledger-provided script-context values are canonically sorted
- [The universal full output scan (spec section 8) — every tx output either has payment credential == progLogicCred (from the authenticated params datum) or holds zero ownCS tokens — is sound as the sole Local custody mode for every transaction shape, because combined with ledger non-ADA value conservation it makes creation of any non-base ownCS output impossible regardless of inputs, mint sign, or co-running validators; in particular a seize-of-B tx that mints A locally cannot be gamed./cross-validator composition: co-occurrence of Local issuance with seize, global TransferAct, register-and-mint, multiple issuance policies, and multiple params-reading validators; one validator satisfying its invariant while creating state another misreads] Companion couplings pinned by section 3.5/14.1 must be maintained for the SURROUNDING claims (burn-only soundness, seize correspondence exactness); the scan itself needs none of them, but the seize-of-A-escape reasoning above cites the current seize code — future seize edits must re-run the 14.1 suite
- [Spec §11.3: mint-walk Member proofs may drop node reference, datum decode, node authentication, and transfer-logic withdrawal check (all four at ProgrammableLogicBase.hs:936-959 today), consing the signed mint entry directly into the programmable mint value that containment requires at the base credential; only NonMember keeps covering-node authentication. Claimed sound because Member is self-penalizing (only ADDS delta to containment), the sole escape-critical direction is registered-policy-obtains-NonMember (blocked by retained covering-node auth), and negative false-Member entries are inert because containment is per-(policy, token-name)./Cross-validator composition: shapes where global+seize, global+multiple issuance policies, register-and-mint+global, or seize+Local-mint co-occur in one transaction, and whether one validator's satisfied invariant creates state another misreads.] The redesigned Member case must cons the VERBATIM signed ledger mint entry and the walk must keep the accumulator reversal (preverseCurrencyPairs, as at line 979/895) — pcurrencyPairsUnionFast and the containment subtract walk require canonically ascending input (comment 294-307); an unsorted delta could under-require outputs.
- [Spec §11.3: mint-walk Member proofs may drop node reference, datum decode, node authentication, and transfer-logic withdrawal check (all four at ProgrammableLogicBase.hs:936-959 today), consing the signed mint entry directly into the programmable mint value that containment requires at the base credential; only NonMember keeps covering-node authentication. Claimed sound because Member is self-penalizing (only ADDS delta to containment), the sole escape-critical direction is registered-policy-obtains-NonMember (blocked by retained covering-node auth), and negative false-Member entries are inert because containment is per-(policy, token-name)./Cross-validator composition: shapes where global+seize, global+multiple issuance policies, register-and-mint+global, or seize+Local-mint co-occur in one transaction, and whether one validator's satisfied invariant creates state another misreads.] pfilterPositiveCurrencyPairs (or equivalent per-name non-positive filter) must remain applied to the merged delta before containment (as at 1157-1174) — the subtract walk's strict-positivity precondition (496-500) is what makes negative entries merely inert rather than corrupting.
- [Spec §11.3: mint-walk Member proofs may drop node reference, datum decode, node authentication, and transfer-logic withdrawal check (all four at ProgrammableLogicBase.hs:936-959 today), consing the signed mint entry directly into the programmable mint value that containment requires at the base credential; only NonMember keeps covering-node authentication. Claimed sound because Member is self-penalizing (only ADDS delta to containment), the sole escape-critical direction is registered-policy-obtains-NonMember (blocked by retained covering-node auth), and negative false-Member entries are inert because containment is per-(policy, token-name)./Cross-validator composition: shapes where global+seize, global+multiple issuance policies, register-and-mint+global, or seize+Local-mint co-occur in one transaction, and whether one validator's satisfied invariant creates state another misreads.] Lockstep proof/mint-entry consumption with missing/extra-proof failure (973/979) must be preserved — the self-penalizing argument assumes every minted policy is classified; a skippable entry would be a full bypass, not a misclassification.
- [Spec §11.3: mint-walk Member proofs may drop node reference, datum decode, node authentication, and transfer-logic withdrawal check (all four at ProgrammableLogicBase.hs:936-959 today), consing the signed mint entry directly into the programmable mint value that containment requires at the base credential; only NonMember keeps covering-node authentication. Claimed sound because Member is self-penalizing (only ADDS delta to containment), the sole escape-critical direction is registered-policy-obtains-NonMember (blocked by retained covering-node auth), and negative false-Member entries are inert because containment is per-(policy, token-name)./Cross-validator composition: shapes where global+seize, global+multiple issuance policies, register-and-mint+global, or seize+Local-mint co-occur in one transaction, and whether one validator's satisfied invariant creates state another misreads.] NonMember must keep BOTH interval bounds plus the phasCSH directory-NFT check, and directory integrity must hold (dir-NFTs only in authentic nodes, insertion consumes the covering node, node datums immutable at the directory validator) — the entire escape-critical direction rests on this.
- [Spec §11.3: mint-walk Member proofs may drop node reference, datum decode, node authentication, and transfer-logic withdrawal check (all four at ProgrammableLogicBase.hs:936-959 today), consing the signed mint entry directly into the programmable mint value that containment requires at the base credential; only NonMember keeps covering-node authentication. Claimed sound because Member is self-penalizing (only ADDS delta to containment), the sole escape-critical direction is registered-policy-obtains-NonMember (blocked by retained covering-node auth), and negative false-Member entries are inert because containment is per-(policy, token-name)./Cross-validator composition: shapes where global+seize, global+multiple issuance policies, register-and-mint+global, or seize+Local-mint co-occur in one transaction, and whether one validator's satisfied invariant creates state another misreads.] Input-side transfer proofs stay unchanged (spec test 14.1.14): spending or burning base-held tokens of P still requires P's transfer-logic withdrawal; the removal is mint-side only.
- [Spec §11.3: mint-walk Member proofs may drop node reference, datum decode, node authentication, and transfer-logic withdrawal check (all four at ProgrammableLogicBase.hs:936-959 today), consing the signed mint entry directly into the programmable mint value that containment requires at the base credential; only NonMember keeps covering-node authentication. Claimed sound because Member is self-penalizing (only ADDS delta to containment), the sole escape-critical direction is registered-policy-obtains-NonMember (blocked by retained covering-node auth), and negative false-Member entries are inert because containment is per-(policy, token-name)./Cross-validator composition: shapes where global+seize, global+multiple issuance policies, register-and-mint+global, or seize+Local-mint co-occur in one transaction, and whether one validator's satisfied invariant creates state another misreads.] The global must keep rejecting non-TransferAct redeemers (1196-1197), and issuance DelegateTransfer must verify the same-instance global credential's withdrawal, so 'global present' always means this TransferAct code ran to completion.
- [Spec §11.3: mint-walk Member proofs may drop node reference, datum decode, node authentication, and transfer-logic withdrawal check (all four at ProgrammableLogicBase.hs:936-959 today), consing the signed mint entry directly into the programmable mint value that containment requires at the base credential; only NonMember keeps covering-node authentication. Claimed sound because Member is self-penalizing (only ADDS delta to containment), the sole escape-critical direction is registered-policy-obtains-NonMember (blocked by retained covering-node auth), and negative false-Member entries are inert because containment is per-(policy, token-name)./Cross-validator composition: shapes where global+seize, global+multiple issuance policies, register-and-mint+global, or seize+Local-mint co-occur in one transaction, and whether one validator's satisfied invariant creates state another misreads.] Register-and-mint must remain unable to satisfy Delegate modes pre-insert (nodeRefIdx exact-ownCS-NFT check, spec §14.1.4), so the Local no_escape scan custodies the one shape where a not-yet-registered policy is minted while the global runs — the claim's soundness for that shape comes from the issuance policy, not from the global.
- [Spec §11.3: mint-walk Member proofs may drop node reference, datum decode, node authentication, and transfer-logic withdrawal check (all four at ProgrammableLogicBase.hs:936-959 today), consing the signed mint entry directly into the programmable mint value that containment requires at the base credential; only NonMember keeps covering-node authentication. Claimed sound because Member is self-penalizing (only ADDS delta to containment), the sole escape-critical direction is registered-policy-obtains-NonMember (blocked by retained covering-node auth), and negative false-Member entries are inert because containment is per-(policy, token-name)./Cross-validator composition: shapes where global+seize, global+multiple issuance policies, register-and-mint+global, or seize+Local-mint co-occur in one transaction, and whether one validator's satisfied invariant creates state another misreads.] Deployment is a full redeploy (spec §12): no legacy tokens whose transfer-logic scripts assumed they would be invoked on mints carry into the new instance; minting-logic authors operate under the §10.4 contract.
- [The BurnOnly witness is sound; I could not construct any ledger-valid tx that escapes registered-policy supply while all validators accept. The argument's spine is airtight under per-asset value conservation:

Custody breach for ownCS = "quantity of ownCS outside progLogicCred increases." By ledger conservation, total_out(ownCS) = total_in(ownCS) + mint(ownCS). BurnOnly's only substantive check forces mint(ownCS) <= 0 (no positive entry), so total_out <= total_in. Therefore nonbase_out = total_out - base_out <= total_in - base_out; for nonbase_out > nonbase_in = total_in - base_in we need base_out < base_in, i.e. a base UTxO holding ownCS MUST be spent. "Base" is definitionally the progLogicCred payment credential, so any such spend invokes the base spending validator (mkProgrammableLogicBase), which requires the global OR seize withdrawal credential to be present (an actual script, so it runs and enforces over the whole tx).

- TransferAct branch: pvalueFromCred aggregates EVERY base input's non-Ada value with no silent omission (missing owner witness -> perror; pjustData on a stake-less base input -> phead-on-empty error), so ownCS's spent base value is in totalProgTokenValue. Its input-side transfer proof must be Member (registered ownCS can never satisfy the covering nodeKey<cs<nodeNext test on an insert-only sorted directory), so it is retained. Containment (poutputsContainExpectedValueAtCred, a >= lower bound) then forces base_out(ownCS) >= base_input(ownCS) + mint(ownCS), which combined with conservation pins nonbase_out <= nonbase_in. Burning only shrinks the required amount by exactly the burned quantity, which conservation destroys — no slack.
- Seize branch (ownCS not seized): the walk visits every input, pairs each base input with a same-address (=base) output, and pvalueEqualsDeltaCurrencySymbol perrors if any non-seized policy differs, so ownCS is preserved exactly at base. A burn of base-held ownCS is simply unbalanceable there.
- Seize branch (ownCS seized): ownCS may move but only between base outputs; the seized policy's signed mint map is folded into the balance and required in leftover progLogicCred outputs (checkBalanceInvariant counts only base outputs).
- Pre-escaped (non-base) ownCS inputs are skipped by seize / bounded by conservation under TransferAct; nonbase_out <= nonbase_in in every case.

Registration re-proof is genuinely redundant: ownCS supply can only exist if ownCS minted before, every positive-mint issuance arm (Local/Delegate) requires a registration/nodeRef proof, and the directory is insert-only, so the node persists and blocks any non-member exemption. This is exactly what makes the registered->non-member direction (the sole escape) unsatisfiable. The redesign's nullary mint-side MintProof.Member does not weaken this: it leaves the input-side transfer proof (the load-bearing one) authenticated, and a false Member tag is self-penalizing (adds the tagger's own delta to base containment)./Conway-ledger value-conservation + companion-validator coupling audit of the BurnOnly custody skip] Verification is at the argument/spec level: the redesigned two-parameter issuance policy and 4-constructor redeemer are NOT yet implemented; I audited the couplings in the current companion validators (base/global/seize), which embody them.
- [The BurnOnly witness is sound; I could not construct any ledger-valid tx that escapes registered-policy supply while all validators accept. The argument's spine is airtight under per-asset value conservation:

Custody breach for ownCS = "quantity of ownCS outside progLogicCred increases." By ledger conservation, total_out(ownCS) = total_in(ownCS) + mint(ownCS). BurnOnly's only substantive check forces mint(ownCS) <= 0 (no positive entry), so total_out <= total_in. Therefore nonbase_out = total_out - base_out <= total_in - base_out; for nonbase_out > nonbase_in = total_in - base_in we need base_out < base_in, i.e. a base UTxO holding ownCS MUST be spent. "Base" is definitionally the progLogicCred payment credential, so any such spend invokes the base spending validator (mkProgrammableLogicBase), which requires the global OR seize withdrawal credential to be present (an actual script, so it runs and enforces over the whole tx).

- TransferAct branch: pvalueFromCred aggregates EVERY base input's non-Ada value with no silent omission (missing owner witness -> perror; pjustData on a stake-less base input -> phead-on-empty error), so ownCS's spent base value is in totalProgTokenValue. Its input-side transfer proof must be Member (registered ownCS can never satisfy the covering nodeKey<cs<nodeNext test on an insert-only sorted directory), so it is retained. Containment (poutputsContainExpectedValueAtCred, a >= lower bound) then forces base_out(ownCS) >= base_input(ownCS) + mint(ownCS), which combined with conservation pins nonbase_out <= nonbase_in. Burning only shrinks the required amount by exactly the burned quantity, which conservation destroys — no slack.
- Seize branch (ownCS not seized): the walk visits every input, pairs each base input with a same-address (=base) output, and pvalueEqualsDeltaCurrencySymbol perrors if any non-seized policy differs, so ownCS is preserved exactly at base. A burn of base-held ownCS is simply unbalanceable there.
- Seize branch (ownCS seized): ownCS may move but only between base outputs; the seized policy's signed mint map is folded into the balance and required in leftover progLogicCred outputs (checkBalanceInvariant counts only base outputs).
- Pre-escaped (non-base) ownCS inputs are skipped by seize / bounded by conservation under TransferAct; nonbase_out <= nonbase_in in every case.

Registration re-proof is genuinely redundant: ownCS supply can only exist if ownCS minted before, every positive-mint issuance arm (Local/Delegate) requires a registration/nodeRef proof, and the directory is insert-only, so the node persists and blocks any non-member exemption. This is exactly what makes the registered->non-member direction (the sole escape) unsatisfiable. The redesign's nullary mint-side MintProof.Member does not weaken this: it leaves the input-side transfer proof (the load-bearing one) authenticated, and a false Member tag is self-penalizing (adds the tagger's own delta to base containment)./Conway-ledger value-conservation + companion-validator coupling audit of the BurnOnly custody skip] Soundness depends entirely on the section 3.5 couplings remaining intact: base requires global-or-seize on every base spend; global aggregates ALL base inputs with no silent omission and enforces lower-bound containment; seize preserves non-seized policies exactly and keeps the seized policy within base outputs.
- [The BurnOnly witness is sound; I could not construct any ledger-valid tx that escapes registered-policy supply while all validators accept. The argument's spine is airtight under per-asset value conservation:

Custody breach for ownCS = "quantity of ownCS outside progLogicCred increases." By ledger conservation, total_out(ownCS) = total_in(ownCS) + mint(ownCS). BurnOnly's only substantive check forces mint(ownCS) <= 0 (no positive entry), so total_out <= total_in. Therefore nonbase_out = total_out - base_out <= total_in - base_out; for nonbase_out > nonbase_in = total_in - base_in we need base_out < base_in, i.e. a base UTxO holding ownCS MUST be spent. "Base" is definitionally the progLogicCred payment credential, so any such spend invokes the base spending validator (mkProgrammableLogicBase), which requires the global OR seize withdrawal credential to be present (an actual script, so it runs and enforces over the whole tx).

- TransferAct branch: pvalueFromCred aggregates EVERY base input's non-Ada value with no silent omission (missing owner witness -> perror; pjustData on a stake-less base input -> phead-on-empty error), so ownCS's spent base value is in totalProgTokenValue. Its input-side transfer proof must be Member (registered ownCS can never satisfy the covering nodeKey<cs<nodeNext test on an insert-only sorted directory), so it is retained. Containment (poutputsContainExpectedValueAtCred, a >= lower bound) then forces base_out(ownCS) >= base_input(ownCS) + mint(ownCS), which combined with conservation pins nonbase_out <= nonbase_in. Burning only shrinks the required amount by exactly the burned quantity, which conservation destroys — no slack.
- Seize branch (ownCS not seized): the walk visits every input, pairs each base input with a same-address (=base) output, and pvalueEqualsDeltaCurrencySymbol perrors if any non-seized policy differs, so ownCS is preserved exactly at base. A burn of base-held ownCS is simply unbalanceable there.
- Seize branch (ownCS seized): ownCS may move but only between base outputs; the seized policy's signed mint map is folded into the balance and required in leftover progLogicCred outputs (checkBalanceInvariant counts only base outputs).
- Pre-escaped (non-base) ownCS inputs are skipped by seize / bounded by conservation under TransferAct; nonbase_out <= nonbase_in in every case.

Registration re-proof is genuinely redundant: ownCS supply can only exist if ownCS minted before, every positive-mint issuance arm (Local/Delegate) requires a registration/nodeRef proof, and the directory is insert-only, so the node persists and blocks any non-member exemption. This is exactly what makes the registered->non-member direction (the sole escape) unsatisfiable. The redesign's nullary mint-side MintProof.Member does not weaken this: it leaves the input-side transfer proof (the load-bearing one) authenticated, and a false Member tag is self-penalizing (adds the tagger's own delta to base containment)./Conway-ledger value-conservation + companion-validator coupling audit of the BurnOnly custody skip] The directory must remain insert-only, sorted, unique-keyed so a registered policy can never obtain a valid covering/non-member proof (relied upon, pinned by section 3.5; the directory policy code itself was not re-audited here).
- [The BurnOnly witness is sound; I could not construct any ledger-valid tx that escapes registered-policy supply while all validators accept. The argument's spine is airtight under per-asset value conservation:

Custody breach for ownCS = "quantity of ownCS outside progLogicCred increases." By ledger conservation, total_out(ownCS) = total_in(ownCS) + mint(ownCS). BurnOnly's only substantive check forces mint(ownCS) <= 0 (no positive entry), so total_out <= total_in. Therefore nonbase_out = total_out - base_out <= total_in - base_out; for nonbase_out > nonbase_in = total_in - base_in we need base_out < base_in, i.e. a base UTxO holding ownCS MUST be spent. "Base" is definitionally the progLogicCred payment credential, so any such spend invokes the base spending validator (mkProgrammableLogicBase), which requires the global OR seize withdrawal credential to be present (an actual script, so it runs and enforces over the whole tx).

- TransferAct branch: pvalueFromCred aggregates EVERY base input's non-Ada value with no silent omission (missing owner witness -> perror; pjustData on a stake-less base input -> phead-on-empty error), so ownCS's spent base value is in totalProgTokenValue. Its input-side transfer proof must be Member (registered ownCS can never satisfy the covering nodeKey<cs<nodeNext test on an insert-only sorted directory), so it is retained. Containment (poutputsContainExpectedValueAtCred, a >= lower bound) then forces base_out(ownCS) >= base_input(ownCS) + mint(ownCS), which combined with conservation pins nonbase_out <= nonbase_in. Burning only shrinks the required amount by exactly the burned quantity, which conservation destroys — no slack.
- Seize branch (ownCS not seized): the walk visits every input, pairs each base input with a same-address (=base) output, and pvalueEqualsDeltaCurrencySymbol perrors if any non-seized policy differs, so ownCS is preserved exactly at base. A burn of base-held ownCS is simply unbalanceable there.
- Seize branch (ownCS seized): ownCS may move but only between base outputs; the seized policy's signed mint map is folded into the balance and required in leftover progLogicCred outputs (checkBalanceInvariant counts only base outputs).
- Pre-escaped (non-base) ownCS inputs are skipped by seize / bounded by conservation under TransferAct; nonbase_out <= nonbase_in in every case.

Registration re-proof is genuinely redundant: ownCS supply can only exist if ownCS minted before, every positive-mint issuance arm (Local/Delegate) requires a registration/nodeRef proof, and the directory is insert-only, so the node persists and blocks any non-member exemption. This is exactly what makes the registered->non-member direction (the sole escape) unsatisfiable. The redesign's nullary mint-side MintProof.Member does not weaken this: it leaves the input-side transfer proof (the load-bearing one) authenticated, and a false Member tag is self-penalizing (adds the tagger's own delta to base containment)./Conway-ledger value-conservation + companion-validator coupling audit of the BurnOnly custody skip] The redesign's global change (section 11.3: mint-side MintProof.Member becomes a nullary un-authenticated tag) MUST keep the INPUT-side transfer proof's member-node authentication and the NonMember covering authentication; the escape-critical registered->non-member direction must stay unsatisfiable. If a future edit lets the mint-side nullary Member logic leak into input-side classification, the argument breaks.
- [The BurnOnly witness is sound; I could not construct any ledger-valid tx that escapes registered-policy supply while all validators accept. The argument's spine is airtight under per-asset value conservation:

Custody breach for ownCS = "quantity of ownCS outside progLogicCred increases." By ledger conservation, total_out(ownCS) = total_in(ownCS) + mint(ownCS). BurnOnly's only substantive check forces mint(ownCS) <= 0 (no positive entry), so total_out <= total_in. Therefore nonbase_out = total_out - base_out <= total_in - base_out; for nonbase_out > nonbase_in = total_in - base_in we need base_out < base_in, i.e. a base UTxO holding ownCS MUST be spent. "Base" is definitionally the progLogicCred payment credential, so any such spend invokes the base spending validator (mkProgrammableLogicBase), which requires the global OR seize withdrawal credential to be present (an actual script, so it runs and enforces over the whole tx).

- TransferAct branch: pvalueFromCred aggregates EVERY base input's non-Ada value with no silent omission (missing owner witness -> perror; pjustData on a stake-less base input -> phead-on-empty error), so ownCS's spent base value is in totalProgTokenValue. Its input-side transfer proof must be Member (registered ownCS can never satisfy the covering nodeKey<cs<nodeNext test on an insert-only sorted directory), so it is retained. Containment (poutputsContainExpectedValueAtCred, a >= lower bound) then forces base_out(ownCS) >= base_input(ownCS) + mint(ownCS), which combined with conservation pins nonbase_out <= nonbase_in. Burning only shrinks the required amount by exactly the burned quantity, which conservation destroys — no slack.
- Seize branch (ownCS not seized): the walk visits every input, pairs each base input with a same-address (=base) output, and pvalueEqualsDeltaCurrencySymbol perrors if any non-seized policy differs, so ownCS is preserved exactly at base. A burn of base-held ownCS is simply unbalanceable there.
- Seize branch (ownCS seized): ownCS may move but only between base outputs; the seized policy's signed mint map is folded into the balance and required in leftover progLogicCred outputs (checkBalanceInvariant counts only base outputs).
- Pre-escaped (non-base) ownCS inputs are skipped by seize / bounded by conservation under TransferAct; nonbase_out <= nonbase_in in every case.

Registration re-proof is genuinely redundant: ownCS supply can only exist if ownCS minted before, every positive-mint issuance arm (Local/Delegate) requires a registration/nodeRef proof, and the directory is insert-only, so the node persists and blocks any non-member exemption. This is exactly what makes the registered->non-member direction (the sole escape) unsatisfiable. The redesign's nullary mint-side MintProof.Member does not weaken this: it leaves the input-side transfer proof (the load-bearing one) authenticated, and a false Member tag is self-penalizing (adds the tagger's own delta to base containment)./Conway-ledger value-conservation + companion-validator coupling audit of the BurnOnly custody skip] Ledger enforces per-asset value conservation and runs every mint policy with a nonzero entry and every withdrawal script (assumed per section 3.1; standard Conway semantics).
- [Local custody = one universal full-output scan: for every tx output, paymentCred == progLogicCred (from the NFT-authenticated params datum) OR the output holds zero tokens of ownCS. No input walk, no mint-delta read. Soundness: per-asset ledger conservation plus strictly-positive canonical output values means zero-at-non-base-outputs forces non-base quantity of every ownCS token name to be non-increasing, for every tx shape (positive mint, mixed mint/burn, register-and-mint, cross-policy seize, many base inputs). Interaction: in a seize-of-B tx minting A locally, seize per-UTxO preservation of A plus the A-scan cannot be gamed./Adversarial code-vs-spec verification: re-derive the conservation argument from first principles, then attack every silent assumption against the actual Plutarch sources (base spend gate, seize input walk and per-UTxO preservation, global no-omission mint walk, directory insert-only, params anchor authenticity).] Params-anchor hardening (spec §4.1) is NOT yet in code: current mkProtocolParametersMinting checks only one-shot + single fixed-name NFT — it does not force the NFT to an alwaysFailScript output, an exact min-ada+NFT value (which licenses the cheap phasCSH first-non-Ada lookup), a fully-decoding datum, or script-credential fields. Until Phase 1 lands, anchor immutability and progLogicCred being a script credential are genesis-builder trust; a spendable or malformed anchor voids the entire custody argument.
- [Local custody = one universal full-output scan: for every tx output, paymentCred == progLogicCred (from the NFT-authenticated params datum) OR the output holds zero tokens of ownCS. No input walk, no mint-delta read. Soundness: per-asset ledger conservation plus strictly-positive canonical output values means zero-at-non-base-outputs forces non-base quantity of every ownCS token name to be non-increasing, for every tx shape (positive mint, mixed mint/burn, register-and-mint, cross-policy seize, many base inputs). Interaction: in a seize-of-B tx minting A locally, seize per-UTxO preservation of A plus the A-scan cannot be gamed./Adversarial code-vs-spec verification: re-derive the conservation argument from first principles, then attack every silent assumption against the actual Plutarch sources (base spend gate, seize input walk and per-UTxO preservation, global no-omission mint walk, directory insert-only, params anchor authenticity).] Current on-chain params datum (Types/ProtocolParams.hs) has only 2 of the spec's 4 fields; the Local arm needs only field 1 (progLogicCred), which exists, but the datum/type migration must keep fields 0-1 byte-compatible as §4 promises.
- [Local custody = one universal full-output scan: for every tx output, paymentCred == progLogicCred (from the NFT-authenticated params datum) OR the output holds zero tokens of ownCS. No input walk, no mint-delta read. Soundness: per-asset ledger conservation plus strictly-positive canonical output values means zero-at-non-base-outputs forces non-base quantity of every ownCS token name to be non-increasing, for every tx shape (positive mint, mixed mint/burn, register-and-mint, cross-policy seize, many base inputs). Interaction: in a seize-of-B tx minting A locally, seize per-UTxO preservation of A plus the A-scan cannot be gamed./Adversarial code-vs-spec verification: re-derive the conservation argument from first principles, then attack every silent assumption against the actual Plutarch sources (base spend gate, seize input walk and per-UTxO preservation, global no-omission mint walk, directory insert-only, params anchor authenticity).] The implemented scan must traverse the COMPLETE txInfo.outputs list with per-output short-circuit only — no index-driven skipping, no early global success exit; and the credential compare must be whole-credential equalsData (constructor + hash), not hash-only.
- [Local custody = one universal full-output scan: for every tx output, paymentCred == progLogicCred (from the NFT-authenticated params datum) OR the output holds zero tokens of ownCS. No input walk, no mint-delta read. Soundness: per-asset ledger conservation plus strictly-positive canonical output values means zero-at-non-base-outputs forces non-base quantity of every ownCS token name to be non-increasing, for every tx shape (positive mint, mixed mint/burn, register-and-mint, cross-policy seize, many base inputs). Interaction: in a seize-of-B tx minting A locally, seize per-UTxO preservation of A plus the A-scan cannot be gamed./Adversarial code-vs-spec verification: re-derive the conservation argument from first principles, then attack every silent assumption against the actual Plutarch sources (base spend gate, seize input walk and per-UTxO preservation, global no-omission mint walk, directory insert-only, params anchor authenticity).] The scan's 'zero tokens' check may rely on sorted-map absence only because ledger output values are canonical, sorted, strictly positive; any use on non-output values (e.g. mint) would need the negative/zero-entry handling the outputs case gets for free.
- [Local custody = one universal full-output scan: for every tx output, paymentCred == progLogicCred (from the NFT-authenticated params datum) OR the output holds zero tokens of ownCS. No input walk, no mint-delta read. Soundness: per-asset ledger conservation plus strictly-positive canonical output values means zero-at-non-base-outputs forces non-base quantity of every ownCS token name to be non-increasing, for every tx shape (positive mint, mixed mint/burn, register-and-mint, cross-policy seize, many base inputs). Interaction: in a seize-of-B tx minting A locally, seize per-UTxO preservation of A plus the A-scan cannot be gamed./Adversarial code-vs-spec verification: re-derive the conservation argument from first principles, then attack every silent assumption against the actual Plutarch sources (base spend gate, seize input walk and per-UTxO preservation, global no-omission mint walk, directory insert-only, params anchor authenticity).] Deployment derivation coherence (§12): progLogicCred in the params datum must be the credential of mkProgrammableLogicBase compiled against the real global and seize credentials — otherwise 'at base' does not imply future spends are policed. The base validator's gate credentials are compile-time parameters, not read from the params datum.
- [Local custody = one universal full-output scan: for every tx output, paymentCred == progLogicCred (from the NFT-authenticated params datum) OR the output holds zero tokens of ownCS. No input walk, no mint-delta read. Soundness: per-asset ledger conservation plus strictly-positive canonical output values means zero-at-non-base-outputs forces non-base quantity of every ownCS token name to be non-increasing, for every tx shape (positive mint, mixed mint/burn, register-and-mint, cross-policy seize, many base inputs). Interaction: in a seize-of-B tx minting A locally, seize per-UTxO preservation of A plus the A-scan cannot be gamed./Adversarial code-vs-spec verification: re-derive the conservation argument from first principles, then attack every silent assumption against the actual Plutarch sources (base spend gate, seize input walk and per-UTxO preservation, global no-omission mint walk, directory insert-only, params anchor authenticity).] The custody invariant is quantity-outside-base only: minted tokens paid to base with a missing/pointer stake credential or unexpected datum are confined but may be unattributable (spec §2.2 owner-approved exclusion) — builders must enforce §10.6 off-chain.
- [Local custody = one universal full-output scan: for every tx output, paymentCred == progLogicCred (from the NFT-authenticated params datum) OR the output holds zero tokens of ownCS. No input walk, no mint-delta read. Soundness: per-asset ledger conservation plus strictly-positive canonical output values means zero-at-non-base-outputs forces non-base quantity of every ownCS token name to be non-increasing, for every tx shape (positive mint, mixed mint/burn, register-and-mint, cross-policy seize, many base inputs). Interaction: in a seize-of-B tx minting A locally, seize per-UTxO preservation of A plus the A-scan cannot be gamed./Adversarial code-vs-spec verification: re-derive the conservation argument from first principles, then attack every silent assumption against the actual Plutarch sources (base spend gate, seize input walk and per-UTxO preservation, global no-omission mint walk, directory insert-only, params anchor authenticity).] The global and seize rewarding credentials must be registered on-chain so their withdraw-zero withdrawals are admissible (operational precondition of the withdraw-zero pattern).
- [Local custody = one universal full-output scan: for every tx output, paymentCred == progLogicCred (from the NFT-authenticated params datum) OR the output holds zero tokens of ownCS. No input walk, no mint-delta read. Soundness: per-asset ledger conservation plus strictly-positive canonical output values means zero-at-non-base-outputs forces non-base quantity of every ownCS token name to be non-increasing, for every tx shape (positive mint, mixed mint/burn, register-and-mint, cross-policy seize, many base inputs). Interaction: in a seize-of-B tx minting A locally, seize per-UTxO preservation of A plus the A-scan cannot be gamed./Adversarial code-vs-spec verification: re-derive the conservation argument from first principles, then attack every silent assumption against the actual Plutarch sources (base spend gate, seize input walk and per-UTxO preservation, global no-omission mint walk, directory insert-only, params anchor authenticity).] Current Issuance.hs is still the POC (first-output rule, single token name); the verified claim describes the spec design to be built in Phases 2-3, not code that exists today — the verified couplings are the EXISTING companion validators the spec leans on, and any edit to them must re-run the §14.1 suite as §3.5 requires.
- [Local custody = one universal full-output scan: for every tx output, paymentCred == progLogicCred (from the NFT-authenticated params datum) OR the output holds zero tokens of ownCS. No input walk, no mint-delta read. Soundness: per-asset ledger conservation plus strictly-positive canonical output values means zero-at-non-base-outputs forces non-base quantity of every ownCS token name to be non-increasing, for every tx shape (positive mint, mixed mint/burn, register-and-mint, cross-policy seize, many base inputs). Interaction: in a seize-of-B tx minting A locally, seize per-UTxO preservation of A plus the A-scan cannot be gamed./Adversarial code-vs-spec verification: re-derive the conservation argument from first principles, then attack every silent assumption against the actual Plutarch sources (base spend gate, seize input walk and per-UTxO preservation, global no-omission mint walk, directory insert-only, params anchor authenticity).] Noted stricter-than-spec fact: ProgrammableLogicBase.hs:1744-1747 forces every base input in a seize tx to hold the seized policy, so the §3.3 masking shape additionally requires the victim/attacker input to carry B; this narrows shapes but changes no conclusion.
- [Spec §11.3: mint-walk proofs become MintProof = Member | NonMember nodeIdx. Member does no node reference, no datum decode, no node authentication, no transfer-logic withdrawal check — it conses the signed mint entry into the programmable mint value containment demands at base. Only NonMember keeps the authenticated covering-node check. Sound because Member is self-penalizing (only ADDS the claimed policy's per-name delta to base containment), and the escape-critical direction (registered policy obtaining NonMember) remains blocked by covering-node authentication./Adversarial classification-soundness: for each (registration-status × delta-sign × proof-tag) cell, construct the tx that profits, then trace it through the actual union/filter/containment algebra and directory authentication code.] Phase-3 issuance policy (not yet implemented; Issuance.hs is still the old policy) must carry the authorization the deleted mint-side transfer-logic check provided: Local enforces its own no-escape custody scan; DelegateTransfer requires the GLOBAL credential's withdrawal specifically (not seize, not another script) plus a nodeRefIdx proof of the exact ownCS-named directory NFT among reference inputs (making same-tx-registration delegation unrepresentable, spec 14.1.4); BurnOnly rejects any positive entry.
- [Spec §11.3: mint-walk proofs become MintProof = Member | NonMember nodeIdx. Member does no node reference, no datum decode, no node authentication, no transfer-logic withdrawal check — it conses the signed mint entry into the programmable mint value containment demands at base. Only NonMember keeps the authenticated covering-node check. Sound because Member is self-penalizing (only ADDS the claimed policy's per-name delta to base containment), and the escape-critical direction (registered policy obtaining NonMember) remains blocked by covering-node authentication./Adversarial classification-soundness: for each (registration-status × delta-sign × proof-tag) cell, construct the tx that profits, then trace it through the actual union/filter/containment algebra and directory authentication code.] The redesigned mint walk must preserve: one proof per mint-map entry in ascending-CS lockstep with missing/extra-proof errors; Member conses the ledger's own mint entry (never redeemer-supplied data); and the descending accumulator is reversed (preverseCurrencyPairs) before pcurrencyPairsUnionFast — the union and containment subtract-walk silently require canonical ascending sorted input.
- [Spec §11.3: mint-walk proofs become MintProof = Member | NonMember nodeIdx. Member does no node reference, no datum decode, no node authentication, no transfer-logic withdrawal check — it conses the signed mint entry into the programmable mint value containment demands at base. Only NonMember keeps the authenticated covering-node check. Sound because Member is self-penalizing (only ADDS the claimed policy's per-name delta to base containment), and the escape-critical direction (registered policy obtaining NonMember) remains blocked by covering-node authentication./Adversarial classification-soundness: for each (registration-status × delta-sign × proof-tag) cell, construct the tx that profits, then trace it through the actual union/filter/containment algebra and directory authentication code.] The directory must remain insert-only in perpetuity: no remove/burn redeemer added to MintDirectory (the blacklist's pRemove machinery in BlacklistCommon.hs must never be wired to the directory CS), the deployed node-spend validator keeps forcing a directory mint, and Insert keeps single-node-input + pinned-output invariants.
- [Spec §11.3: mint-walk proofs become MintProof = Member | NonMember nodeIdx. Member does no node reference, no datum decode, no node authentication, no transfer-logic withdrawal check — it conses the signed mint entry into the programmable mint value containment demands at base. Only NonMember keeps the authenticated covering-node check. Sound because Member is self-penalizing (only ADDS the claimed policy's per-name delta to base containment), and the escape-critical direction (registered policy obtaining NonMember) remains blocked by covering-node authentication./Adversarial classification-soundness: for each (registration-status × delta-sign × proof-tag) cell, construct the tx that profits, then trace it through the actual union/filter/containment algebra and directory authentication code.] Input-side transfer proofs stay fully authenticated in both directions and NonMember input entries stay DROPPED from the aggregated value — the harmlessness of a false negative Member rests on unregistered CS never appearing positively in totalProgTokenValue_.
- [Spec §11.3: mint-walk proofs become MintProof = Member | NonMember nodeIdx. Member does no node reference, no datum decode, no node authentication, no transfer-logic withdrawal check — it conses the signed mint entry into the programmable mint value containment demands at base. Only NonMember keeps the authenticated covering-node check. Sound because Member is self-penalizing (only ADDS the claimed policy's per-name delta to base containment), and the escape-critical direction (registered policy obtaining NonMember) remains blocked by covering-node authentication./Adversarial classification-soundness: for each (registration-status × delta-sign × proof-tag) cell, construct the tx that profits, then trace it through the actual union/filter/containment algebra and directory authentication code.] Ledger assumptions: Plutus V3 mint value is sorted, CS-deduplicated, nonzero, with no Ada pseudo-entry (union correctness), and Conway's inputs/reference-inputs disjointness closes the pre-insert covering-node reference window for same-tx registration (belt-and-braces with condition 1).
- [Spec §11.3: mint-walk proofs become MintProof = Member | NonMember nodeIdx. Member does no node reference, no datum decode, no node authentication, no transfer-logic withdrawal check — it conses the signed mint entry into the programmable mint value containment demands at base. Only NonMember keeps the authenticated covering-node check. Sound because Member is self-penalizing (only ADDS the claimed policy's per-name delta to base containment), and the escape-critical direction (registered policy obtaining NonMember) remains blocked by covering-node authentication./Adversarial classification-soundness: for each (registration-status × delta-sign × proof-tag) cell, construct the tx that profits, then trace it through the actual union/filter/containment algebra and directory authentication code.] Every path that feeds poutputsContainExpectedValueAtCred keeps the strictly-positive precondition via pfilterPositiveCurrencyPairs — a refactor that skips the filter on any mint-merged path silently breaks the subtract-walk semantics.
- [BurnOnly delegates all custody to base->global/seize, and those companions are comprehensive, so no composition I could build lets pre-existing ownCS supply increase outside the base.

WHY IT HOLDS. BurnOnly runs only because ownCS is in the mint field (ledger requires the CS present for the minting script to execute), and it requires every ownCS mint entry <= 0. So the tx creates no new ownCS. The only harm BurnOnly could do is let PRE-EXISTING ownCS move base->non-base. Two cases, both blocked by validators that are already comprehensive:

(1) ownCS at a BASE input. Spending it runs mkProgrammableLogicBase (ProgrammableLogicBase.hs:714-727), which forces globalCred or seizeCred in the withdrawals. If global TransferAct runs, pvalueFromCred (386-433) aggregates EVERY progLogicCred input's ownCS and poutputsContainExpectedValueAtCred forces >= (base_in - burn) back to base outputs; value conservation then bounds non-base ownCS by nonbase_in. If seize runs, processThirdPartyTransfer (1409-1462) walks EVERY input and pairs each base input to a continuing output under full-address equality, with pvalueEqualsDeltaCurrencySymbol permitting only the seized CS to change; non-seized ownCS is copied to a base output, seized ownCS is contained in leftover base outputs. Neither lets base ownCS reach a non-base output.

(2) ownCS at a NON-base input (already escaped). No base spend, so no forced validator, but by ledger value conservation total ownCS out = nonbase_in - burn <= nonbase_in, so non-base supply cannot increase. Relocating outside->outside is not a custody violation (§3.2 only forbids an increase outside base).

CROSS-VALIDATOR PROBES that failed to break it: (a) burn ownCS while seizing a DIFFERENT policy B - seize preserves non-seized ownCS exactly in the paired base output, burn is charged to nonbase_in; (b) global + many BurnOnly policies at once - global's full mint walk demands one authentic directory proof per policy, no omission; (c) covering-node lie to exclude ownCS from global containment - impossible for a registered key because the sorted insert-only directory has a node AT ownCS, so no covering node satisfies key < ownCS < next; (d) params spoofing to feed global/seize a fake progLogicCred - blocked because mkProtocolParametersMinting is one-shot (phasUTxO # oref, ProtocolParams.hs:40), so only one protocolParamsCS UTxO can ever exist; (e) both global and seize withdrawals in one tx - both run, both enforce containment, strictly stricter. Registration-skip is sound: any positive ownCS supply required a prior registered mint and the directory is insert-only, so the node persists; and the global's mint walk re-demands ownCS's directory proof + transfer script on the burn anyway.

The argument is airtight GIVEN the companions keep their §3.5 invariants (they do in current code) AND given a faithful BurnOnly. The one place the redesign could self-inflict a hole is the positivity check itself: the redesigned policy is NOT yet implemented (current Issuance.hs is the old single-token POC using pheadSingleton). BurnOnly MUST scan the WHOLE ownCS token map for any positive entry. If it were implemented head-only / single-entry like the current code, a mixed map {A:+100, B:-1} would smuggle a real positive mint of A under BurnOnly with NO registration, NO containment, NO params - a direct unbacked-mint escape. That is a condition on the implementation, not a break of the design as specified./cross-validator composition (base + global + seize + one-shot params co-occurring with BurnOnly)] The redesigned BurnOnly's 'no positive entry' check MUST scan the entire ownCS token-name map, not a head/single-entry (pheadSingleton) form; otherwise a mixed {+,-} multi-name map smuggles an unbacked positive mint with no registration/custody/params. The redesigned policy is not yet implemented (current Issuance.hs is the old single-token POC).
- [BurnOnly delegates all custody to base->global/seize, and those companions are comprehensive, so no composition I could build lets pre-existing ownCS supply increase outside the base.

WHY IT HOLDS. BurnOnly runs only because ownCS is in the mint field (ledger requires the CS present for the minting script to execute), and it requires every ownCS mint entry <= 0. So the tx creates no new ownCS. The only harm BurnOnly could do is let PRE-EXISTING ownCS move base->non-base. Two cases, both blocked by validators that are already comprehensive:

(1) ownCS at a BASE input. Spending it runs mkProgrammableLogicBase (ProgrammableLogicBase.hs:714-727), which forces globalCred or seizeCred in the withdrawals. If global TransferAct runs, pvalueFromCred (386-433) aggregates EVERY progLogicCred input's ownCS and poutputsContainExpectedValueAtCred forces >= (base_in - burn) back to base outputs; value conservation then bounds non-base ownCS by nonbase_in. If seize runs, processThirdPartyTransfer (1409-1462) walks EVERY input and pairs each base input to a continuing output under full-address equality, with pvalueEqualsDeltaCurrencySymbol permitting only the seized CS to change; non-seized ownCS is copied to a base output, seized ownCS is contained in leftover base outputs. Neither lets base ownCS reach a non-base output.

(2) ownCS at a NON-base input (already escaped). No base spend, so no forced validator, but by ledger value conservation total ownCS out = nonbase_in - burn <= nonbase_in, so non-base supply cannot increase. Relocating outside->outside is not a custody violation (§3.2 only forbids an increase outside base).

CROSS-VALIDATOR PROBES that failed to break it: (a) burn ownCS while seizing a DIFFERENT policy B - seize preserves non-seized ownCS exactly in the paired base output, burn is charged to nonbase_in; (b) global + many BurnOnly policies at once - global's full mint walk demands one authentic directory proof per policy, no omission; (c) covering-node lie to exclude ownCS from global containment - impossible for a registered key because the sorted insert-only directory has a node AT ownCS, so no covering node satisfies key < ownCS < next; (d) params spoofing to feed global/seize a fake progLogicCred - blocked because mkProtocolParametersMinting is one-shot (phasUTxO # oref, ProtocolParams.hs:40), so only one protocolParamsCS UTxO can ever exist; (e) both global and seize withdrawals in one tx - both run, both enforce containment, strictly stricter. Registration-skip is sound: any positive ownCS supply required a prior registered mint and the directory is insert-only, so the node persists; and the global's mint walk re-demands ownCS's directory proof + transfer script on the burn anyway.

The argument is airtight GIVEN the companions keep their §3.5 invariants (they do in current code) AND given a faithful BurnOnly. The one place the redesign could self-inflict a hole is the positivity check itself: the redesigned policy is NOT yet implemented (current Issuance.hs is the old single-token POC using pheadSingleton). BurnOnly MUST scan the WHOLE ownCS token map for any positive entry. If it were implemented head-only / single-entry like the current code, a mixed map {A:+100, B:-1} would smuggle a real positive mint of A under BurnOnly with NO registration, NO containment, NO params - a direct unbacked-mint escape. That is a condition on the implementation, not a break of the design as specified./cross-validator composition (base + global + seize + one-shot params co-occurring with BurnOnly)] Companion validators MUST retain their §3.5 invariants: base forces global||seize; global walks all base inputs and the full mint field with per-policy proofs and base containment; seize walks every input, preserves non-seized tokens per corresponding output, and contains the seized delta+mint at base. All hold in current code.
- [BurnOnly delegates all custody to base->global/seize, and those companions are comprehensive, so no composition I could build lets pre-existing ownCS supply increase outside the base.

WHY IT HOLDS. BurnOnly runs only because ownCS is in the mint field (ledger requires the CS present for the minting script to execute), and it requires every ownCS mint entry <= 0. So the tx creates no new ownCS. The only harm BurnOnly could do is let PRE-EXISTING ownCS move base->non-base. Two cases, both blocked by validators that are already comprehensive:

(1) ownCS at a BASE input. Spending it runs mkProgrammableLogicBase (ProgrammableLogicBase.hs:714-727), which forces globalCred or seizeCred in the withdrawals. If global TransferAct runs, pvalueFromCred (386-433) aggregates EVERY progLogicCred input's ownCS and poutputsContainExpectedValueAtCred forces >= (base_in - burn) back to base outputs; value conservation then bounds non-base ownCS by nonbase_in. If seize runs, processThirdPartyTransfer (1409-1462) walks EVERY input and pairs each base input to a continuing output under full-address equality, with pvalueEqualsDeltaCurrencySymbol permitting only the seized CS to change; non-seized ownCS is copied to a base output, seized ownCS is contained in leftover base outputs. Neither lets base ownCS reach a non-base output.

(2) ownCS at a NON-base input (already escaped). No base spend, so no forced validator, but by ledger value conservation total ownCS out = nonbase_in - burn <= nonbase_in, so non-base supply cannot increase. Relocating outside->outside is not a custody violation (§3.2 only forbids an increase outside base).

CROSS-VALIDATOR PROBES that failed to break it: (a) burn ownCS while seizing a DIFFERENT policy B - seize preserves non-seized ownCS exactly in the paired base output, burn is charged to nonbase_in; (b) global + many BurnOnly policies at once - global's full mint walk demands one authentic directory proof per policy, no omission; (c) covering-node lie to exclude ownCS from global containment - impossible for a registered key because the sorted insert-only directory has a node AT ownCS, so no covering node satisfies key < ownCS < next; (d) params spoofing to feed global/seize a fake progLogicCred - blocked because mkProtocolParametersMinting is one-shot (phasUTxO # oref, ProtocolParams.hs:40), so only one protocolParamsCS UTxO can ever exist; (e) both global and seize withdrawals in one tx - both run, both enforce containment, strictly stricter. Registration-skip is sound: any positive ownCS supply required a prior registered mint and the directory is insert-only, so the node persists; and the global's mint walk re-demands ownCS's directory proof + transfer script on the burn anyway.

The argument is airtight GIVEN the companions keep their §3.5 invariants (they do in current code) AND given a faithful BurnOnly. The one place the redesign could self-inflict a hole is the positivity check itself: the redesigned policy is NOT yet implemented (current Issuance.hs is the old single-token POC using pheadSingleton). BurnOnly MUST scan the WHOLE ownCS token map for any positive entry. If it were implemented head-only / single-entry like the current code, a mixed map {A:+100, B:-1} would smuggle a real positive mint of A under BurnOnly with NO registration, NO containment, NO params - a direct unbacked-mint escape. That is a condition on the implementation, not a break of the design as specified./cross-validator composition (base + global + seize + one-shot params co-occurring with BurnOnly)] The single protocolParamsCS UTxO's datum (progLogicCred/directoryNodeCS) must be genesis-honest; current mkProtocolParametersMinting guarantees one-shot uniqueness but does NOT yet enforce the §4.1 address/datum-shape hardening (Phase 1 pending). BurnOnly does not read params directly, but global/seize custody depends on this value.
- [BurnOnly delegates all custody to base->global/seize, and those companions are comprehensive, so no composition I could build lets pre-existing ownCS supply increase outside the base.

WHY IT HOLDS. BurnOnly runs only because ownCS is in the mint field (ledger requires the CS present for the minting script to execute), and it requires every ownCS mint entry <= 0. So the tx creates no new ownCS. The only harm BurnOnly could do is let PRE-EXISTING ownCS move base->non-base. Two cases, both blocked by validators that are already comprehensive:

(1) ownCS at a BASE input. Spending it runs mkProgrammableLogicBase (ProgrammableLogicBase.hs:714-727), which forces globalCred or seizeCred in the withdrawals. If global TransferAct runs, pvalueFromCred (386-433) aggregates EVERY progLogicCred input's ownCS and poutputsContainExpectedValueAtCred forces >= (base_in - burn) back to base outputs; value conservation then bounds non-base ownCS by nonbase_in. If seize runs, processThirdPartyTransfer (1409-1462) walks EVERY input and pairs each base input to a continuing output under full-address equality, with pvalueEqualsDeltaCurrencySymbol permitting only the seized CS to change; non-seized ownCS is copied to a base output, seized ownCS is contained in leftover base outputs. Neither lets base ownCS reach a non-base output.

(2) ownCS at a NON-base input (already escaped). No base spend, so no forced validator, but by ledger value conservation total ownCS out = nonbase_in - burn <= nonbase_in, so non-base supply cannot increase. Relocating outside->outside is not a custody violation (§3.2 only forbids an increase outside base).

CROSS-VALIDATOR PROBES that failed to break it: (a) burn ownCS while seizing a DIFFERENT policy B - seize preserves non-seized ownCS exactly in the paired base output, burn is charged to nonbase_in; (b) global + many BurnOnly policies at once - global's full mint walk demands one authentic directory proof per policy, no omission; (c) covering-node lie to exclude ownCS from global containment - impossible for a registered key because the sorted insert-only directory has a node AT ownCS, so no covering node satisfies key < ownCS < next; (d) params spoofing to feed global/seize a fake progLogicCred - blocked because mkProtocolParametersMinting is one-shot (phasUTxO # oref, ProtocolParams.hs:40), so only one protocolParamsCS UTxO can ever exist; (e) both global and seize withdrawals in one tx - both run, both enforce containment, strictly stricter. Registration-skip is sound: any positive ownCS supply required a prior registered mint and the directory is insert-only, so the node persists; and the global's mint walk re-demands ownCS's directory proof + transfer script on the burn anyway.

The argument is airtight GIVEN the companions keep their §3.5 invariants (they do in current code) AND given a faithful BurnOnly. The one place the redesign could self-inflict a hole is the positivity check itself: the redesigned policy is NOT yet implemented (current Issuance.hs is the old single-token POC using pheadSingleton). BurnOnly MUST scan the WHOLE ownCS token map for any positive entry. If it were implemented head-only / single-entry like the current code, a mixed map {A:+100, B:-1} would smuggle a real positive mint of A under BurnOnly with NO registration, NO containment, NO params - a direct unbacked-mint escape. That is a condition on the implementation, not a break of the design as specified./cross-validator composition (base + global + seize + one-shot params co-occurring with BurnOnly)] Directory well-formedness (sorted, insert-only, unique keys, authentic node NFTs) must hold for the covering-node exclusion to be unforgeable; this is the standard directory assumption BurnOnly's registration-skip inherits.
- [The §3.4 soundness argument for BurnOnly (checking only: no positive ownCS mint + minting-logic withdrawal present, skipping custody/registration/params) HOLDS against the actual companion code, but only because of several silent preconditions the claim leans on without stating. I traced every escape vector for "pre-existing ownCS moves base->non-base while no positive ownCS is minted" and each dies on a companion invariant that IS present in the cited code.

WHY IT HOLDS (verified per-path):

1. Base spend forces a container. mkProgrammableLogicBase (ProgrammableLogicBase.hs:714-727) is the spending validator at progLogicCred; it walks ALL withdrawals and passes iff globalCred OR seizeCred is present (line 723). So any spend of a base ownCS UTxO forces either the global TransferAct or the seize validator to run over the whole tx. No third route.

2. Global TransferAct contains ownCS with no omission. pvalueFromCred (386-433) visits EVERY input, and for each base-cred input it forces an owner witness — pubkey signer (415-418) or script withdrawal (421-424) — erroring otherwise; a pointer/absent stake errors via pjustData/`_ -> perror` (425). So no base ownCS input can be silently dropped from the aggregate. pcheckTransferLogicAndGetProgrammableValue (837-901) consumes exactly one directory proof per input currency-pair (no omission: phead#proofs on the input walk). The burned ownCS enters the mint field; pcheckMintLogicAndGetProgrammableValue (927-981) requires exactly one proof per mint entry ("mint proof missing" 973 / "extra mint proof" 979). The union + pfilterPositiveCurrencyPairs (1157-1174) then feeds poutputsContainExpectedValueAtCred (512-697), which only credits outputs at progLogicCred. Net: spent ownCS minus burn must remain at base; non-base cannot gain.

3. The covering-proof forgery that WOULD break (2) is impossible. To exclude ownCS from containment the attacker needs a NEGATIVE/covering proof (nodeKey < ownCS < nodeNext, lines 855-861). ownCS is registered (see condition C2), so a well-formed node with key==ownCS exists and its predecessor's next==ownCS; the sorted, insert-only, one-shot-rooted directory has no overlapping interval, so no valid node covers a registered key. A forged reference UTxO can't substitute because directory node tokens can never leave the directory address with an attacker-chosen datum: makeCommon.atNodeValidator forces node outputs to the node-input address (Common.hs:145-154), pInsert takes exactly one node input via pheadSingleton (250) and re-locks it with structurally-enforced datums (pisInsertedOnNode/pisInsertedNode, 263-272), and DirectoryNodeAction has only Init/Insert — both mint exactly +1 (correctNodeTokenMinted, Common.hs:116,276), so there is no delete/burn and no node-mobility path.

4. Seize path preserves non-seized ownCS exactly. processThirdPartyTransfer.go (1445-1462) walks EVERY input; pcheckCorrespondingThirdPartyTransferInputsAndOutputs (1355-1392) pairs each base-cred input with a base output enforcing pvalueEqualsDeltaCurrencySymbol (1383) — equal everywhere except the seized CS — so burning/moving a NON-seized ownCS out of base can't validate. When ownCS IS the seized policy, checkBalanceInvariant/go2 (1419-1438) only sums progLogicCred outputs, so the seized delta+mint must reappear at base; outputs before poutputsStartIdx are simply unexamined but ledger value-conservation makes any ownCS there sourced from already-escaped non-base inputs, never a new escape.

5. Non-base ownCS inputs are already-escaped supply; burning them (negative delta) only shrinks escaped supply.

So with (b) "no positive ownCS entry" guaranteeing zero new tokens, every remaining ownCS in any output is pre-existing and every base->non-base move is caught. The argument is correct — but it is NOT self-contained; it silently requires the conditions listed. Verdict HOLDS_WITH_CONDITIONS rather than an unqualified HOLDS because those couplings live in OTHER validators and in the redesigned Mint path, none of which BurnOnly checks; if any regresses, BurnOnly becomes an exploitable escape./adversarial escape/custody analysis of the redesigned BurnOnly issuance witness against the actual base/global/seize/directory validators] C1 (BurnOnly own check, in the redesigned issuance itself): predicate (b) must reject ANY positive-quantity entry across EVERY token name in ownCS's mint map, not just a head/single entry — the current file uses pheadSingleton (Issuance.hs:141) which would misbehave on multi-token maps; the redesign must scan all names.
- [The §3.4 soundness argument for BurnOnly (checking only: no positive ownCS mint + minting-logic withdrawal present, skipping custody/registration/params) HOLDS against the actual companion code, but only because of several silent preconditions the claim leans on without stating. I traced every escape vector for "pre-existing ownCS moves base->non-base while no positive ownCS is minted" and each dies on a companion invariant that IS present in the cited code.

WHY IT HOLDS (verified per-path):

1. Base spend forces a container. mkProgrammableLogicBase (ProgrammableLogicBase.hs:714-727) is the spending validator at progLogicCred; it walks ALL withdrawals and passes iff globalCred OR seizeCred is present (line 723). So any spend of a base ownCS UTxO forces either the global TransferAct or the seize validator to run over the whole tx. No third route.

2. Global TransferAct contains ownCS with no omission. pvalueFromCred (386-433) visits EVERY input, and for each base-cred input it forces an owner witness — pubkey signer (415-418) or script withdrawal (421-424) — erroring otherwise; a pointer/absent stake errors via pjustData/`_ -> perror` (425). So no base ownCS input can be silently dropped from the aggregate. pcheckTransferLogicAndGetProgrammableValue (837-901) consumes exactly one directory proof per input currency-pair (no omission: phead#proofs on the input walk). The burned ownCS enters the mint field; pcheckMintLogicAndGetProgrammableValue (927-981) requires exactly one proof per mint entry ("mint proof missing" 973 / "extra mint proof" 979). The union + pfilterPositiveCurrencyPairs (1157-1174) then feeds poutputsContainExpectedValueAtCred (512-697), which only credits outputs at progLogicCred. Net: spent ownCS minus burn must remain at base; non-base cannot gain.

3. The covering-proof forgery that WOULD break (2) is impossible. To exclude ownCS from containment the attacker needs a NEGATIVE/covering proof (nodeKey < ownCS < nodeNext, lines 855-861). ownCS is registered (see condition C2), so a well-formed node with key==ownCS exists and its predecessor's next==ownCS; the sorted, insert-only, one-shot-rooted directory has no overlapping interval, so no valid node covers a registered key. A forged reference UTxO can't substitute because directory node tokens can never leave the directory address with an attacker-chosen datum: makeCommon.atNodeValidator forces node outputs to the node-input address (Common.hs:145-154), pInsert takes exactly one node input via pheadSingleton (250) and re-locks it with structurally-enforced datums (pisInsertedOnNode/pisInsertedNode, 263-272), and DirectoryNodeAction has only Init/Insert — both mint exactly +1 (correctNodeTokenMinted, Common.hs:116,276), so there is no delete/burn and no node-mobility path.

4. Seize path preserves non-seized ownCS exactly. processThirdPartyTransfer.go (1445-1462) walks EVERY input; pcheckCorrespondingThirdPartyTransferInputsAndOutputs (1355-1392) pairs each base-cred input with a base output enforcing pvalueEqualsDeltaCurrencySymbol (1383) — equal everywhere except the seized CS — so burning/moving a NON-seized ownCS out of base can't validate. When ownCS IS the seized policy, checkBalanceInvariant/go2 (1419-1438) only sums progLogicCred outputs, so the seized delta+mint must reappear at base; outputs before poutputsStartIdx are simply unexamined but ledger value-conservation makes any ownCS there sourced from already-escaped non-base inputs, never a new escape.

5. Non-base ownCS inputs are already-escaped supply; burning them (negative delta) only shrinks escaped supply.

So with (b) "no positive ownCS entry" guaranteeing zero new tokens, every remaining ownCS in any output is pre-existing and every base->non-base move is caught. The argument is correct — but it is NOT self-contained; it silently requires the conditions listed. Verdict HOLDS_WITH_CONDITIONS rather than an unqualified HOLDS because those couplings live in OTHER validators and in the redesigned Mint path, none of which BurnOnly checks; if any regresses, BurnOnly becomes an exploitable escape./adversarial escape/custody analysis of the redesigned BurnOnly issuance witness against the actual base/global/seize/directory validators] C2 (Mint/Issue path must retain registration binding): every positive-mint path of the redesigned policy MUST still prove ownCS is registered under directoryNodeCS with node-key==ownCS (as Issuance.hs:127-136 does today). If ownCS supply could exist unregistered, a covering proof WOULD be constructible and the transfer-path containment (attack 1) would let it escape. BurnOnly does not re-check this and inherits it.
- [The §3.4 soundness argument for BurnOnly (checking only: no positive ownCS mint + minting-logic withdrawal present, skipping custody/registration/params) HOLDS against the actual companion code, but only because of several silent preconditions the claim leans on without stating. I traced every escape vector for "pre-existing ownCS moves base->non-base while no positive ownCS is minted" and each dies on a companion invariant that IS present in the cited code.

WHY IT HOLDS (verified per-path):

1. Base spend forces a container. mkProgrammableLogicBase (ProgrammableLogicBase.hs:714-727) is the spending validator at progLogicCred; it walks ALL withdrawals and passes iff globalCred OR seizeCred is present (line 723). So any spend of a base ownCS UTxO forces either the global TransferAct or the seize validator to run over the whole tx. No third route.

2. Global TransferAct contains ownCS with no omission. pvalueFromCred (386-433) visits EVERY input, and for each base-cred input it forces an owner witness — pubkey signer (415-418) or script withdrawal (421-424) — erroring otherwise; a pointer/absent stake errors via pjustData/`_ -> perror` (425). So no base ownCS input can be silently dropped from the aggregate. pcheckTransferLogicAndGetProgrammableValue (837-901) consumes exactly one directory proof per input currency-pair (no omission: phead#proofs on the input walk). The burned ownCS enters the mint field; pcheckMintLogicAndGetProgrammableValue (927-981) requires exactly one proof per mint entry ("mint proof missing" 973 / "extra mint proof" 979). The union + pfilterPositiveCurrencyPairs (1157-1174) then feeds poutputsContainExpectedValueAtCred (512-697), which only credits outputs at progLogicCred. Net: spent ownCS minus burn must remain at base; non-base cannot gain.

3. The covering-proof forgery that WOULD break (2) is impossible. To exclude ownCS from containment the attacker needs a NEGATIVE/covering proof (nodeKey < ownCS < nodeNext, lines 855-861). ownCS is registered (see condition C2), so a well-formed node with key==ownCS exists and its predecessor's next==ownCS; the sorted, insert-only, one-shot-rooted directory has no overlapping interval, so no valid node covers a registered key. A forged reference UTxO can't substitute because directory node tokens can never leave the directory address with an attacker-chosen datum: makeCommon.atNodeValidator forces node outputs to the node-input address (Common.hs:145-154), pInsert takes exactly one node input via pheadSingleton (250) and re-locks it with structurally-enforced datums (pisInsertedOnNode/pisInsertedNode, 263-272), and DirectoryNodeAction has only Init/Insert — both mint exactly +1 (correctNodeTokenMinted, Common.hs:116,276), so there is no delete/burn and no node-mobility path.

4. Seize path preserves non-seized ownCS exactly. processThirdPartyTransfer.go (1445-1462) walks EVERY input; pcheckCorrespondingThirdPartyTransferInputsAndOutputs (1355-1392) pairs each base-cred input with a base output enforcing pvalueEqualsDeltaCurrencySymbol (1383) — equal everywhere except the seized CS — so burning/moving a NON-seized ownCS out of base can't validate. When ownCS IS the seized policy, checkBalanceInvariant/go2 (1419-1438) only sums progLogicCred outputs, so the seized delta+mint must reappear at base; outputs before poutputsStartIdx are simply unexamined but ledger value-conservation makes any ownCS there sourced from already-escaped non-base inputs, never a new escape.

5. Non-base ownCS inputs are already-escaped supply; burning them (negative delta) only shrinks escaped supply.

So with (b) "no positive ownCS entry" guaranteeing zero new tokens, every remaining ownCS in any output is pre-existing and every base->non-base move is caught. The argument is correct — but it is NOT self-contained; it silently requires the conditions listed. Verdict HOLDS_WITH_CONDITIONS rather than an unqualified HOLDS because those couplings live in OTHER validators and in the redesigned Mint path, none of which BurnOnly checks; if any regresses, BurnOnly becomes an exploitable escape./adversarial escape/custody analysis of the redesigned BurnOnly issuance witness against the actual base/global/seize/directory validators] C3 (Mint path must keep first-output-at-base + inline stake): minted ownCS must land at progLogicCred with an inline stake credential (Issuance.hs:186,173-179) so that (a) all live ownCS is at the base spending script (so spends are gated by mkProgrammableLogicBase) and (b) pvalueFromCred can attribute it without erroring.
- [The §3.4 soundness argument for BurnOnly (checking only: no positive ownCS mint + minting-logic withdrawal present, skipping custody/registration/params) HOLDS against the actual companion code, but only because of several silent preconditions the claim leans on without stating. I traced every escape vector for "pre-existing ownCS moves base->non-base while no positive ownCS is minted" and each dies on a companion invariant that IS present in the cited code.

WHY IT HOLDS (verified per-path):

1. Base spend forces a container. mkProgrammableLogicBase (ProgrammableLogicBase.hs:714-727) is the spending validator at progLogicCred; it walks ALL withdrawals and passes iff globalCred OR seizeCred is present (line 723). So any spend of a base ownCS UTxO forces either the global TransferAct or the seize validator to run over the whole tx. No third route.

2. Global TransferAct contains ownCS with no omission. pvalueFromCred (386-433) visits EVERY input, and for each base-cred input it forces an owner witness — pubkey signer (415-418) or script withdrawal (421-424) — erroring otherwise; a pointer/absent stake errors via pjustData/`_ -> perror` (425). So no base ownCS input can be silently dropped from the aggregate. pcheckTransferLogicAndGetProgrammableValue (837-901) consumes exactly one directory proof per input currency-pair (no omission: phead#proofs on the input walk). The burned ownCS enters the mint field; pcheckMintLogicAndGetProgrammableValue (927-981) requires exactly one proof per mint entry ("mint proof missing" 973 / "extra mint proof" 979). The union + pfilterPositiveCurrencyPairs (1157-1174) then feeds poutputsContainExpectedValueAtCred (512-697), which only credits outputs at progLogicCred. Net: spent ownCS minus burn must remain at base; non-base cannot gain.

3. The covering-proof forgery that WOULD break (2) is impossible. To exclude ownCS from containment the attacker needs a NEGATIVE/covering proof (nodeKey < ownCS < nodeNext, lines 855-861). ownCS is registered (see condition C2), so a well-formed node with key==ownCS exists and its predecessor's next==ownCS; the sorted, insert-only, one-shot-rooted directory has no overlapping interval, so no valid node covers a registered key. A forged reference UTxO can't substitute because directory node tokens can never leave the directory address with an attacker-chosen datum: makeCommon.atNodeValidator forces node outputs to the node-input address (Common.hs:145-154), pInsert takes exactly one node input via pheadSingleton (250) and re-locks it with structurally-enforced datums (pisInsertedOnNode/pisInsertedNode, 263-272), and DirectoryNodeAction has only Init/Insert — both mint exactly +1 (correctNodeTokenMinted, Common.hs:116,276), so there is no delete/burn and no node-mobility path.

4. Seize path preserves non-seized ownCS exactly. processThirdPartyTransfer.go (1445-1462) walks EVERY input; pcheckCorrespondingThirdPartyTransferInputsAndOutputs (1355-1392) pairs each base-cred input with a base output enforcing pvalueEqualsDeltaCurrencySymbol (1383) — equal everywhere except the seized CS — so burning/moving a NON-seized ownCS out of base can't validate. When ownCS IS the seized policy, checkBalanceInvariant/go2 (1419-1438) only sums progLogicCred outputs, so the seized delta+mint must reappear at base; outputs before poutputsStartIdx are simply unexamined but ledger value-conservation makes any ownCS there sourced from already-escaped non-base inputs, never a new escape.

5. Non-base ownCS inputs are already-escaped supply; burning them (negative delta) only shrinks escaped supply.

So with (b) "no positive ownCS entry" guaranteeing zero new tokens, every remaining ownCS in any output is pre-existing and every base->non-base move is caught. The argument is correct — but it is NOT self-contained; it silently requires the conditions listed. Verdict HOLDS_WITH_CONDITIONS rather than an unqualified HOLDS because those couplings live in OTHER validators and in the redesigned Mint path, none of which BurnOnly checks; if any regresses, BurnOnly becomes an exploitable escape./adversarial escape/custody analysis of the redesigned BurnOnly issuance witness against the actual base/global/seize/directory validators] C4 (Config coupling): the progLogicCred stored in ProgrammableLogicGlobalParams and the globalCred/seizeCred compiled into mkProgrammableLogicBase must actually equal the hashes of the deployed base/global/seize validators; a mismatch (or a base script not equal to progLogicCred) voids the 'base spend forces a container' premise.
- [The §3.4 soundness argument for BurnOnly (checking only: no positive ownCS mint + minting-logic withdrawal present, skipping custody/registration/params) HOLDS against the actual companion code, but only because of several silent preconditions the claim leans on without stating. I traced every escape vector for "pre-existing ownCS moves base->non-base while no positive ownCS is minted" and each dies on a companion invariant that IS present in the cited code.

WHY IT HOLDS (verified per-path):

1. Base spend forces a container. mkProgrammableLogicBase (ProgrammableLogicBase.hs:714-727) is the spending validator at progLogicCred; it walks ALL withdrawals and passes iff globalCred OR seizeCred is present (line 723). So any spend of a base ownCS UTxO forces either the global TransferAct or the seize validator to run over the whole tx. No third route.

2. Global TransferAct contains ownCS with no omission. pvalueFromCred (386-433) visits EVERY input, and for each base-cred input it forces an owner witness — pubkey signer (415-418) or script withdrawal (421-424) — erroring otherwise; a pointer/absent stake errors via pjustData/`_ -> perror` (425). So no base ownCS input can be silently dropped from the aggregate. pcheckTransferLogicAndGetProgrammableValue (837-901) consumes exactly one directory proof per input currency-pair (no omission: phead#proofs on the input walk). The burned ownCS enters the mint field; pcheckMintLogicAndGetProgrammableValue (927-981) requires exactly one proof per mint entry ("mint proof missing" 973 / "extra mint proof" 979). The union + pfilterPositiveCurrencyPairs (1157-1174) then feeds poutputsContainExpectedValueAtCred (512-697), which only credits outputs at progLogicCred. Net: spent ownCS minus burn must remain at base; non-base cannot gain.

3. The covering-proof forgery that WOULD break (2) is impossible. To exclude ownCS from containment the attacker needs a NEGATIVE/covering proof (nodeKey < ownCS < nodeNext, lines 855-861). ownCS is registered (see condition C2), so a well-formed node with key==ownCS exists and its predecessor's next==ownCS; the sorted, insert-only, one-shot-rooted directory has no overlapping interval, so no valid node covers a registered key. A forged reference UTxO can't substitute because directory node tokens can never leave the directory address with an attacker-chosen datum: makeCommon.atNodeValidator forces node outputs to the node-input address (Common.hs:145-154), pInsert takes exactly one node input via pheadSingleton (250) and re-locks it with structurally-enforced datums (pisInsertedOnNode/pisInsertedNode, 263-272), and DirectoryNodeAction has only Init/Insert — both mint exactly +1 (correctNodeTokenMinted, Common.hs:116,276), so there is no delete/burn and no node-mobility path.

4. Seize path preserves non-seized ownCS exactly. processThirdPartyTransfer.go (1445-1462) walks EVERY input; pcheckCorrespondingThirdPartyTransferInputsAndOutputs (1355-1392) pairs each base-cred input with a base output enforcing pvalueEqualsDeltaCurrencySymbol (1383) — equal everywhere except the seized CS — so burning/moving a NON-seized ownCS out of base can't validate. When ownCS IS the seized policy, checkBalanceInvariant/go2 (1419-1438) only sums progLogicCred outputs, so the seized delta+mint must reappear at base; outputs before poutputsStartIdx are simply unexamined but ledger value-conservation makes any ownCS there sourced from already-escaped non-base inputs, never a new escape.

5. Non-base ownCS inputs are already-escaped supply; burning them (negative delta) only shrinks escaped supply.

So with (b) "no positive ownCS entry" guaranteeing zero new tokens, every remaining ownCS in any output is pre-existing and every base->non-base move is caught. The argument is correct — but it is NOT self-contained; it silently requires the conditions listed. Verdict HOLDS_WITH_CONDITIONS rather than an unqualified HOLDS because those couplings live in OTHER validators and in the redesigned Mint path, none of which BurnOnly checks; if any regresses, BurnOnly becomes an exploitable escape./adversarial escape/custody analysis of the redesigned BurnOnly issuance witness against the actual base/global/seize/directory validators] C5 (Withdraw-zero ledger semantics): presence of globalCred/seizeCred in ptxInfo'wdrl is trusted to mean those stake scripts actually validated this tx (standard Cardano semantics). The base validator only checks credential presence (723), not re-runs the logic.
- [The §3.4 soundness argument for BurnOnly (checking only: no positive ownCS mint + minting-logic withdrawal present, skipping custody/registration/params) HOLDS against the actual companion code, but only because of several silent preconditions the claim leans on without stating. I traced every escape vector for "pre-existing ownCS moves base->non-base while no positive ownCS is minted" and each dies on a companion invariant that IS present in the cited code.

WHY IT HOLDS (verified per-path):

1. Base spend forces a container. mkProgrammableLogicBase (ProgrammableLogicBase.hs:714-727) is the spending validator at progLogicCred; it walks ALL withdrawals and passes iff globalCred OR seizeCred is present (line 723). So any spend of a base ownCS UTxO forces either the global TransferAct or the seize validator to run over the whole tx. No third route.

2. Global TransferAct contains ownCS with no omission. pvalueFromCred (386-433) visits EVERY input, and for each base-cred input it forces an owner witness — pubkey signer (415-418) or script withdrawal (421-424) — erroring otherwise; a pointer/absent stake errors via pjustData/`_ -> perror` (425). So no base ownCS input can be silently dropped from the aggregate. pcheckTransferLogicAndGetProgrammableValue (837-901) consumes exactly one directory proof per input currency-pair (no omission: phead#proofs on the input walk). The burned ownCS enters the mint field; pcheckMintLogicAndGetProgrammableValue (927-981) requires exactly one proof per mint entry ("mint proof missing" 973 / "extra mint proof" 979). The union + pfilterPositiveCurrencyPairs (1157-1174) then feeds poutputsContainExpectedValueAtCred (512-697), which only credits outputs at progLogicCred. Net: spent ownCS minus burn must remain at base; non-base cannot gain.

3. The covering-proof forgery that WOULD break (2) is impossible. To exclude ownCS from containment the attacker needs a NEGATIVE/covering proof (nodeKey < ownCS < nodeNext, lines 855-861). ownCS is registered (see condition C2), so a well-formed node with key==ownCS exists and its predecessor's next==ownCS; the sorted, insert-only, one-shot-rooted directory has no overlapping interval, so no valid node covers a registered key. A forged reference UTxO can't substitute because directory node tokens can never leave the directory address with an attacker-chosen datum: makeCommon.atNodeValidator forces node outputs to the node-input address (Common.hs:145-154), pInsert takes exactly one node input via pheadSingleton (250) and re-locks it with structurally-enforced datums (pisInsertedOnNode/pisInsertedNode, 263-272), and DirectoryNodeAction has only Init/Insert — both mint exactly +1 (correctNodeTokenMinted, Common.hs:116,276), so there is no delete/burn and no node-mobility path.

4. Seize path preserves non-seized ownCS exactly. processThirdPartyTransfer.go (1445-1462) walks EVERY input; pcheckCorrespondingThirdPartyTransferInputsAndOutputs (1355-1392) pairs each base-cred input with a base output enforcing pvalueEqualsDeltaCurrencySymbol (1383) — equal everywhere except the seized CS — so burning/moving a NON-seized ownCS out of base can't validate. When ownCS IS the seized policy, checkBalanceInvariant/go2 (1419-1438) only sums progLogicCred outputs, so the seized delta+mint must reappear at base; outputs before poutputsStartIdx are simply unexamined but ledger value-conservation makes any ownCS there sourced from already-escaped non-base inputs, never a new escape.

5. Non-base ownCS inputs are already-escaped supply; burning them (negative delta) only shrinks escaped supply.

So with (b) "no positive ownCS entry" guaranteeing zero new tokens, every remaining ownCS in any output is pre-existing and every base->non-base move is caught. The argument is correct — but it is NOT self-contained; it silently requires the conditions listed. Verdict HOLDS_WITH_CONDITIONS rather than an unqualified HOLDS because those couplings live in OTHER validators and in the redesigned Mint path, none of which BurnOnly checks; if any regresses, BurnOnly becomes an exploitable escape./adversarial escape/custody analysis of the redesigned BurnOnly issuance witness against the actual base/global/seize/directory validators] C6 (Directory well-formedness maintained across future edits): the covering-proof-impossibility relies on the directory staying sorted, unique-keyed, insert-only, one-shot-rooted, with node tokens immobile and datum-locked (Common.hs atNodeValidator + pInsert + MintDirectory). Any future directory change (a delete redeemer, relaxed atNodeValidator, or node mobility) reopens attacks 1-2.
- [The §3.4 soundness argument for BurnOnly (checking only: no positive ownCS mint + minting-logic withdrawal present, skipping custody/registration/params) HOLDS against the actual companion code, but only because of several silent preconditions the claim leans on without stating. I traced every escape vector for "pre-existing ownCS moves base->non-base while no positive ownCS is minted" and each dies on a companion invariant that IS present in the cited code.

WHY IT HOLDS (verified per-path):

1. Base spend forces a container. mkProgrammableLogicBase (ProgrammableLogicBase.hs:714-727) is the spending validator at progLogicCred; it walks ALL withdrawals and passes iff globalCred OR seizeCred is present (line 723). So any spend of a base ownCS UTxO forces either the global TransferAct or the seize validator to run over the whole tx. No third route.

2. Global TransferAct contains ownCS with no omission. pvalueFromCred (386-433) visits EVERY input, and for each base-cred input it forces an owner witness — pubkey signer (415-418) or script withdrawal (421-424) — erroring otherwise; a pointer/absent stake errors via pjustData/`_ -> perror` (425). So no base ownCS input can be silently dropped from the aggregate. pcheckTransferLogicAndGetProgrammableValue (837-901) consumes exactly one directory proof per input currency-pair (no omission: phead#proofs on the input walk). The burned ownCS enters the mint field; pcheckMintLogicAndGetProgrammableValue (927-981) requires exactly one proof per mint entry ("mint proof missing" 973 / "extra mint proof" 979). The union + pfilterPositiveCurrencyPairs (1157-1174) then feeds poutputsContainExpectedValueAtCred (512-697), which only credits outputs at progLogicCred. Net: spent ownCS minus burn must remain at base; non-base cannot gain.

3. The covering-proof forgery that WOULD break (2) is impossible. To exclude ownCS from containment the attacker needs a NEGATIVE/covering proof (nodeKey < ownCS < nodeNext, lines 855-861). ownCS is registered (see condition C2), so a well-formed node with key==ownCS exists and its predecessor's next==ownCS; the sorted, insert-only, one-shot-rooted directory has no overlapping interval, so no valid node covers a registered key. A forged reference UTxO can't substitute because directory node tokens can never leave the directory address with an attacker-chosen datum: makeCommon.atNodeValidator forces node outputs to the node-input address (Common.hs:145-154), pInsert takes exactly one node input via pheadSingleton (250) and re-locks it with structurally-enforced datums (pisInsertedOnNode/pisInsertedNode, 263-272), and DirectoryNodeAction has only Init/Insert — both mint exactly +1 (correctNodeTokenMinted, Common.hs:116,276), so there is no delete/burn and no node-mobility path.

4. Seize path preserves non-seized ownCS exactly. processThirdPartyTransfer.go (1445-1462) walks EVERY input; pcheckCorrespondingThirdPartyTransferInputsAndOutputs (1355-1392) pairs each base-cred input with a base output enforcing pvalueEqualsDeltaCurrencySymbol (1383) — equal everywhere except the seized CS — so burning/moving a NON-seized ownCS out of base can't validate. When ownCS IS the seized policy, checkBalanceInvariant/go2 (1419-1438) only sums progLogicCred outputs, so the seized delta+mint must reappear at base; outputs before poutputsStartIdx are simply unexamined but ledger value-conservation makes any ownCS there sourced from already-escaped non-base inputs, never a new escape.

5. Non-base ownCS inputs are already-escaped supply; burning them (negative delta) only shrinks escaped supply.

So with (b) "no positive ownCS entry" guaranteeing zero new tokens, every remaining ownCS in any output is pre-existing and every base->non-base move is caught. The argument is correct — but it is NOT self-contained; it silently requires the conditions listed. Verdict HOLDS_WITH_CONDITIONS rather than an unqualified HOLDS because those couplings live in OTHER validators and in the redesigned Mint path, none of which BurnOnly checks; if any regresses, BurnOnly becomes an exploitable escape./adversarial escape/custody analysis of the redesigned BurnOnly issuance witness against the actual base/global/seize/directory validators] C7 (Authorization is separate and still required): (a) 'minting-logic withdrawal present at witnessed index' must actually bind to the mintingLogic credential (not merely read an arbitrary indexed withdrawal); this is the §7 supply-authorization guarantee, outside the escape argument but required for overall soundness.

## Change-site maps


### ProgrammableLogicGlobalParams datum expansion (add globalLogicCred + seizeLogicCred) and §4.1 anchor-policy hardening — every construction/consumption site across onchain, offchain, examples, tests, benches, and JSON/OpenApi schemas in /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d

Key facts:
- ENCODING IS DATA-LIST, NOT CONSTR: ToData at src/programmable-tokens-onchain/lib/SmartTokens/Types/ProtocolParams.hs:58-62 hand-builds BI.mkList [directoryNodeCS, progLogicCred]; FromData at :51-56 uses BI.chooseData/unsafeDataAsList + head/tail and SILENTLY IGNORES extra fields (lenient decode). The Aiken baseline instead uses Constr 0 with 3 fields (aikenProtocolParamsDatumData, src/programmable-tokens-test/exe/BenchmarkOnchainAikenScripts.hs:161-179, third field unfracking_cred) - do not copy its shape.
- Plutarch type PProgrammableLogicGlobalParams (Types/ProtocolParams.hs:64-74) derives PlutusType via DeriveAsDataRec (data-list record) and PLiftable via DeriveDataPLiftable; every onchain decode is punsafeCoerce @(PAsData PProgrammableLogicGlobalParams) on the inline datum (ProgrammableLogicBase.hs:797, SpendDirectory.hs:50, :75). Spec section 4 mandates: fields 0/1 byte-compatible and appended fields 2/3 MUST NOT be forced by hot paths (raw branch-local access).
- Spec section 4 normative field order: 0=directoryNodeCS, 1=progLogicCred (unchanged), 2=globalLogicCred (global transfer rewarding credential), 3=seizeLogicCred (standalone seize rewarding credential); both stored as Data-encoded Credential (NOT ScriptHash) so consumers get direct equalsData compares (601 mem vs 2129 mem byte-decomposed).
- Section 4.1 hardened anchor checks for mkProtocolParametersMinting (src/programmable-tokens-onchain/lib/SmartTokens/Contracts/ProtocolParams.hs:24-41, currently only: single-TN pheadSingleton :34, TN==ProtocolParams :38, qty==1 :39, one-shot input spent :40): ADD (a) NFT output sits at canonical alwaysFailScript address (alwaysFailScript :55-56) with NO stake credential, (b) output value is exactly min-ada + the single NFT (licenses runtime first-non-Ada phasCSH check), (c) inline datum fully decodes as the 4-field type, (d) currency symbol canonical 28 bytes, (e) each of the 3 credential fields is ScriptCredential (constructor 1) wrapping a canonical 28-byte hash. All genesis-only; runtime consumers keep cheap phasCSH (ProgrammableLogicBase.hs:746-749) + raw datum indexing and MUST NOT re-verify TN/qty/datum shape.
- Runtime onchain consumers that decode the params (all read only fields 0/1 today): pfindReferenceInputByCS ProgrammableLogicBase.hs:789-806 (decode :797); mkProgrammableLogicGlobal TransferAct arm :1120-1123 (uses pdirectoryNodeCS :1135/:1167, pprogLogicCred :1123); mkProgrammableSeize :1219-1222 (uses both, :1244/:1246); pmkDirectoryGlobalLogic SpendDirectory.hs:50-51 (field 0 only); pmkDirectorySpending SpendDirectory.hs:75,:89 (field 0 only). None need the new fields; the NEW consumers of fields 2/3 are the redesigned issuance policy (spec section 5: 2 params protocolParamsCS + mintingLogicHash, replacing Issuance.hs:100-126 3-param version) and genesis derivation.
- The base validator (ProgrammableLogicBase.hs:714-727) keeps globalCred/seizeCred as COMPILE-TIME params (spec 11.5); genesis builder must derive them from the same scripts stored in the params datum. Offchain that derivation already exists: programmableLogicBaseScript Scripts.hs:92-101 internally computes seizeCred from programmableSeizeScript :106-108; globalParams (Env/Directory.hs:169-174) must now also expose these two credentials to fill fields 2/3.
- Offchain datum WRITE site is single: mintProtocolParams src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/ProtocolParams.hs:27-53 (datum :46 toHashableScriptData params; addr already NoStakeAddress :43; value :35-36 NFT-only so the exact-min-ada rule constrains balancing). Offchain datum READ/DECODE sites: getProtocolParamsGlobalInline :55-59; Query.globalParamsNode Query.hs:179-185; issueProgrammableToken equality gate BuildTx/ProgrammableLogic.hs:71 'glParams == uDatum paramsTxOut' (Eq on the record - env derivation and deployed datum must match exactly, all 4 fields).
- JSON: ToJSON/FromJSON (Types/ProtocolParams.hs:87-98) use per-field scriptDataToJson NoSchema under keys directory_node_currency_symbol / programmable_logic_credential; two new keys needed. OpenApi: ToParamSchema :100-118 (2 properties) + ToSchema :120-121; regenerated artifacts generated/openapi/schema.json:122-130,282-285,473 and frontend/src/app/store/schema.json:51-59,180-183,321 (regenerate via src/examples/regulated-stablecoin/exe/write-openapi-schema/Main.hs).
- Issuance-template CBOR split couples to the params: issuerPrefixPostfixBytes (BuildTx/IssuanceCborHexRef.hs:41-56) currently applies progLogicCred + directoryNodeCS to mkProgrammableLogicMinting and splits on the last-applied mintingLogicHash; under section 5 it applies protocolParamsCS only. Same for programmableTokenMintingScript (Env/TransferLogic.hs:59-64) and the stale exporter copy issuerPrefixPostfixBytes in src/examples/regulated-stablecoin/exe/export-smart-tokens/Main.hs:141-152 (+ consumption :228-229).
- Spec section 12 derivation order (one public constructor, no caller-supplied hashes): anchor script -> protocolParamsCS -> global+seize validators (parameterized by protocolParamsCS) -> base validator (their two hashes) -> directoryNodeCS -> 4-field datum -> issuance template. mkDirectoryEnv (Env/Directory.hs:128-150) is the knot-tied constructor to rework; protocolParamsMintingScript (Scripts.hs:48-49, params: target+TxIn) will need the anchor script hash if the hardened policy takes the canonical address as a parameter.
- Redeemer companion (spec 11.3/11.4, ProgrammableLogicBase.hs:983-998): TransferAct gains mintProofs::[MintProof=Member|NonMember nodeIdx] + paramsRefIdx; SeizeAct appends paramsRefIdx as final field; existing positions frozen (golden test). Mint walk pcheckMintLogicAndGetProgrammableValue :920-981 loses the member-case transfer-logic invocation (:953) and member node fetch/decode/auth.

Change sites:
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-onchain/lib/SmartTokens/Types/ProtocolParams.hs @ lines 44-47 (record), 51-56 (FromData), 58-62 (ToData), 64-74 (PProgrammableLogicGlobalParams + PLiftable), 87-98 (ToJSON/FromJSON), 100-121 (ToParamSchema/ToSchema): Add globalLogicCred/seizeLogicCred :: Credential as fields 2/3 in every encoding: extend the hand-rolled List ToData (append two BI.mkCons), extend FromData (two more BI.tail steps; decide strict-4 vs lenient), add pglobalLogicCred/pseizeLogicCred :: Term s (PAsData PCredential) to the Plutarch record, add two JSON keys, add two OpenApi properties.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-onchain/lib/SmartTokens/Contracts/ProtocolParams.hs @ mkProtocolParametersMinting lines 24-41; alwaysFailScript lines 55-56: Harden per section 4.1: locate the unique output at the canonical alwaysFail address (new parameter or derived hash), require no stake credential, value exactly min-ada + single NFT, inline datum that fully decodes as the 4-field type, 28-byte CS, and each of the 3 credentials ScriptCredential w/ 28-byte hash; keep existing one-shot/TN/qty checks.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-onchain/lib/SmartTokens/Contracts/ProgrammableLogicBase.hs @ pfindReferenceInputByCS 789-806 (decode :797); mkProgrammableLogicGlobal 1098-1197 (params 1120-1123); mkProgrammableSeize 1213-1248 (params 1219-1222); redeemer type 983-998; mint walk 920-981; base validator 714-727 (unchanged params, derivation coupling): Keep field-0/1 access raw and lazy after the type gains fields 2/3 (verify DeriveAsDataRec partial pmatch does not force new fields, else switch to explicit head/tail indexing); apply companion changes: paramsRefIdx in TransferAct/SeizeAct, MintProof Member|NonMember, delete member-case transfer-logic invocation at :953.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-onchain/lib/SmartTokens/LinkedList/SpendDirectory.hs @ pmkDirectoryGlobalLogic decode line 50; pmkDirectorySpending decode line 75: Consumption sites reading only pdirectoryNodeCS via punsafeCoerce record match - must keep compiling and must not start decoding fields 2/3; re-audit after type change.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-onchain/lib/SmartTokens/Contracts/Issuance.hs @ mkProgrammableLogicMinting lines ~100-126 (current 3-param signature; directoryNodeCS param :105/:126): Redesign to 2 params (protocolParamsCS, mintingLogicHash last) per spec section 5; becomes the FIRST onchain consumer of fields 2 (globalLogicCred, DelegateTransfer) and 3 (seizeLogicCred, DelegateSeize) read from the authenticated params ref input.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Env/Directory.hs @ globalParams 169-174; getGlobalParams 176-177; mkDirectoryEnv 128-150; programmableLogicStakeCredential 152-154; DirectoryEnv record 115-126: Construct the two NEW fields: globalLogicCred from dsProgrammableLogicGlobalScript hash, seizeLogicCred from programmableSeizeScript hash (currently derived only inside Scripts.programmableLogicBaseScript - expose it, likely as a DirectoryEnv field/accessor); rework mkDirectoryEnv to the section 12 acyclic derivation order.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Scripts.hs @ protocolParamsMintingScript 48-49; protocolParamsSpendingScript 55-56; programmableLogicBaseScript 92-101; programmableSeizeScript 106-108; programmableLogicGlobalScript 111-112; programmableLogicMintingScript ~124-131: Thread the anchor-script hash into protocolParamsMintingScript if section 4.1 parameterizes it; change programmableLogicMintingScript to the new 2-param issuance application (protocolParamsCS + mintingLogicHash).
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/ProtocolParams.hs @ mintProtocolParams 27-53 (datum :46, value 35-36, addr 38-43); getProtocolParamsGlobalInline 55-59: Sole datum-write site: emits the 4-field datum automatically once globalParams changes, but the output must now satisfy the hardened policy (exact min-ada + NFT only; no other assets after balancing). Decode helper picks up new FromData.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/IssuanceCborHexRef.hs @ issuerPrefixPostfixBytes 41-56 (template application :52); mintIssuanceCborHexNFT field consumption 75-76; getCborHexInline 123-127: Rebuild CBOR prefix/postfix around the new 2-param issuance template (apply protocolParamsCS instead of progLogicCred+directoryNodeCS; mintingLogicHash stays the split point); note getCborHexInline's return type is ProgrammableLogicGlobalParams (looks like a latent copy-paste - should be IssuanceCborHex) and will silently change behavior with the new FromData.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Env/TransferLogic.hs @ programmableTokenMintingScript 59-64 (destructures directoryNodeCS+progLogicCred at :61): Switch to instantiating the issuance template with protocolParamsCS + token minting-logic hash; record destructuring keeps compiling (NamedFieldPuns subset) but the applied params change.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/ProgrammableLogic.hs @ issueProgrammableToken 57-71 (Eq gate :71); transferProgrammableToken 187: Signatures take UTxODat era ProgrammableLogicGlobalParams (type flows through); the :71 equality between env-derived params and the deployed datum now covers 4 fields - env derivation must match the deployed instance exactly.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/Directory.hs @ import :48; insertDirectoryNode signature 90-94 (paramsRef reference input): Type flows through as reference-input datum; recompiles against new record. Registration redeemer/CBOR reconstruction updates come with the issuance-template change (spec 11.6).
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Query.hs @ import :52; globalParamsNode 179-185: FromData decode of the deployed datum - after redeploy returns 4-field values; if FromData is made strict, old 2-field deployments become undecodable (acceptable per full-redeploy decision).
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/examples/regulated-stablecoin/exe/export-smart-tokens/Main.hs @ issuerPrefixPostfixBytes 141-152 (stale 1-param application :147); consumption 228-229; script exports 219-238: Update the exporter's own prefix/postfix derivation to the new template and export the seize validator + new params policy artifacts.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/examples/regulated-stablecoin/lib/Wst/Client.hs + Wst/Server/Types.hs + Wst/Offchain/BuildTx/TransferLogic.hs + Wst/Offchain/BuildTx/ProgrammableLogic.hs + Wst/Offchain/Env.hs @ Client.hs:27,39 (getGlobalParams API); Server/Types.hs:70,144 (global-params route, JSON+ToSchema); TransferLogic.hs:230,238,290,341 (signatures); ProgrammableLogic.hs:58,80; Env.hs:24,87 (re-exports): Example-app pass-through sites: recompile against new record; the served JSON and OpenApi schema change shape (two new keys/properties); regenerate schema artifacts.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/lib/ProgrammableTokens/Gen.hs @ genGlobalParams 27-31: Add two more genCredential applications for the new fields (no current downstream users found, but exported).
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/test/ProgrammableTokens/Test/ProgrammableLogicGlobal.hs @ protocolParamsDatum 540-542; fixture ids 468-490 (globalCred :480, needs seizeCred); script application 284, 388-408; ~20 params ref-input embeds at 656-657, 700-701, 736-737, 762-763, 795-796, 818-819, 841-842, 870-871, 903-904, 936-937, 988-989, 1025-1026, 1063-1064, 1097-1098, 1123-1124, plus 300-312: Extend the 2-field datum construction to 4 fields (add globalCred + a new seize fixture credential); embedded datum bytes update automatically via ToData.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/test/ProgrammableTokens/Test/ProgrammableLogicMinting.hs @ mintArgs 80-86 (applies progLogicBaseCred, directoryNodeCS, mintingLogicHash); fixtures 108-147: Rewrite applied-arg list for the 2-param issuance policy and add a protocol-params reference input (with 4-field datum) to every minting fixture, since the policy now reads credentials from the params UTxO.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchainScripts.hs @ protocolParamsDatum 186-188; insertProtocolParamsDatum 916; protocolParamsAlwaysFailHash 178-180; protocolParamsMintCtx 1255-1271; ~25 params ref-input embeds (354-355, 392-393, 449-450, 525-526, 587-588, 632-633, 702-703, 732-733, 830-831, 865-866, 981, 1036-1037, 1081-1082, 1129-1130, 1179-1180, 1410-1411); issuance case args 1517-1522; global/seize case args 1487-1514, 1523, 1537: 4-field datum fixtures; protocolParamsMintCtx must satisfy all new hardened checks (exact min-ada value calibration at :1268, 4-field inline datum, script-credential fields); issuance benchmark arg lists shrink to [protocolParamsCS, mintingLogicHash, ctx] and their contexts gain a params ref input.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchain/ScriptFixtureIds.hs @ protocolParamsCS 73-74, directoryNodeCS 76-77, protocolParamsInitRef 244-245: Add shared fixture ids for global/seize credentials if benches need the real derived ones for the hardened mint ctx and issuance arms.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchainAikenScripts.hs @ aikenProtocolParamsDatumData 165-179 (Constr 0, 3 fields incl. unfracking_cred); protocolParamsDatum 296-298; Aiken script applications 1591-1795: Comparison baseline (Aiken ca7864f) - encodings stay AS-IS; only touch if shared fixture ids move. Do not port the Plutarch 4-field List datum here.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/generated/openapi/schema.json and /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/frontend/src/app/store/schema.json @ generated: 122-130, 282-285, 473; frontend: 51-59, 180-183, 321: Regenerate (write-openapi-schema exe) and copy to frontend: ProgrammableLogicGlobalParams gains global_logic_credential and seize_logic_credential properties.

Risks:
- DeriveAsDataRec pmatch strictness: if pattern-matching PProgrammableLogicGlobalParams{pdirectoryNodeCS,pprogLogicCred} forces decoding of appended fields 2/3, every hot consumer (global TransferAct, seize, SpendDirectory) regresses - spec section 4 explicitly forbids this; may need to abandon record pmatch at those sites for raw head/tail indexing. Verify with the isolation bench before accepting.
- FromData leniency decision: the current hand-rolled decoder ignores trailing fields, so a strict 4-field offchain decoder will refuse old 2-field deployments (fine, full redeploy) but a lenient one lets a 2-field datum slip past offchain checks while the hardened MINT policy is the only strict gate - the two must not disagree about validity.
- issueProgrammableToken's Eq gate (BuildTx/ProgrammableLogic.hs:71) silently couples env derivation to the deployed datum: if globalParams derives seizeLogicCred any way other than exactly how genesis wrote it (e.g. Production vs Debug ScriptTarget variants), issuance breaks at runtime with a mismatch error, not a compile error.
- protocolParamsMintingScript's parameter set may grow (anchor script hash for the canonical-address check) - that changes protocolParamsCS itself, which cascades through EVERY derived hash (mkDirectoryEnv knot at Env/Directory.hs:128-150, section 12 order) and invalidates all golden hashes; get the derivation order right once.
- Exact 'min-ada plus that single NFT' check needs a precise onchain formulation; the offchain builder (BuildTx/ProtocolParams.hs:35-36) currently emits an NFT-only value and relies on balancing to top up ada - if balancing adds more than min-ada or another asset the genesis mint becomes impossible. Bench fixture protocolParamsMintCtx:1268 hardcodes 2 ada and must be recalibrated to whatever the check accepts.
- getCborHexInline (IssuanceCborHexRef.hs:123-127) is typed Maybe ProgrammableLogicGlobalParams but reads the IssuanceCborHex UTxO - latent bug; with a strict 4-field FromData it will start returning Nothing differently. Fix or it will confuse the Phase 4 offchain work.
- The 466 grep hits include many same-named local binders (e.g. poutputsContainExpectedValueAtCred's progLogicCred lambda arg, test-local directoryNodeCS constants, PTokenDirectory's unrelated directoryNodeCS' let at PTokenDirectory.hs:169) - do not confuse them with datum-field sites; only the sites listed in change_sites construct/decode the params record.
- Aiken benchmark baseline (BenchmarkOnchainAikenScripts.hs, AikenProgrammableTokenScripts.hs - both currently git-modified on this branch) must keep its own 3-field Constr datum; accidentally 'fixing' it to the Plutarch shape invalidates the section 14.3 comparison gates.
- OpenApi schema regeneration is easy to forget: generated/openapi/schema.json and the frontend copy are committed artifacts; CI or frontend consumers will drift if only the Haskell instances change.

Test touchpoints:
- src/programmable-tokens-test/test/ProgrammableTokens/Test/ProgrammableLogicGlobal.hs - protocolParamsDatum (540-542) + ~20 ref-input fixtures + fixture credentials (468-490); all global/seize unit tests re-run against the 4-field datum
- src/programmable-tokens-test/test/ProgrammableTokens/Test/ProgrammableLogicMinting.hs - mintArgs (80-86) arg-shape change + params ref input added to every issuance fixture; the whole file exercises the redesigned policy
- src/programmable-tokens-test/exe/BenchmarkOnchainScripts.hs - protocolParamsMintCtx (1255-1271) becomes the de-facto positive test of the hardened anchor policy; issuance/global/seize case arg lists (1487-1537)
- src/programmable-tokens-test/exe/BenchmarkOnchainAikenScripts.hs + BenchmarkOnchain/ScriptRunner.hs + ScriptFixtureIds.hs - comparison harness must still run both sides (section 14.3 gates)
- src/programmable-tokens-test/lib/ProgrammableTokens/Gen.hs genGlobalParams - extend for any JSON/roundtrip property tests added in Phase 1 (golden encoding test for the datum List layout and redeemer layout is a spec 11.3 requirement - add here)
- src/examples/regulated-stablecoin/test/unit/Wst/Test/UnitTest.hs - end-to-end deploy/issue/transfer/seize via Query.globalParamsNode (:96) and Endpoints.seizeCredentialAssetsTx (:253) exercises the full 4-field datum lifecycle on the emulator
- New spec-mandated tests (sections 4.1/14.1): mint-time red tests - key credential in any credential slot unmintable, wrong address/stake cred/extra assets/short hash/malformed datum all rejected; and golden test comparing every DirectoryEnv stored hash against the exported script (section 12)
- Build note: tests/benchmarks need /usr/local/lib libsodium (VRF) - use --extra-lib-dirs (from project memory)

### Global transfer validator, standalone seize validator, base validator, and protocol-params datum/policy in the Plutarch CIP-113 implementation — mapping for spec §11.3 (global: MintProof Member|NonMember + paramsRefIdx), §11.4 (seize: paramsRefIdx), §11.5 (base: keep scan), §4/§4.1 (4-field params datum + anchor hardening), grounded in spec §§3.5, 9, 11 of /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/doc/design-issuance-dual-arm-custody.md

Key facts:
- CURRENT REDEEMER ENCODING (src/programmable-tokens-onchain/lib/SmartTokens/Contracts/ProgrammableLogicBase.hs:983-998): data ProgrammableLogicGlobalRedeemer = TransferAct { plgrTransferProofs :: [Integer], plgrMintProofs :: [Integer] } | SeizeAct { plgrDirectoryNodeIdx :: Integer, plgrInputIdxs :: [Integer], plgrOutputsStartIdx :: Integer, plgrLengthInputIdxs :: Integer }; encoded via PlutusTx.makeIsDataIndexed [('TransferAct,0),('SeizeAct,1)] at lines 996-998. So today: Constr 0 [List[I], List[I]] and Constr 1 [I, List[I], I, I].
- PLUTARCH MIRROR (same file 1063-1086): PProgrammableLogicGlobalRedeemer with PTransferAct { ptransferProofs, pmintProofs :: PAsData (PBuiltinList (PAsData PInteger)) } | PSeizeAct { pdirectoryNodeIdx, pinputIdxs, poutputsStartIdx, plengthInputIdxs }; deriving PlutusType via DeriveAsDataStruct (1081), PLiftable via DeriveDataPLiftable (1083-1086). The validators read it by punsafeCoerce of the raw redeemer, not pfromData decode: global at 1102, seize at 1217.
- SPEC FROZEN TARGET LAYOUT (doc §11.3, lines 601-603): TransferAct { transferProofs, mintProofs :: [MintProof], paramsRefIdx } and SeizeAct with paramsRefIdx appended as its FINAL (5th) field; existing field positions unchanged; both pinned by a golden encoding test (none exists today — grep for 'golden' in tests is empty). MintProof = Member | NonMember nodeIdx (§11.3 line 585); Member is a nullary tag (line 596-597); constructor indices are a fresh choice to pin (suggest Member=0, NonMember=1).
- PARAMS UTXO LOOKUP TODAY IS A SCAN: pfindReferenceInputByCS (ProgrammableLogicBase.hs:789-806) walks reference inputs using phasCSHOrFalse (766-772, first-non-Ada probe) and decodes the datum via punsafeCoerce to PAsData PProgrammableLogicGlobalParams (797). Exactly two consumers in this module: global TransferAct branch at 1120-1122 and seize preamble at 1219-1221. A separate PRIVATE duplicate of pfindReferenceInputByCS lives in SmartTokens/LinkedList/SpendDirectory.hs:24-28 (used at 48, 72; reads only pdirectoryNodeCS) — unaffected by paramsRefIdx but recompiles against the 4-field datum type.
- MINT-WALK STRUCTURE (pcheckMintLogicAndGetProgrammableValue, ProgrammableLogicBase.hs:920-981): walks mint currency-pairs in lockstep with proofList :: PBuiltinList (PAsData PInteger). Per entry: ref-input fetch at nodeIdx via pdropFast (938), POutputDatum decode (939), FULL PDirectorySetNode decode of pkey/pnext/ptransferLogicScript (940-945), then branch on nodeKey #== currCS: MEMBER case (949-960) requires transfer-logic withdrawal scan pisScriptInvokedEntries at line 953 (THE mint-path transfer-logic check §11.3 deletes) + node auth phasCSH at 954, accumulates csPair at 958; NON-MEMBER case (961-971) requires covering interval nodeKey<currCS<nodeNext (963-964) + phasCSH (965), does not accumulate. 'mint proof missing' (973) / 'extra mint proof' (979) enforce no-omission/no-extras; accumulator reversed via preverseCurrencyPairs (979). Result merged into expected output value at 1157-1174 via pcurrencyPairsUnionFast + pfilterPositiveCurrencyPairs, then containment via poutputsContainExpectedValueAtCred (1179-1182).
- SPEC §11.3 MEMBER DELETIONS: for a Member tag, delete the ref-input fetch (938), the datum decode (939-945), the node authentication (954), AND the transfer-logic invocation scan (953) — accumulate the mint entry unconditionally (self-penalizing: only adds the claimant's delta to containment). NonMember nodeIdx keeps fetch + key/next decode + covering checks + phasCSH — that direction (registered policy obtaining a non-member proof) is the escape and is load-bearing for DelegateTransfer (§9.1: an authentic node at issuance nodeRefIdx makes any NonMember claim for ownCS unsatisfiable). Input-side transfer proofs (pcheckTransferLogicAndGetProgrammableValue, 827-901) are UNCHANGED per §11.3.
- SEIZE VALIDATOR PARAMS ACCESS (mkProgrammableSeize, ProgrammableLogicBase.hs:1213-1248): params scan currently runs in the PREAMBLE (1218-1222) BEFORE pmatch on the redeemer (1224) — to use SeizeAct's new paramsRefIdx it must move inside the PSeizeAct branch. Seize keeps the seized policy's mint map in its balance equation: seizeMintedTokens = ptokensForCurrencySymbol # nodeKey # mint (1240-1241), passed into processThirdPartyTransfer (1244) and unioned into the delta at 1459 — load-bearing for DelegateSeize (§9.2) and §3.4; MUST be retained (§11.4). SeizeAct.plgrDirectoryNodeIdx stays field 0: the issuance DelegateSeize arm decodes it and requires equality with its nodeRefIdx (§9.2 item 2). pinputIdxs/plengthInputIdxs are already dead on-chain (comment 1227-1229) but stay in the encoding (frozen positions).
- BASE VALIDATOR — NO CHANGE (§11.5, spec lines 607-622): mkProgrammableLogicBase (714-727) keeps the per-spend withdrawal scan over (globalCred, seizeCred) compile-time params; the spend-redeemer index was measured and REJECTED (worse on the binding memory axis for common shapes). The genesis builder MUST derive these credentials from the same scripts stored in the params datum — currently programmableLogicBaseScript (src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Scripts.hs:92-101) takes globalCred and internally derives the seize cred from paramsPolId (94-96).
- CURRENT PARAMS DATUM (src/programmable-tokens-onchain/lib/SmartTokens/Types/ProtocolParams.hs:44-74): 2 fields { directoryNodeCS :: CurrencySymbol, progLogicCred :: Credential }; hand-written FromData (51-56) and ToData (58-62) encode a PLAIN LIST (BI.mkList), not Constr; Plutarch side PProgrammableLogicGlobalParams via DeriveAsDataRec (64-71). §4 appends globalLogicCred and seizeLogicCred as Data-encoded Credential (NOT ScriptHash — reversal justified by equalsData mem numbers, spec lines 234-254); fields 0/1 stay byte-compatible so all existing containment comparisons and raw field reads are untouched. §4.1 makes misconfiguration unrepresentable at mint time instead of typing it away.
- PARAMS ANCHOR POLICY TODAY (src/programmable-tokens-onchain/lib/SmartTokens/Contracts/ProtocolParams.hs:24-41): mkProtocolParametersMinting checks only (i) minted TN == protocolParamsToken (38), (ii) qty == 1 (39), (iii) one-shot oref spent (40). §4.1 items it MUST add: NFT output at the canonical alwaysFailScript address with no stake credential and value exactly min-ada + NFT; inline datum fully decodes to the 4-field type; canonical 28-byte CS; each of the three credential fields is ScriptCredential (constructor 1) wrapping a 28-byte hash. This licenses runtime consumers to authenticate via the phasCSH first-non-Ada idiom + raw field indexing with NO re-validation (spec lines 282-289), which is what makes the paramsRefIdx indexed lookup sound: verify the value at the index with phasCSH, then punsafeCoerce raw field access.
- GLOBAL §3.5 INVARIANTS THAT MUST SURVIVE (now load-bearing for the issuance delegate arms): accepts only TransferAct (PSeizeAct rejected at 1196-1197); entire-mint-field walk with exactly one proof per minted policy (973/979); mandatory per-policy input transfer proofs (827-901); expected value at base credential (1176-1183); exact directory NFT auth (phasCSH). pisRewardingScript checks at 1177 (global) and 1243 (seize) stay.
- OFFCHAIN REDEEMER PRODUCERS: TransferAct built at src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/ProgrammableLogic.hs:209-213 (params ref input added at 222; proofNodeForSymbol/proofNodeIndex 237-249 supply node indices); SeizeAct built via mkSeizeActRedeemerFromAbsoluteInputIdxs at src/examples/regulated-stablecoin/lib/Wst/Offchain/BuildTx/ProgrammableLogic.hs:142-146 (params ref added at 155); smart constructors mkSeizeActRedeemerFromRelativeInputIdxs (ProgrammableLogicBase.hs:1034-1043) and mkSeizeActRedeemerFromAbsoluteInputIdxs (1057-1061) must grow a paramsRefIdx argument. The 4-field datum is CONSTRUCTED offchain only in Env/Directory.hs globalParams (169-174) — DirectoryEnv (124-137) has NO seize-script field today (derived internally in Scripts.hs:94-96), so it must be exposed to build seizeLogicCred and satisfy §12's golden-hash rule.

Change sites:
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-onchain/lib/SmartTokens/Contracts/ProgrammableLogicBase.hs @ lines 983-998 (Haskell redeemer + makeIsDataIndexed): Add data MintProof = Member | NonMember Integer with PlutusTx.makeIsDataIndexed [('Member',0),('NonMember',1)]; change plgrMintProofs :: [Integer] -> [MintProof] (986); append plgrParamsRefIdx :: Integer as TransferAct's final field (after 986) and as SeizeAct's final field (after plgrLengthInputIdxs, 992). Constructor indices 0/1 for TransferAct/SeizeAct unchanged.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-onchain/lib/SmartTokens/Contracts/ProgrammableLogicBase.hs @ lines 1063-1086 (PProgrammableLogicGlobalRedeemer + PLiftable): Add PMintProof Plutarch type (DeriveAsDataStruct; PMember nullary, PNonMember with PAsData PInteger) + PLiftable; change pmintProofs to PAsData (PBuiltinList (PAsData PMintProof)) (1068); add pparamsRefIdx :: Term s (PAsData PInteger) as last field of BOTH PTransferAct and PSeizeAct (1067-1078).
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-onchain/lib/SmartTokens/Contracts/ProgrammableLogicBase.hs @ lines 920-981 (pcheckMintLogicAndGetProgrammableValue): Signature: proofList becomes PBuiltinList (PAsData PMintProof). Restructure the walk to dispatch on the proof tag: Member -> accumulate mintCsPair with ZERO checks (delete ref fetch 937-938, POutputDatum decode 939, PDirectorySetNode decode 940-945, transfer-logic scan 953, phasCSH 954); NonMember nodeIdx -> keep pdropFast fetch, decode only pkey/pnext, keep covering checks (963-964) + phasCSH (965), no accumulation. Keep 'mint proof missing' (973), 'extra mint proof' (979), preverseCurrencyPairs (979).
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-onchain/lib/SmartTokens/Contracts/ProgrammableLogicBase.hs @ lines 1112-1183 (global PTransferAct branch): Positional pmatch PTransferAct transferProofs mintProofs (1112) gains a third paramsRefIdx binding. Replace pfindReferenceInputByCS scan (1120-1122) with verified indexed lookup: phead # (pdropFast # pfromData paramsRefIdx # referenceInputs), authenticate value via phasCSH # protocolParamsCS, then raw branch-local field access for fields 0 (directoryNodeCS) and 1 (progLogicCred) only — do NOT force fields 2/3 (spec §4 line 263-264). Mint-proof call site 1166-1172 passes the new proof-list type.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-onchain/lib/SmartTokens/Contracts/ProgrammableLogicBase.hs @ lines 1213-1248 (mkProgrammableSeize): Move the params lookup (currently preamble 1218-1222, before pmatch red at 1224) INTO the PSeizeAct branch; bind pparamsRefIdx in the record match at 1230 and replace the scan with the same verified indexed lookup (phasCSH-authenticated pdropFast). Fix the positional rejection pattern PTransferAct _ _ at 1225 (arity grows to 3). KEEP seizeMintedTokens in the balance equation (1240-1241, 1244; union at 1459) — load-bearing for DelegateSeize (§9.2) and burn soundness (§3.4). directoryNodeIdx stays field 0 and its post-lookup verification (phasCSH at 1246) stays.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-onchain/lib/SmartTokens/Contracts/ProgrammableLogicBase.hs @ lines 1034-1061 (mkSeizeActRedeemerFromRelativeInputIdxs / ...FromAbsoluteInputIdxs): Both offchain smart constructors gain a paramsRefIdx :: Integer argument (non-negative validated, §10.2) and populate the new final SeizeAct field.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-onchain/lib/SmartTokens/Contracts/ProgrammableLogicBase.hs @ lines 766-806 (phasCSHOrFalse + pfindReferenceInputByCS) and exports 13-25: After both consumers (1122, 1221) switch to indexed lookup, pfindReferenceInputByCS and phasCSHOrFalse become dead in this module — delete or leave for the SpendDirectory duplicate only (that module has its own copy). Export MintProof (..) from the module header; keep existing exports.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-onchain/lib/SmartTokens/Contracts/ProgrammableLogicBase.hs @ lines 714-727 (mkProgrammableLogicBase): NO code change (spec §11.5: indexed alternative measured and REJECTED — keep the withdrawal scan). Listed to prevent an over-eager 'optimization'; the coupling contract is that base spends require the global or seize withdrawal (§3.5).
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-onchain/lib/SmartTokens/Types/ProtocolParams.hs @ lines 44-121 (whole datum type): Append globalLogicCred, seizeLogicCred :: Credential to ProgrammableLogicGlobalParams (44-47); extend hand-written FromData (51-56) and ToData (58-62) to the 4-element plain list; add pglobalLogicCred/pseizeLogicCred :: PAsData PCredential to PProgrammableLogicGlobalParams (64-71, DeriveAsDataRec — appended so fields 0/1 stay byte-compatible); update ToJSON (87-92), FromJSON (94-98), ToParamSchema (100-118); ToSchema (120-121) follows.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-onchain/lib/SmartTokens/Contracts/ProtocolParams.hs @ lines 24-41 (mkProtocolParametersMinting) and 55-56 (alwaysFailScript): §4.1 hardening (genesis-only cost): keep existing 3 checks; add (a) nothing else minted under ownCS beyond the single NFT (pheadSingleton at 34 already pins one TN — confirm it excludes extra names), (b) the NFT output sits at the canonical alwaysFailScript address with NO stake credential and value exactly min-ada + the NFT (licenses runtime phasCSH), (c) inline datum fully decodes to the 4-field type, (d) canonical 28-byte CS, (e) each of the 3 credential fields is Constr-1 ScriptCredential with a 28-byte hash. Requires the anchor script hash as a new parameter or derivation (spec §12 step 1-2: compile anchor first). alwaysFailScript itself unchanged.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-onchain/lib/SmartTokens/LinkedList/SpendDirectory.hs @ lines 24-28, 48-50, 72-75: Private pfindReferenceInputByCS duplicate + PProgrammableLogicGlobalParams{pdirectoryNodeCS} record pmatches. No semantic change needed (reads only field 0, appended fields are backward compatible), but recompiles against the 4-field type — verify the DeriveAsDataRec pmatch stays lazy on fields 2/3 (bench).
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Env/Directory.hs @ lines 124-137 (DirectoryEnv) and 169-177 (globalParams/getGlobalParams): Add a dsProgrammableSeizeScript field (currently the seize script exists only inside Scripts.hs programmableLogicBaseScript, 94-96) and build the 4-field datum: globalLogicCred from dsProgrammableLogicGlobalScript hash (as ScriptCredential), seizeLogicCred from the seize script hash. §12: one public constructor, no caller-supplied hashes; golden test compares stored hashes vs exported scripts.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Scripts.hs @ lines 92-114: Expose programmableSeizeScript (already standalone at 106-109) through DirectoryEnv so datum creds and base params derive from the SAME artifacts; programmableLogicBaseScript (92-101) keeps its two-credential parameterization unchanged.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/ProgrammableLogic.hs @ lines 200-235 (transferProgrammableToken) and 237-249 (proof helpers): TransferAct construction (209-213): populate plgrParamsRefIdx with the post-balancing reference-input index of uIn paramsTxIn (added at 222; use findIndexReference against the final txBody, never default 0 — §10.2); mintProofs become MintProof values — Member (no index) for registered minted policies, NonMember coveringNodeIdx (via proofNodeForSymbol/proofNodeIndex) for unregistered; mintProofReferences (205) then only needs ref inputs for NonMember proofs.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/examples/regulated-stablecoin/lib/Wst/Offchain/BuildTx/ProgrammableLogic.hs @ lines 121-163 (seize builder): mkSeizeActRedeemerFromAbsoluteInputIdxs call (142-146) passes the new paramsRefIdx (params ref added at 155; compute via findIndexReference paramsTxIn txBody). Import list at 43 unchanged in name.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/ProtocolParams.hs @ lines 27-53 (mintProtocolParams): Datum comes from globalParams so the 4th/3rd fields flow through automatically, but the hardened policy now constrains the anchor output: address must be the canonical alwaysFailScript address (42-43 already NoStakeAddress at the spending script — confirm that script IS the canonical anchor), value exactly min-ada + NFT (35-36), inline datum (46). Keep builder and policy in lockstep or genesis fails.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/lib/ProgrammableTokens/Gen.hs @ lines 27-31 (genGlobalParams): Constructor gains two more credential fields; for fixtures that must satisfy the hardened mint policy, generate ScriptCredential-only values (§4.1 item 5 makes key creds unmintable).
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchainScripts.hs @ lines 186-188, 916 (params datum); 325, 367, 405, 501, 565, 611, 680, 1014, 1063, 1100, 1162, 1395, 1438 (TransferAct); 714, 774, 843 (seize redeemers); 1487-1514, 1534 (case wiring): ProgrammableLogicGlobalParams positional construction gains 2 credentials; every TransferAct gains paramsRefIdx + MintProof-typed mint proofs (1014/1063/1100 use non-empty mint proofs today — become [Member] or [NonMember i] per scenario); every mkSeizeActRedeemerFrom* call gains paramsRefIdx; all synthesized contexts must place the params UTxO at the declared ref-input index.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/test/ProgrammableTokens/Test/ProgrammableLogicGlobal.hs @ lines 540-542 (params datum); 140, 148, 163, 175, 190, 202, 214, 222, 230, 718 (TransferAct); 748, 774, 806, 829, 852, 881, 914, 964, 1001, 1039, 1077, 1108 (seize redeemers): Same mechanical updates as the benchmark exe: 4-field datum, TransferAct paramsRefIdx + [MintProof], seize constructors' new argument. Add the NEW golden encoding test pinning TransferAct/SeizeAct/MintProof Data layouts (spec §11.3 requires it; none exists today).
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/examples/regulated-stablecoin/exe/full-transaction-benchmarks/FullTransactionBenchmarks.hs @ lines 63, 1143: TransferAct import/construction gains paramsRefIdx and MintProof-typed plgrMintProofs.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/IssuanceCborHexRef.hs and /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Env/TransferLogic.hs and /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/examples/regulated-stablecoin/exe/export-smart-tokens/Main.hs @ IssuanceCborHexRef.hs:75, TransferLogic.hs:61, Main.hs:228 (NamedFieldPuns record matches); Query.hs:179, Wst/Client.hs:39, Wst/Server/Types.hs:144, BuildTx/Directory.hs:90 (type-only uses): Record-pun matches on {directoryNodeCS, progLogicCred} still compile with appended fields — verify only. Type-only signatures (Query/Client/Server/Directory) are transparent; JSON round-trip consumers pick up the new FromJSON/ToJSON shape (API payload change for the global-params endpoint).

Risks:
- Positional pattern arity: PTransferAct transferProofs mintProofs (ProgrammableLogicBase.hs:1112) and PTransferAct _ _ (1225) are positional pmatches; the compiler catches these, but raw-Data redeemers built in tests (PlutusTx.toBuiltinData $ TransferAct ...) will silently encode the OLD 2-field shape until every site is updated — the on-chain punsafeCoerce reads (1102, 1217) will not catch a stale encoding, they will misindex fields at runtime.
- Seize params-lookup relocation changes evaluation order: today the scan (1218-1222) runs before the redeemer pmatch, so even the TransferAct-rejection path forces reference inputs. Moving it into PSeizeAct is a behavior change (benign — cheaper rejection) but any test asserting the current 'params missing' failure before 'seize validator invoked with TransferAct' will flip.
- NonMember must keep node authentication (phasCSH, 965) and the covering-interval check — deleting the wrong side of the member/non-member pair inverts the §11.3 security argument (the ONLY escape-critical direction is a registered policy getting a NonMember proof). The Member deletions are safe precisely and only because containment then forces the claimed delta to base.
- paramsRefIdx must be VERIFIED at the index (phasCSH on the value) — an unverified pdropFast is a spoofable params source. Soundness of phasCSH-only auth depends on landing the §4.1 anchor hardening IN THE SAME CHANGE (one-shot uniqueness: no other UTxO can ever hold protocolParamsCS assets). Shipping the indexed lookup without the hardened mint policy is an ordering hazard across phases.
- DeriveAsDataRec pmatch on the 4-field datum: spec (lines 263-264) mandates that appending fields 2/3 MUST NOT force decoding them on the transfer/seize hot paths. If Plutarch's record pmatch strictly materializes all projections, switch the hot-path reads to raw punsafeCoerce field indexing (the §4.1-licensed idiom); this is a bench gate, not just a compile concern. Same question applies to SpendDirectory.hs:50/75.
- MintProof constructor indices are unpinned until the golden encoding test exists; Aiken's analogous proofs (TokenExists/TokenDoesNotExist, BenchmarkOnchainAikenScripts.hs:247-248) use a different shape — do not copy its indices by reflex. Freeze Member=0 nullary / NonMember=1 [I] in the golden test FIRST, then write builders against it.
- The hand-written FromData for ProgrammableLogicGlobalParams (Types/ProtocolParams.hs:51-56) does not validate list length; a 2-field datum still 'succeeds' decoding if only 2 heads are taken after the change is half-applied — extend it to consume all 4 fields so stale fixtures fail loudly offchain.
- Builders MUST recompute paramsRefIdx after final balancing (change-output/ref-input insertion invalidates indices, §10.2) and never fall back to 0; both existing builders currently rely on findIndex* helpers over the final body — keep the redeemer a txBody-function as done today (ProgrammableLogic.hs:209, Wst 142) rather than a precomputed constant.
- DirectoryEnv currently derives the seize script only inside programmableLogicBaseScript (Scripts.hs:94-96); building seizeLogicCred a second time in Env/Directory risks hash drift between the base's compiled-in credential and the datum's stored credential — derive once, thread everywhere, and pin with the §12 golden hash test.
- MEMORY.md notes tests/benchmarks need /usr/local/lib libsodium (VRF) via --extra-lib-dirs — build verification of this map's changes will fail without it.

Test touchpoints:
- NEW golden Data-encoding test pinning TransferAct (Constr 0, 3 fields), SeizeAct (Constr 1, 5 fields), MintProof (Member=Constr 0 [], NonMember=Constr 1 [I]) and the 4-field params datum (plain List, field order §4) — required by spec §11.3/§14; none exists today (grep 'golden' in src/programmable-tokens-test and src/examples is empty).
- src/programmable-tokens-test/test/ProgrammableTokens/Test/ProgrammableLogicGlobal.hs — the §3.5/§14.1 coupling suite: update all TransferAct sites (140-230, 718) and seize constructor sites (748-1108); add red tests: Member tag for an unregistered policy must still force tokens to base (self-penalizing, not an escape); NonMember for a registered policy must fail; wrong/unauthenticated paramsRefIdx must fail; SeizeAct routed to the global still fails (1196-1197); params datum with key credential / wrong-length hash unmintable (§14.1 items 1-2).
- src/programmable-tokens-test/exe/BenchmarkOnchainScripts.hs — §14.3 bench gates: TransferAct scenarios (1487-1500), seize scenarios (1501-1514), mainnet-shape tx (1534); re-baseline after Member-tag deletions (expected win: no ref-fetch/decode/auth/logic-scan per delegated minted policy) and after indexed params lookup (expected ~6.5M CPU / 18.7K mem at ref position 0 per spec §13); compare against Aiken ca7864f via BenchmarkOnchainAikenScripts.hs.
- src/programmable-tokens-test/lib/ProgrammableTokens/Gen.hs genGlobalParams (27-31) — 4-field generator; property tests of JSON/Data round-trips through Types/ProtocolParams.hs instances.
- Full-transaction integration: src/examples/regulated-stablecoin/exe/full-transaction-benchmarks/FullTransactionBenchmarks.hs (63, 1143) and the Wst end-to-end paths (issueSmartTokens/transferSmartTokens/seizeSmartTokens/multiSeizeSmartTokens in Wst/Offchain/BuildTx/TransferLogic.hs:230-341) exercising the updated builders against the updated validators.
- §12 derivation golden test: every hash stored in the 4-field params datum equals the hash of the script the environment exports (Env/Directory.hs constructor vs Scripts.hs artifacts), including the seize script now exposed in DirectoryEnv.
- mkProtocolParametersMinting hardening tests: params NFT to a spendable/wrong address, extra assets in the anchor output, missing/extra datum fields, key credential in a credential slot, non-28-byte hash — all MUST fail at mint time (§14.1 items 1-2); positive genesis mint via BuildTx/ProtocolParams.hs mintProtocolParams still succeeds.

### CIP-113 Plutarch issuance policy (SmartTokens.Contracts.Issuance) — full map of current checks, exports, importers, parameter-order constraint, and delete/add worklist for the 2-param dual-arm-custody rewrite (spec doc/design-issuance-dual-arm-custody.md §5-§9)

Key facts:
- CURRENT PARAMS (Issuance.hs:104-106): PAsData PCredential programmableLogicBase -> PAsData PCurrencySymbol directoryNodeCS -> PAsData PScriptHash mintingLogicCred' -> PScriptContext. Comment at Issuance.hs:100-103 pins WHY mintingLogicHash is LAST: the offchain issuance-cbor-hex derivation (issuerPrefixPostfixBytes, IssuanceCborHexRef.hs:41-59) compiles the template with all-but-last params applied, then applyArguments with a placeholder ScriptHash and splits the CBOR bytes around serialiseData(toData placeholder) (breakCborHexBS, IssuanceCborHexRef.hs:60-66). The directory policy reconstructs the policy id on-chain as blake2b_224([3] <> prefix <> serialiseData(hashedParam) <> postfix) via _pisProgrammableTokenRegistration in LinkedList/Common.hs:222-229 (test mirror: DirectoryMint.hs:149-157). Spec §5 (doc lines 291-314) keeps mintingLogicHash last for exactly this reason; the rewrite replaces the other two params with a single protocolParamsCS.
- CURRENT CHECKS, mint branch (ownNumMinted > 0, Issuance.hs:181-192): (1) purpose is PMintingScript, binds ownCS (110-111); (2) registration proof S2: a reference input OR output holds directory NFT TokenName(ownCS) under directoryNodeCS, qty==1, unindexed pany scan (125-136, applied at 188); (3) single token-name per policy: ptryLookupValue # ownCS + pheadSingleton (138-143) — also fails if ownCS absent from mint; (4) FIRST output holds exactly ownNumMinted of (ownCS, ownTokenName) (147, 185); (5) first output payment cred == programmableLogicBase (186); (6) first output has INLINE StakingHash stake cred — DNothing and pointer rejected (170-179, 187); (7) minting-logic invoked: unindexed pany over wdrl for ScriptCredential mintingLogicHash (157-160, 189); (8) redeemer data == mintingLogicCred credential (190); (9) logic-isolation scan psingleMintWithCredential: exactly one Minting-purpose redeemer in txInfo.redeemers equals own redeemer (191, impl 203-252, go/go1/go2 walk).
- CURRENT CHECKS, burn branch (ownNumMinted <= 0, Issuance.hs:194-201): registrationProven (197) + mintingLogicInvoked (198) + psingleMintWithCredential (199). Burn implicitly also requires ownCS in the mint map with exactly one token name via check (3) at 138-143 (bound before the branch).
- EXPORTS (Issuance.hs:7-10): mkProgrammableLogicMinting and SmartTokenMintingAction(..) only. PSmartTokenMintingAction (44-58) is internal and referenced nowhere else in the worktree. SmartTokenMintingAction is imported in exactly ONE place: ProgrammableTokens/OffChain/BuildTx/ProgrammableLogic.hs:35, constructed at :64, passed as mint redeemer at :91 and :99 (mintPlutus mintingScript mintingLogicCred an q).
- mkProgrammableLogicMinting import sites (complete, whole-worktree grep): (a) programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Scripts.hs:34, used in programmableLogicMintingScript :123-137 (applies baseCred + directoryNodeCS via Plutarch, mintingLogicHash via Data-level applyArguments); (b) programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/IssuanceCborHexRef.hs:33, used at :53 in issuerPrefixPostfixBytes (MUST mirror Scripts.hs application exactly — comment :47-50); (c) programmable-tokens-test/exe/BenchmarkOnchainScripts.hs:23, used at :115 (ssbProgrammableMintSpec :111-117) and mkCase rows :1517-1522; (d) programmable-tokens-test/test/ProgrammableTokens/Test/ProgrammableLogicMinting.hs:32, used at :103, args at :80-86; (e) examples/regulated-stablecoin/exe/export-smart-tokens/Main.hs:55, used at :147 (local stale issuerPrefixPostfixBytes :141-153) and unapplied exports :275, :289, :303 (rank-2 Term, no signature dependency).
- Derivation chain consuming the template: Env/TransferLogic.hs:59-64 programmableTokenMintingScript (DirectoryEnv+TransferLogicEnv -> applied script) is the single chokepoint used by BuildTx/ProgrammableLogic.hs:75, BuildTx/Directory.hs:53 (insertNodeArgs — inaHashedParam = minting-logic hash), Query.hs:96, regulated-stablecoin Wst/Offchain/Env.hs:228, examples/aiken Wst/Cli.hs:121, export-smart-tokens Main.hs:232. Its inputs today are globalParams (directoryNodeCS + progLogicCred, 2-field ProgrammableLogicGlobalParams); after the rewrite it needs protocolParamsPolicyId (already exists: Env/Directory.hs:163-164).
- REWRITE DELETES (per spec §5-§9): both non-last params (base cred, directoryNodeCS) -> replaced by protocolParamsCS; SmartTokenMintingAction/PSmartTokenMintingAction (36-58); redeemer==credential check (190); psingleMintWithCredential entirely (191, 199, 203-252 — §7.1: within one protocol instance an identical logic hash yields an identical policy id, so the scan protects nothing); first-output rule + inline-stake check (147, 165-179, 185-187 — replaced by Local universal full scan, stake check explicitly out of scope §2.2/§8); unindexed pany registration scan (125-136) and wdrl scan (157-160) -> indexed witnesses; ptryLookupValue/pheadSingleton mint-map read (138-143) — mint map read ONLY by BurnOnly afterwards; ownNumMinted branch dispatch (181) -> redeemer-constructor dispatch.
- REWRITE ADDS (§5-§9): MintRedeemer sum type — Local{mintingLogicWdrlIdx,paramsRefIdx,registration} idx 0, DelegateTransfer{+nodeRefIdx,globalWdrlIdx} idx 1, DelegateSeize{+nodeRefIdx,seizeRedeemerIdx} idx 2, BurnOnly{mintingLogicWdrlIdx} idx 3; RegistrationWitness — RegisteredByReferenceInput 0 / RegisteredByOutput 1; constructor indices/field order frozen by golden test; every index non-negative with EXPLICIT rejection (§6). Common path (§7): PMintingScript match; indexed wdrl check == hoisted ScriptCredential mintingLogicHash constant; params ref-input authenticated by first-non-Ada phasCSH # protocolParamsCS with raw-field datum access (4-field params datum from phase 1: directory/base/global/seize creds); registration = one directory NFT named ownCS under params directoryNodeCS at the witnessed position, NO datum decode. Local (§8): every output with payment cred != progLogicCred must hold zero ownCS (delta- and input-agnostic). DelegateTransfer (§9.1): nodeRefIdx NFT check + globalWdrlIdx withdrawal == params globalLogicCred, nothing else. DelegateSeize (§9.2): nodeRefIdx NFT check + at seizeRedeemerIdx a Rewarding purpose of params seizeLogicCred whose SeizeAct.directoryNodeIdx == nodeRefIdx. BurnOnly: ownCS mint lookup has no positive entry — its only check. Delegate witnesses have no output-registration variant by construction (§9.3).
- LATENT STALENESS FOUND (pre-existing, current tree): (1) BenchmarkOnchainScripts.hs:111-117 ssbProgrammableMintSpec applies only [progLogicBaseCred, mintingLogicHash, ctx] — missing directoryNodeCS vs the current 3-param signature (Data-level applyArguments hides it; the scenario cross-eval path via ScriptScenario.hs:96-97 dispatch on sseMintingPolicyCS would mis-evaluate); (2) export-smart-tokens Main.hs:141-153 issuerPrefixPostfixBytes applies only ONE param (# pconstant progLogicCred, :147) and splits on an 8-byte "deadbeefcafebabe" hex — stale vs both current and target signatures. Both must be fixed or deleted during the rewrite, not blindly ported.
- Things that do NOT change mechanically but must be re-verified: LinkedList/Common.hs pInsert :210-262 (splice mechanism and S1 authorization via hashedParam-as-wdrl-credential :240-247 unchanged; only the prefix/postfix DATUM BYTES change because the template embeds protocolParamsCS instead of baseCred+directoryNodeCS — deployed IssuanceCborHex datums must be re-derived); DirectoryMint tests use fake "0d"/"0e" prefixes (DirectoryMint.hs:138-140) so they are insensitive to real template bytes; BenchmarkOnchainAikenScripts.hs:118-123 is the Aiken baseline (applies its own 4 params) and stays untouched; TxD29Fixture.hs replays a captured tx (issuance policy not evaluated there).

Change sites:
- src/programmable-tokens-onchain/lib/SmartTokens/Contracts/Issuance.hs @ whole file (exports 7-10, redeemer types 36-58, policy 104-201, psingleMintWithCredential 203-252): Full rewrite: 2 params (PAsData PCurrencySymbol protocolParamsCS :--> PAsData PScriptHash mintingLogicHash, hash stays LAST) + new exported MintRedeemer/RegistrationWitness Haskell+Plutarch types (frozen constructor indices 0-3 / 0-1); delete SmartTokenMintingAction, PSmartTokenMintingAction, psingleMintWithCredential, first-output rule, inline-stake check, redeemer==credential check, all unindexed scans; add common path (PMintingScript, indexed wdrl check, params-anchor lookup, indexed registration) and the four arms Local / DelegateTransfer / DelegateSeize / BurnOnly with explicit negative-index rejection
- src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Scripts.hs @ import :34; programmableLogicMintingScript :123-137: Signature ScriptTarget -> C.PaymentCredential -> CurrencySymbol -> C.StakeCredential becomes ScriptTarget -> C.PolicyId(params) -> C.StakeCredential; Plutarch-apply only protocolParamsCS, keep applyArguments [toData mintingLogicHash] as the last Data-level application (CBOR split point); update the :125-127 comment
- src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/IssuanceCborHexRef.hs @ import :33; issuerPrefixPostfixBytes :38-59; caller mintIssuanceCborHexNFT :74-77: issuerPrefixPostfixBytes drops the (directoryNodeCS, progLogicCred) pair for a single protocolParamsCS argument; the compile at :53 applies only protocolParamsCS (must mirror Scripts.hs exactly, comment :47-50); caller at :74-76 stops destructuring globalParams and passes protocolParamsPolicyId from DirectoryEnv
- src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/ProgrammableLogic.hs @ import :35; redeemer build :64; mintPlutus calls :91, :99; register-vs-mint branch :86-101: Replace SmartTokenMintingAction credential redeemer with MintRedeemer construction per spec §10: already-registered mint -> Local + RegisteredByReferenceInput, register-and-mint -> Local + RegisteredByOutput, delegate arms when global/seize runs; requires computing mintingLogicWdrlIdx / paramsRefIdx / node & registration indices against the balanced tx (§10.2 index construction), which likely restructures the plain mintPlutus call
- src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Env/TransferLogic.hs @ programmableTokenMintingScript :59-64 (and programmableTokenPolicyId :67-69 unchanged consumer): Stop deriving from globalParams (directoryNodeCS + unTransCredential progLogicCred); pass protocolParamsPolicyId dirEnv (Env/Directory.hs:163-164) to the new programmableLogicMintingScript signature
- src/programmable-tokens-test/exe/BenchmarkOnchainScripts.hs @ import :23; ssbProgrammableMintSpec :111-117; mkCase rows :1517-1522; mint contexts programmableMintCtx :990, programmableBurnCtx :1008, programmableBurnRedeem10Ctx :1058, programmableMintTopUpCtx :1095, programmableMintBusyTxCtx :1199, programmableMintManyWithdrawalsCtx :1231; scriptRedeemer builds :992, :1010, :1060, :1097, :1201, :1233: Arg lists become [toData protocolParamsCS, toData mintingLogicHash, toData ctx] (note :111-117 is ALREADY stale — missing directoryNodeCS today; fix, don't port); every context gains a params reference input (NFT under sseProtocolParamsCS + 4-field datum) and swaps the ScriptCredential-as-redeemer for indexed MintRedeemer values; scenarioEnv :131-148 already carries sseProtocolParamsCS
- src/programmable-tokens-test/test/ProgrammableTokens/Test/ProgrammableLogicMinting.hs @ import :32; test list :41-45; mintArgs :80-86; mintingScript :103; mintRedeemer :159-160; contexts :166-252 (programmableMintCtx :166, Unregistered :185, NoStake :198, Escape :211, SplitEscape :214+): mintArgs shrinks to [protocolParamsCS, mintingLogicHash, ctx]; redeemer becomes indexed MintRedeemer data; every ctx gains the params reference input; existing five tests survive with reshaped contexts EXCEPT unit_mint_to_base_without_stake_cred_rejected (:44, :67-70) whose check the rewrite deletes (§2.2) — drop or invert it; §14.1/§14.2 suites are added here in phase 5
- src/examples/regulated-stablecoin/exe/export-smart-tokens/Main.hs @ import :55; stale local issuerPrefixPostfixBytes :141-153 (apply site :147); usage :228-231; unapplied exports :275, :289, :303: The local derivation is stale even today (1 param applied, 8-byte split token); either delete it in favor of the library issuerPrefixPostfixBytes or rewrite to compile with protocolParamsCS and split on the 28-byte placeholder; :275/:289/:303 writePlutusScript* calls need no change (rank-2 term)
- src/programmable-tokens-onchain/lib/SmartTokens/LinkedList/Common.hs @ pInsert :210-262 (splice :222-229, S1 authorization :240-247): NO code change — the blake2b_224(prefix <> serialiseData(scriptHash) <> postfix) reconstruction and hashedParam-as-wdrl-cred authorization are template-agnostic; verify only. The IssuanceCborHex DATUM contents change because the template bytes change (redeploy/re-derive)
- src/programmable-tokens-test/exe/BenchmarkOnchainAikenScripts.hs @ ssbProgrammableMintSpec :118-123: NO change (Aiken baseline applies its own aikenProgrammableLogicMintingScript params); keep row labels aligned so the §14.3 comparison tables still pair up
- src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Query.hs + BuildTx/Directory.hs + examples (Wst/Offchain/Env.hs:228, aiken Wst/Cli.hs:121) @ programmableTokenMintingScript consumers: Query.hs:96, Directory.hs:53, Env.hs:228, Cli.hs:121: No direct edits if Env/TransferLogic.hs keeps the DirectoryEnv -> TransferLogicEnv -> script shape; recompile-and-verify only. Directory.hs insertNodeArgs (:51-64) keeps inaHashedParam = minting-logic hash, which stays valid since the hash remains the last applied parameter

Risks:
- The two independently-maintained CBOR-split derivations must stay byte-identical with the on-chain application order: offchain Scripts.hs:129-132 vs IssuanceCborHexRef.hs:53-57 (comment :47-50 demands EXACT same pdata(pconstant ...) wrapping). Any divergence silently produces a different reconstructed policy id in LinkedList/Common.hs pInsert and registration fails on-chain only.
- Two pre-existing stale sites will be copied forward if unnoticed: BenchmarkOnchainScripts.hs:111-117 (missing directoryNodeCS arg today) and export-smart-tokens Main.hs:141-153 (1-param apply, 8-byte split token). applyArguments is untyped Data application, so neither fails at compile time — they fail (or mis-measure) only at eval time.
- Deleting psingleMintWithCredential and the first-output rule removes the current restrictions memoized as 'Issuance first-output rule forbids one-tx multi-recipient mint'; the Local full scan deliberately permits multi-recipient base outputs. Any offchain code or test that relied on first-output placement (e.g. prependTxOut ordering assumptions in builders) must be re-audited rather than assumed.
- The rewrite depends on the phase-1 4-field params datum (directory/base/global/seize creds) and its §4.1 anchor hardening: Issuance raw-accesses datum fields with no shape validation, so field ORDER in Types/ProtocolParams.hs becomes consensus-critical and must be frozen by the same golden tests as the redeemer encoding.
- unit_mint_to_base_without_stake_cred_rejected (test :44) guards Item 4 (permanently-locked no-stake outputs). The spec explicitly drops that check (§2.2, §8); deleting the test is intentional scope, but the offchain builder (programmableTokenReceivingAddress path) must still always attach a stake credential (§10.6) or funds lock.
- SmartTokenMintingAction is re-exported nowhere else, but its ToData encoding (bare Credential) is the CURRENT on-chain redeemer; any deployed instance or captured fixture replaying old redeemers (TxD29-style) must not be migrated to the new encoding — old and new templates are distinct policies by design (§7.1 removed coexistence from scope).
- Negative/out-of-bounds index handling must be explicit in the new code (§6): relying on pdropFast budget exhaustion is spec-noncompliant and a red-suite target (§14.1).

Test touchpoints:
- src/programmable-tokens-test/test/ProgrammableTokens/Test/ProgrammableLogicMinting.hs — 5 existing unit tests (:41-45) all reshaped: mintArgs (:80-86) to 2 params, contexts (:166-252) gain params ref input + indexed redeemers; drop/replace the no-stake test; phase-5 adds the §14.1 malicious-shape and §14.2 functionality suites here
- src/programmable-tokens-test/test/ProgrammableTokens/Test/DirectoryMint.hs — insensitive to real template bytes (fake prefix/postfix :138-140) but exercises the reconstruction formula (:149-157) and S1 wdrl authorization; keep green as the coupling pin for the unchanged splice mechanism
- src/programmable-tokens-test/exe/BenchmarkOnchainScripts.hs — mkCase rows :1517-1522 and all six programmableMint/Burn contexts; §14.3 gates run these against BenchmarkOnchainAikenScripts.hs rows (Aiken ca7864f baseline, rows :118-123) so names must stay paired
- Golden encoding test (NEW, per §6): freeze MintRedeemer constructor indices 0-3, RegistrationWitness 0-1, and field order; same treatment for the 4-field params datum the policy raw-accesses
- Build/eval prerequisite: tests and benchmarks need /usr/local/lib libsodium (VRF) — pass --extra-lib-dirs (memory: wsc-poc-build-env)
- Integration path: programmable-tokens-test/lib/ProgrammableTokens/Test.hs deployment flow (:199, :249) exercises deployIssuanceCborHex + issueProgrammableToken end-to-end and will catch index-construction bugs in the reworked BuildTx/ProgrammableLogic.hs builder

### Offchain script-derivation + env pipeline (spec §12, with §5/§11.6 inputs) for the CIP-113 Plutarch implementation in src/programmable-tokens-offchain, plus its genesis builders and downstream consumers

Key facts:
- CURRENT DERIVATION DAG (mkDirectoryEnv, /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Env/Directory.hs:128-150; roots srTxIn, srIssuanceCborHexTxIn, srTarget from DirectoryScriptRoot :76-85): (1) dsProtocolParamsMintingScript = mkProtocolParametersMinting # TxOutRef srTxIn (Scripts.hs:48-51) -> protocolParamsPolicyId (Env/Directory.hs:163-164); (2) dsProtocolParamsSpendingScript = alwaysFailScript # "" nonce (Scripts.hs:55-58); (3) dsIssuanceCborHexMintingScript = mkIssuanceCborHexMinting # TxOutRef srIssuanceCborHexTxIn (Scripts.hs:62-65) -> issuanceCborHexPolicyId (Env/Directory.hs:166-167); (4) dsIssuanceCborHexSpendingScript = alwaysFailScript # "deadbeef" (Scripts.hs:68-71); (5) dsDirectoryMintingScript = mkDirectoryNodeMP # TxOutRef srTxIn # issuanceCS-from-(3) (Scripts.hs:75-79) -> directoryNodePolicyId (Env/Directory.hs:160-161); (6) dsDirectorySpendingScript = pmkDirectorySpending # paramsPolicyId-from-(1) (Scripts.hs:83-86); (7) dsProgrammableLogicGlobalScript = mkProgrammableLogicGlobal # paramsPolicyId (Scripts.hs:111-114) -> programmableLogicStakeCredential (Env/Directory.hs:152-154); (8) TRANSIENT seize = mkProgrammableSeize # paramsPolicyId compiled INSIDE programmableLogicBaseScript (Scripts.hs:94-96 via :106-109) and NOT stored in DirectoryEnv; (9) dsProgrammableLogicBaseScript = mkProgrammableLogicBase # globalStakeCred-from-(7) # seizeCred-from-(8) (Scripts.hs:92-101) -> programmableLogicBaseCredential (Env/Directory.hs:156-158). Laziness knot: fields 6,7,9 read `result` (Env/Directory.hs:135-138).
- PER-TOKEN DERIVATION: programmableTokenMintingScript dirEnv tle (Env/TransferLogic.hs:59-64) reads globalParams' directoryNodeCS+progLogicCred and calls programmableLogicMintingScript srTarget progLogicCred directoryNodeCS mintingCred (Scripts.hs:123-137), which Plutarch-applies params 1-2 (base cred, directoryNodeCS) via Scripts.tryCompile Production — NOTE `_target` is IGNORED, hardcoded Production (Scripts.hs:124,128) — then applies param 3 (mintingLogicHash) via Plutarch.Evaluate.applyArguments at the UPLC level (Scripts.hs:132) so it stays the last-applied argument. Consumers of this per-token script: BuildTx/ProgrammableLogic.hs:75-77 (issueProgrammableToken), BuildTx/Directory.hs:53-54 (insertNodeArgs), Query.hs:96-98 (registryNode), Env/TransferLogic.hs:67-69 (programmableTokenPolicyId), examples/regulated-stablecoin/lib/Wst/Offchain/Env.hs:228, exe/export-smart-tokens/Main.hs:232, examples/aiken/haskell/lib/Wst/Cli.hs:121.
- CBOR SPLIT MECHANICS: issuerPrefixPostfixBytes directoryNodeCS progLogicCred (BuildTx/IssuanceCborHexRef.hs:41-56) compiles the SAME two-applied base with `compile NoTracing` (:52 — must mirror programmableLogicMintingScript exactly, comment :47-50; NoTracing == tryCompile Production per onchain Core/Scripts.hs:50), applies a placeholder 28-byte ScriptHash 0xdeadbeef... via applyArguments (:44,:55), serialises the UPLC, and breakCborHexBS (:58-63) splits those bytes on the first occurrence of serialiseData(toData placeholder) (= 0x581c + 28 placeholder bytes, :45), yielding (prefix, postfix). These are stored as the IssuanceCborHex inline datum (prefix/postfix, onchain type at SmartTokens/Contracts/IssuanceCborHex.hs:28-45) by mintIssuanceCborHexNFT (IssuanceCborHexRef.hs:70-99, datum built :75-77). Onchain the directory pInsert (LinkedList/Common.hs:210-228) fetches that ref UTxO by issuanceCborHexCS and _papplyHashedParameter (Common.hs:306-322) recomputes blake2b_224(0x03 <> prefix <> serialiseData(hashedParam) <> postfix) == key-to-insert (Common.hs:303-304).
- WHAT CHANGES AT 3->2 APPLIED PARAMS (spec §5, doc/design-issuance-dual-arm-custody.md:291-313): mkProgrammableLogicMinting goes from `PAsData PCredential :--> PAsData PCurrencySymbol :--> PAsData PScriptHash :--> ...` (Issuance.hs:104) to `PAsData PCurrencySymbol (protocolParamsCS) :--> PAsData PScriptHash :--> ...`. The split MECHANISM is unchanged (still applyArguments the hash last + breakSubstring), but the compiled base now Plutarch-applies ONLY protocolParamsCS. issuerPrefixPostfixBytes' signature collapses to one argument (protocolParamsCS instead of directoryNodeCS+progLogicCred), so the IssuanceCborHex datum's dependency edge moves from {directoryNodeCS(5), progLogicCred(9)} to {protocolParamsPolicyId(1)} only — prefix/postfix become computable at §12 step 2, before base/directory exist. Call sites: mintIssuanceCborHexNFT :75-76 switches from globalParams fields to transPolicyId(protocolParamsPolicyId dir); programmableTokenMintingScript (Env/TransferLogic.hs:59-64) likewise.
- GENESIS PARAMS-DATUM VALUE: built by globalParams (Env/Directory.hs:169-174) = ProgrammableLogicGlobalParams{directoryNodeCS = directoryNodePolicyId, progLogicCred = base payment cred} (2 fields, type at src/programmable-tokens-onchain/lib/SmartTokens/Types/ProtocolParams.hs:44-47; hand-rolled LIST-encoded To/FromData :51-62 — NOT Constr — so appending fields 2/3 keeps raw index access to fields 0/1 byte-compatible per spec §4). Deposited on-chain by mintProtocolParams (BuildTx/ProtocolParams.hs:27-53; inline datum at :46; NFT-only value :35-36; alwaysFail anchor address, NoStakeAddress :38-43) inside deployCip143RegistryTx (Endpoints.hs:65-83) which does mintProtocolParams >> initDirectorySet >> registerProgrammableGlobalScript >> mintIssuanceCborHexNFT in ONE tx, balanced by balanceDeployTxEnv_ (Env/Directory.hs:189-200, excludes srIssuanceCborHexTxIn, setMinAdaDepositAll :194). New fields per §4 (doc:226-231): globalLogicCred = ScriptCredential(hash dsProgrammableLogicGlobalScript), seizeLogicCred = ScriptCredential(hash of seize script) — the seize script is currently NOT stored in DirectoryEnv, forcing a new dsProgrammableSeizeScript field. Runtime equality guard glParams == uDatum paramsTxOut at BuildTx/ProgrammableLogic.hs:71 will compare 4-field values.
- SINGLE PUBLIC CONSTRUCTOR (spec §12, doc:628-647): mkDirectoryEnv (Env/Directory.hs:128-150) is the natural candidate — it already takes only DirectoryScriptRoot with no caller-supplied hashes. It replaces/absorbs: (a) the ad-hoc transient seize compile inside programmableLogicBaseScript (Scripts.hs:94-96) — seize must become a stored env artifact so the params datum's seizeLogicCred is the hash of the exact exported script (§12 golden-test requirement, doc:645-647); (b) globalParams assembly (Env/Directory.hs:169-174) extended to 4 fields sourced ONLY from stored scripts; (c) re-ordering to the §12 acyclic order: anchor(2) -> protocolParamsCS(1, now also parameterized by anchor hash per §12 step 2 + §4.1) -> global(7)+seize(8) -> base(9) -> directoryNodeCS(5) -> 4-field datum -> issuance template. Raw DirectoryEnv(..) record is currently fully exported (Env.hs:11-23; re-exported by examples/regulated-stablecoin/lib/Wst/Offchain/Env.hs:73-85), letting callers build inconsistent envs; §12 argues for constructor-only export. mkDirectoryEnv callers: Endpoints.hs:77, programmable-tokens-test/lib/ProgrammableTokens/Test.hs:190, test/ProgrammableTokens/Test/DirectorySet.hs:45, examples .../Wst/Server/DemoEnvironment.hs:133, exe/calculate-hashes/Main.hs:26, exe/export-smart-tokens/Main.hs:201, Wst/Offchain/Env.hs:160,173.
- PARAMS ANCHOR POLICY: current mkProtocolParametersMinting (onchain Contracts/ProtocolParams.hs:24-42) checks only single-NFT + token name + one-shot input; §4.1 (doc:266-289) adds anchor-address/value/datum-shape/credential-constructor checks, which requires the anchor script hash as a new compile-time argument -> protocolParamsMintingScript (Scripts.hs:48-51) gains an argument and its derivation must run AFTER protocolParamsSpendingScript (currently order-independent; the Aiken baseline already does exactly this two-arg shape, AikenProgrammableTokenScripts.hs:50-56).
- REDEEMER INDEXES (spec §6, doc:315-371) touch the pipeline: every positive mint needs paramsRefIdx, but issueProgrammableToken's RefInput branch (BuildTx/ProgrammableLogic.hs:82-89) currently adds ONLY the node as reference — the params UTxO is referenced only on the insert path via insertDirectoryNode (BuildTx/Directory.hs:132-133). Index-against-final-txbody machinery already exists: proofNodeIndex/addReferencesWithTxBody pattern (BuildTx/ProgrammableLogic.hs:245-260).
- AIKEN BASELINE PIPELINE is a PARALLEL, embedded-hex derivation in the same offchain lib (AikenProgrammableTokenScripts.hs: applyDataArguments :111-119, aikenProgrammableLogicMintingScript with FOUR applied params :100-109, its own issuanceCborHexMintingScript blob :153-155) — it mirrors Aiken ca7864f and must NOT be migrated; it shares symbol names with Scripts.hs so imports must stay qualified.
- QUERY LAYER passes datum types through unchanged mechanics: issuanceCborHexUTxO (Query.hs:169-175, finds by dsIssuanceCborHexSpendingScript cred + issuanceCborHexPolicyId NFT), globalParamsNode (Query.hs:179-185, by dsProtocolParamsSpendingScript cred + protocolParamsPolicyId NFT) — both compile against the new 4-field type transparently. getCborHexInline (IssuanceCborHexRef.hs:123-127) decodes ProgrammableLogicGlobalParams from what is actually the IssuanceCborHex output — pre-existing wrong-type latent bug.

Change sites:
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Scripts.hs @ programmableLogicMintingScript, lines 123-137 (and export line 12, comment 125-127): Change signature from (ScriptTarget, PaymentCredential progLogicBase, CurrencySymbol directoryNodeCS, StakeCredential mintingCred) to (ScriptTarget, PolicyId protocolParamsCS, StakeCredential mintingCred); Plutarch-apply only protocolParamsCS; keep mintingLogicHash as the sole applyArguments (CBOR-level, last) argument; resolve the ignored `_target` (must stay mirror-identical with IssuanceCborHexRef's compile config)
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Scripts.hs @ protocolParamsMintingScript, lines 48-51: Add the anchor (alwaysFail params spending script) hash as a second applied argument per spec §4.1/§12 step 2 ('one-shot input AND anchor hash'); derivation must now follow protocolParamsSpendingScript
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Scripts.hs @ programmableLogicBaseScript, lines 92-101: Stop transiently re-compiling the seize script inside (lines 94-96); take the seize credential/script from the env-stored artifact (or keep re-derivation and add the §12 golden test that stored hash == embedded cred); update the doc comment
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Env/Directory.hs @ DirectoryEnv record, lines 115-126: Add dsProgrammableSeizeScript :: PlutusScript PlutusScriptV3 (needed to build seizeLogicCred in the 4-field params datum and for §12 golden hash tests)
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Env/Directory.hs @ mkDirectoryEnv, lines 128-150: Become the §12 single public constructor: derive in the acyclic order anchor -> protocolParamsCS -> global+seize -> base -> directoryNodeCS -> 4-field datum -> issuance template; compile and store the seize script once; pass anchor hash into protocolParamsMintingScript; never accept caller hashes (consider no longer exporting the raw DirectoryEnv constructor via Env.hs:11)
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Env/Directory.hs @ globalParams, lines 169-174 (plus new credential accessors near 152-167): Extend to the 4-field ProgrammableLogicGlobalParams: append globalLogicCred = ScriptCredential(hash dsProgrammableLogicGlobalScript) and seizeLogicCred = ScriptCredential(hash dsProgrammableSeizeScript); fields 0/1 unchanged and in the same order
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Env/TransferLogic.hs @ programmableTokenMintingScript, lines 59-64: Stop reading globalParams' directoryNodeCS/progLogicCred; call the new programmableLogicMintingScript with transPolicyId(protocolParamsPolicyId dirEnv) + mintingCred
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/IssuanceCborHexRef.hs @ issuerPrefixPostfixBytes, lines 41-56: Signature becomes CurrencySymbol (protocolParamsCS) -> (ByteString, ByteString); line 52 compiles mkProgrammableLogicMinting # pdata (pconstant protocolParamsCS) only; placeholder/split logic (44-45, 55, breakCborHexBS 58-63) unchanged; keep the mirror-compile comment (38-40, 47-50) accurate
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/IssuanceCborHexRef.hs @ mintIssuanceCborHexNFT, lines 70-99 (esp. 75-77): Replace globalParams-field extraction with protocolParamsPolicyId; datum construction IssuanceCborHex prefix postfix unchanged
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/IssuanceCborHexRef.hs @ getCborHexInline, lines 123-127: Fix wrong decode type (decodes ProgrammableLogicGlobalParams from the IssuanceCborHex output) — should decode IssuanceCborHex or be removed
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/ProtocolParams.hs @ mintProtocolParams, lines 27-53: 4-field datum flows through `params` (line 30/46) automatically once globalParams changes; verify §4.1 output shape survives balancing: exactly min-ada + single NFT (35-36), NoStakeAddress anchor (38-43)
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-onchain/lib/SmartTokens/Types/ProtocolParams.hs @ lines 44-121 (Haskell type 44-47, FromData 51-56, ToData 58-62, PProgrammableLogicGlobalParams 64-74, ToJSON 87-92, FromJSON 94-98, ToParamSchema/ToSchema 100-121): Append globalLogicCred and seizeLogicCred as fields 2/3 in every representation; the hand-rolled List (not Constr) To/FromData must append only — fields 0/1 byte positions frozen (spec §4)
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/ProgrammableLogic.hs @ issueProgrammableToken, lines 57-98: Params equality guard (line 71) now compares 4-field values; the RefInput branch (82-89) must additionally addReference the params UTxO (paramsTxOut is already an argument but only used for the guard and insert path) and compute paramsRefIdx/nodeRefIdx/registration indexes for the new MintRedeemer, replacing the bare SmartTokenMintingAction redeemer (63-64, 85, 95); reuse the proofNodeIndex/addReferencesWithTxBody final-txbody pattern (245-260)
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/Directory.hs @ insertDirectoryNode, lines 94-137: Registration path keeps params+cborhex references (132-133); reconstruction target is the NEW 2-param issuance template (prefix now bakes protocolParamsCS) — no structural change here, but the mint redeemer InsertDirectoryNode key/hashedParam pairing must match the new template-derived policy id
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Endpoints.hs @ deployCip143RegistryTx, lines 65-83; registerCip143PolicyTx, lines 106-115: Genesis tx sequencing under the new order; decide whether the standalone seize validator's stake credential must also be registered at genesis (currently only the global via registerProgrammableGlobalScript, line 81); registerCip143PolicyTx redeemer plumbing changes with the new MintRedeemer
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Env.hs @ lines 11-23: Re-export surface: add any new accessors (seize script/credentials); consider narrowing DirectoryEnv (..) to enforce the single-constructor rule
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/lib/ProgrammableTokens/Gen.hs @ genGlobalParams, lines 27-32: Generate 4-field params (two extra ScriptCredential-only fields per §4.1)
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/lib/ProgrammableTokens/Test.hs @ lines 158-199 (script-root deploy + reference-script attach), 248-249: Compiles against new env; dsIssuanceCborHexSpendingScript usage at 199 unchanged; any DirectoryScriptRoot construction unchanged in shape
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/examples/regulated-stablecoin/lib/Wst/Offchain/Env.hs @ lines 17-26, 73-85, 159-173, 228: Downstream re-exports of mkDirectoryEnv/globalParams/programmableTokenMintingScript and a direct policy-id derivation at 228 — recompile/adjust to new signatures
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/examples/regulated-stablecoin/exe/export-smart-tokens/Main.hs @ lines 194-232 (dirRoot/globalParams/programmableMinting) and 284-313 (script JSON exports): Export set gains/updates the 2-param issuance template and hardened params policy; globalParams destructuring at 228 gains fields
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/examples/regulated-stablecoin/lib/Wst/Server/DemoEnvironment.hs @ mkDemoEnv, lines 130-137: DirectoryScriptRoot construction — unchanged shape, recompile; pinned demo hashes/addresses all change (full redeploy accepted per §12)
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/examples/regulated-stablecoin/exe/calculate-hashes/Main.hs @ lines 14-26, 43: Hash-listing exe recompiles; output values all change
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/examples/regulated-stablecoin/lib/Wst/Offchain/Endpoints/Deployment.hs @ deployFullTx lines 48-60; globalParamsNode/issuanceCborHexUTxO uses at 99-100, 160, 209, 246: Same genesis sequencing concerns as Endpoints.hs deployCip143RegistryTx; datum type change flows through queries
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Query.hs @ registryNode 89-105, issuanceCborHexUTxO 169-175, globalParamsNode 179-185: No mechanical change, but recompiles against new per-token derivation and 4-field datum; policy-id derivation at 96-98 follows programmableTokenMintingScript
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/generated/openapi/schema.json and /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/frontend/src/app/store/schema.json @ ProgrammableLogicGlobalParams schema entries: Regenerate after the ToJSON/ToParamSchema change (Types/ProtocolParams.hs:87-121)
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/AikenProgrammableTokenScripts.hs @ whole module (esp. 100-109, 153-159): DO NOT migrate — frozen Aiken ca7864f comparison baseline with its own 4-param issuance and embedded issuanceCborHexMintingScript hex; only guard against unqualified name collisions with Scripts.hs

Risks:
- Mirror-compile invariant is the sharpest edge: issuerPrefixPostfixBytes uses `compile NoTracing` (IssuanceCborHexRef.hs:52) while programmableLogicMintingScript uses `Scripts.tryCompile Production` with its ScriptTarget argument silently ignored (Scripts.hs:124,128). Today NoTracing == tryCompile Production (onchain Core/Scripts.hs:50), but any divergence (e.g. honoring target=Debug in one place) makes onchain _papplyHashedParameter (Common.hs:306-322) reconstruct a different policy id and registration fails onchain with no offchain error until submission.
- The seize script is compiled twice today (transiently in Scripts.hs:94-96 and standalone via programmableSeizeScript:106-109); if the 4-field datum's seizeLogicCred is derived by a third compile path, stored-hash vs base-embedded-cred drift is possible — exactly what §12's golden tests (doc:645-647) must pin; store the script once in DirectoryEnv.
- §4.1 anchor hardening adds a datum-shape check to the minting policy while the datum itself contains the global/seize credentials whose scripts are parameterized by protocolParamsCS (the minting policy's own hash) — the policy MUST validate only structure (constructor 1 + 28-byte length, doc:278-280), never concrete values, or the derivation becomes circular.
- paramsRefIdx (and node/registration indexes) must be computed against the FINAL balanced tx body; reference inputs are nubbed and merged by addReferencesWithTxBody (BuildTx/ProgrammableLogic.hs:258-260) and balancing can append more — indexes computed eagerly will be wrong; must use the TxBuilder-closure pattern (proofNodeIndex, :245-249).
- deployCip143RegistryTx mints the params NFT and cborhex NFT in one tx (Endpoints.hs:79-82); §4.1 item 3 requires the params anchor output be exactly min-ada + the NFT — setMinAdaDepositAll (Env/Directory.hs:194) plus prependTxOut ordering must keep the two anchor outputs' values disjoint or the hardened policy rejects genesis.
- ProgrammableLogicGlobalParams uses LIST Data encoding (BI.mkList, Types/ProtocolParams.hs:58-62), not Constr — appending fields keeps 0/1 byte-compatible, but any 'tidy-up' to Constr or field reorder breaks the deployed-datum compatibility requirement of spec §4 and every audited raw field access in the global.
- Whether the standalone seize validator's stake credential needs genesis registration is unresolved: only the global is registered (Endpoints.hs:81, registerProgrammableGlobalScript); DelegateSeize requires the seize withdrawal/redeemer to exist, so a missing registration surfaces only at first seize attempt.
- getCborHexInline (IssuanceCborHexRef.hs:123-127) decodes the wrong type today; if any new code copies it for the params or cborhex UTxO it inherits the bug (related to the memory-noted 'buildIssue latent missing-dir-ref bug' area).
- Name collisions between Scripts.hs and AikenProgrammableTokenScripts.hs (issuanceCborHexMintingScript, protocolParamsMintingScript, etc.) — an unqualified import swap would benchmark or deploy the wrong implementation and typecheck fine.
- issueProgrammableToken's `error "Global params do not match"` (BuildTx/ProgrammableLogic.hs:71-73) will fire on any env/datum version skew during the atomic migration (old deployment JSON + new code), which is a hard crash, not a typed error.

Test touchpoints:
- src/programmable-tokens-test/lib/ProgrammableTokens/Test.hs:158-199,248-249 — deploy + reference-script attach flows; the whole emulator suite goes through mkDirectoryEnv
- src/programmable-tokens-test/lib/ProgrammableTokens/Gen.hs:27-32 — genGlobalParams must produce 4 fields (ScriptCredential-only per §4.1) for round-trip/serialization tests
- src/programmable-tokens-test/test/ProgrammableTokens/Test/DirectorySet.hs:45 — direct mkDirectoryEnv use
- src/programmable-tokens-test/test/ProgrammableTokens/Test/DirectoryMint.hs — issuance-cborhex registration reconstruction against the NEW 2-param template (prefix now bakes protocolParamsCS)
- src/programmable-tokens-test/test/ProgrammableTokens/Test/ProgrammableLogicGlobal.hs:396-399 — standalone seize compile; params-datum fixtures used in ctx builders
- NEW golden tests required by spec: §6 redeemer/constructor-index encoding freeze (doc:353-362), §11.3 TransferAct/SeizeAct layout freeze (doc:601-603), §12 stored-hash vs exported-script equality (doc:645-647), and 4-field datum byte-compat (fields 0/1 prefix identical to 2-field encoding)
- src/programmable-tokens-test/exe/BenchmarkOnchainScripts.hs (issuance + seize cases, e.g. 1501-1514) and exe/BenchmarkOnchain/ScriptRunner.hs + BenchmarkOnchainAikenScripts.hs — §14.3 gates; Plutarch side re-derives with 2-param template, Aiken side stays frozen at ca7864f
- src/examples/regulated-stablecoin/test/unit/Wst/Test/UnitTest.hs and exe/full-transaction-benchmarks/FullTransactionBenchmarks.hs — downstream E2E flows over the new env
- export-smart-tokens exe output (examples/regulated-stablecoin/exe/export-smart-tokens/Main.hs:284-313) — exported script JSON fixtures change wholesale; openapi/frontend schema.json regeneration
- Build note: tests/benchmarks need /usr/local/lib libsodium via --extra-lib-dirs (memory: wsc-poc build env)

### Test infrastructure map for implementing spec §14.1 (security red suite) / §14.2 (functionality suite) of doc/design-issuance-dual-arm-custody.md in src/programmable-tokens-test (worktree /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d)

Key facts:
- HOW SCRIPTS ARE DRIVEN — tier 1 (raw eval, no ledger): tests compile Plutarch terms with `compile NoTracing` and run `evalScript (applyArguments script [toData param.., toData ctx])`, asserting isRight/isLeft. Pattern at src/programmable-tokens-test/test/ProgrammableTokens/Test/ProgrammableLogicGlobal.hs:386-409 (globalScript:443, seizeScript:399, compileNoTracing:438-440), ProgrammableLogicMinting.hs:88-103, DirectoryMint.hs:72-94. `compileNoTracing` is DUPLICATED in each test module and again in exe/BenchmarkOnchain/Compile.hs:7-9 — not in the testlib.
- HOW SCRIPTS ARE DRIVEN — tier 2 (convex emulator): `mockchainSucceedsWithTarget` (lib/ProgrammableTokens/Test.hs:124-126) runs `MockchainT C.ConwayEra IO` with `nodeParamsFor` (Test.hs:100-110; Debug target = 10x tx size + ex-units, Production = real defaults). It drives the REAL offchain builders: deployDirectorySet (Test.hs:145-170 -> Endpoints.frackUtxosTx + Endpoints.deployCip143RegistryTx, offchain Endpoints.hs:37-65), attachProgrammableLogicReferenceScripts (Test.hs:172-221), issueProgrammableTokenTx (Test.hs:224-256 -> Query.registryNodeForReferenceOrInsertion/globalParamsNode/issuanceCborHexUTxO at offchain Query.hs:85,169-180, BuildTx.issueProgrammableToken at offchain BuildTx/ProgrammableLogic.hs:57-99, invokeMintingStakeScript at :115).
- HOW SCRIPTS ARE DRIVEN — tier 3 (benchmarks, §14.3): exe/BenchmarkOnchainScripts.hs (Plutarch) and exe/BenchmarkOnchainAikenScripts.hs (Aiken baseline via ProgrammableTokens.OffChain.AikenProgrammableTokenScripts blueprint loaders, offchain lib AikenProgrammableTokenScripts.hs:2-11) both call ScriptRunner.runScriptBenchmark (exe/BenchmarkOnchain/ScriptRunner.hs:83-101), which evaluates the primary EvalSpec PLUS every co-witness enumerated from the SAME ScriptContext by Scenario.scenarioEvalSpecsFromCtx (exe/BenchmarkOnchain/ScriptScenario.hs:51+, backend record ScriptScenarioBackend :37-48), under the true production budget via evalScript' productionMaxTxExBudget (ScriptRunner.hs:130-144; productionMaxTxExBudget defined Test.hs:112-122).
- SCRIPTCONTEXT BUILDER (the fixture engine): lib/ProgrammableTokens/Test/ScriptContext/Builder.hs — monoid of state transformers (ScriptContextBuilder :318-358). Ledger-realism features that keep raw-eval fixtures ledger-valid: normalizeValue canonical value ordering (:89-96), inputs/refInputs insertion-sorted by TxOutRef (:107-117, withInput :384, withScriptInput :398, withReferenceInput :411), pruneZeroValue (:271-279), buildBalancedScriptContext adds a balancing change output AND ledger-sorts the redeemer map Spending<Minting<Certifying<Rewarding (:504-536); plain buildScriptContext (:480-502) reverses list order and does NOT balance (used by DirectoryMint). Key combinators for the new suite: withMintingScript :416, withSpendingScript :425, withRewardingScript :437, withRewardingScriptWithBuilder :449 (redeemer computed from final builder state — useful for index-based Dual-Arm redeemers), withRewardingScriptWitness :462, withWithdrawal :472, withSigner :366, withReferenceScript :168, withTxOutInlineDatum :215, builderPlaceHolderTxOutRef :140.
- THE 42 CURRENT TESTS: Spec.hs (test/Spec.hs:12-21) aggregates 4 groups. (a) DirectoryMint.hs:57-58 — 2 tests (S1 registration authorization red/green). (b) DirectorySet.hs:26-39 — 4 mockchain tests (deploy + issue, x Debug/Production). (c) ProgrammableLogicGlobal.hs:65-95 — 31 tests (29 unit + 2 QuickCheck properties prop_seizeAct_* :354-368) covering seize correspondence, transfer containment, mint proofs, registry-spend R-01, poutputsContainExpectedValueAtCred helper (driven via a bespoke wrapper script mkOutputsContainScript :445-460). (d) ProgrammableLogicMinting.hs:41-46 — 5 issuance-policy tests.
- RED (must-FAIL) EXPRESSION TODAY: raw-eval tier — assertScriptFails/scriptFails=isLeft (Global.hs:374-376,391-394; Minting.hs:76-78,93-96), assertSeizeFails (Global.hs:415-417), assertOutputsContainFails (Global.hs:382-384), insertFails (DirectoryMint.hs:77-80), property-form seizeFails===True (Global.hs:362-368). Mockchain tier — assertFailingTx (Test.hs:93-98) asserts the tx's script-validity flag is C.ScriptInvalid (requires submitting with SubmitFailingTx); expectLeft (Test.hs:85-88). Neither mockchain red helper is currently used inside programmable-tokens-test; the only user is the example suite src/examples/regulated-stablecoin/test/unit/Wst/Test/UnitTest.hs:73.
- CURRENT ISSUANCE ENCODING IN FIXTURES: policy = mkProgrammableLogicMinting with 3 applied params, arg order [progLogicBaseCred, directoryNodeCS, mintingLogicHash, ctx] (Minting.hs mintArgs :80-86; bench BenchmarkOnchainScripts.hs:1517-1522; onchain signature Issuance.hs:104). Redeemer = SmartTokenMintingAction newtype over Credential, encoded as `toBuiltinData (ScriptCredential mintingLogicHash)` (Minting.hs mintRedeemer :159-160; offchain constructs it at BuildTx/ProgrammableLogic.hs:64). Registration proof = untyped pany scan over ref-inputs OR outputs for the ownCS-named directory NFT (Issuance.hs:127-136) — fixture registryNodeRefBuilder (Minting.hs:138-147). First-output custody rule (Issuance.hs:145-147,185-187) is what fixtures satisfy by putting the mint output first.
- CURRENT PARAMS-DATUM ENCODING IN FIXTURES: 2-field list-encoded ProgrammableLogicGlobalParams{directoryNodeCS, progLogicCred} (onchain Types/ProtocolParams.hs:44-62, hand-rolled list ToData/FromData :51-62). Test fixture instances: Global.hs protocolParamsDatum :540-542 fed into ~20 contexts via withRefInputDatumValue paramRef (:531-538, used at :653-657, 697-701, 759-763, 792-796, 815-819, 838-842, 867-871, 900-904, 933-937, 985-989, 1022-1026, 1060-1064, 1094-1098, 1120-1124 and mkDirSpendCtx :308-312); bench BenchmarkOnchainScripts.hs:186-188 (+ ~18 use sites) and :916; QuickCheck generator genGlobalParams lib/ProgrammableTokens/Gen.hs:27-31.
- CURRENT GLOBAL/SEIZE REDEEMER ENCODING: ProgrammableLogicGlobalRedeemer = TransferAct{plgrTransferProofs::[Integer], plgrMintProofs::[Integer]} | SeizeAct{directoryNodeIdx, inputIdxs, outputsStartIdx, lengthInputIdxs} with makeIsDataIndexed [0,1] (onchain ProgrammableLogicBase.hs:983-998); offchain-safe constructors mkSeizeActRedeemerFromRelativeInputIdxs :1034-1043 / FromAbsoluteInputIdxs :1057-1061 are imported directly by tests (Global.hs:45-51). Global script args [protocolParamsCS, ctx] (Global.hs:388,393); standalone seize validator same args (Global.hs:398-409). Directory MP args [initRef, issuanceCborHexCS, ctx] (DirectoryMint.hs:82-87) and CBOR-split derivation is pinned by DirectoryMint's insertedCs blake2b_224 reconstruction (:149-157) against IssuanceCborHex prefix/postfix datum (:138-140,209-214).
- HARNESS ENTRY POINTS FOR THE NEW DUAL-ARM SUITE: (1) test/Spec.hs:12-21 testGroup list + cabal test-suite other-modules (programmable-tokens-testlib.cabal:83-88) — add new module(s) both places; (2) testlib exposed modules (cabal:50-53): ProgrammableTokens.Gen, ProgrammableTokens.Test, ProgrammableTokens.Test.ScriptContext.Builder; (3) raw-eval pattern (compileNoTracing + applyArguments + isLeft/isRight) for §14.1 red shapes; (4) mockchain pattern (mockchainSucceedsWithTarget + deployDirectorySet + issueProgrammableTokenTx + assertFailingTx) for §14.2 end-to-end green paths — Test.hs:124-126,145-170,224-256,93-98; (5) benchmark plug points for §14.3: mkCase/mkBenchCase + benchCases list (BenchmarkOnchainScripts.hs:48-50,1517-1524), scenarioBackend record (:57-110), EvalKind enum (ScriptRunner.hs:53-63) + name/purpose maps (:298-322), shared fixture constants (exe/BenchmarkOnchain/ScriptFixtureIds.hs, ScriptHelpers.hs).
- CABAL DEPENDENCY NOTE: the testlib (library stanza cabal:48-76) depends on convex-mockchain/coin-selection/wallet + offchain + onchain; the test-suite (cabal:78-110) additionally gets plutarch, plutarch-ledger-api, plutarch-onchain-lib — so raw-eval tests live under test/, not lib/. Build env: tests/benchmarks need /usr/local/lib libsodium via --extra-lib-dirs (per MEMORY).

Change sites:
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/test/ProgrammableTokens/Test/ProgrammableLogicMinting.hs @ whole module; esp. mintArgs :80-86, mintRedeemer :159-160, ctx fixtures :168-239: ALL 5 tests break: issuance becomes a two-param policy with a 4-constructor redeemer (Local/DelegateTransfer/DelegateSeize/BurnOnly, spec §5-§6). Rewrite mintArgs (3 params -> 2), replace the Credential redeemer with the new tagged redeemer incl. witness indices, replace first-output fixtures with Local full-scan shapes, and add params-anchor reference input (§4) that the current fixtures do not carry at all. These 5 become seeds for the §14.1/§14.2 issuance suites.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/test/ProgrammableTokens/Test/ProgrammableLogicGlobal.hs @ protocolParamsDatum :540-542 and every mk*Ctx using withRefInputDatumValue paramRef; TransferAct redeemers :140-232, 633-712; script args :388,393,403,408: All 31 tests recompile against redesign phases 1-2: params datum grows to 4 fields (every fixture's paramRef ref-input datum changes encoding); TransferAct mint-proof list gains MintProof tags (Member/NonMember per §14.1 item 13) and the global gains paramsRefIdx — every `TransferAct [..] [..]` literal and mkGlobalTransferMintCtx/mkGlobalTransferTwoPoliciesCtx breaks; SeizeAct fixtures keep mkSeizeActRedeemer* constructors but those constructors' shape may change (ProgrammableLogicBase.hs:1034-1061). §14.1 item 13 coupling pins land here as new red tests.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/test/ProgrammableTokens/Test/DirectoryMint.hs @ insertArgs :82-87, insertedCs derivation :149-157, issuance ref-input :209-214: The CBOR-split derivation pin: insertedCs reconstructs the on-chain _papplyHashedParameter blake2b over IssuanceCborHex prefix/postfix. When the issuance script becomes two-param (Phase 3/4 changes the CBOR split point and the last-applied-argument contract documented at Issuance.hs:100-103), issuancePrefix/issuancePostfix fixtures and the derivation formula must be updated in lockstep or both tests silently break.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/test/ProgrammableTokens/Test/DirectorySet.hs @ :35-56 (both target groups), issueAlwaysSucceedsValidator :43-56: 4 mockchain tests break at Phase 4 (offchain): they flow through Endpoints.deployCip143RegistryTx, Query.globalParamsNode (decodes the 2-field params datum), Query.issuanceCborHexUTxO, and BuildTx.issueProgrammableToken (constructs the old SmartTokenMintingAction redeemer at BuildTx/ProgrammableLogic.hs:64). §14.2 items 1-2 (simple mint, register+first-mint) are exactly these paths and must stay green after the builder rewrite; §14.2 items 3-12 are NEW mockchain tests plugging in here.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/lib/ProgrammableTokens/Gen.hs @ genGlobalParams :27-31: Constructs 2-field ProgrammableLogicGlobalParams; must gain the two new fields when the params datum becomes 4-field (Phase 1 golden tests will want this generator).
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/lib/ProgrammableTokens/Test.hs @ issueProgrammableTokenTx :224-256; exports :1-24: The single reusable issuance mockchain action — currently passes one opaque `redeemer` to invokeMintingStakeScript and knows nothing about witness arms/indices. Extend (or add variants: issueViaLocal / issueViaDelegateTransfer / burnOnlyTx / seizeViaDelegateTx) for §14.2 items 4-11; also promote a shared `compileNoTracing` + red-assertion helpers here to stop the per-module duplication.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/test/Spec.hs @ :12-21: Register the new Dual-Arm test module(s) (e.g. ProgrammableTokens.Test.IssuanceDualArm for §14.1, extend DirectorySet or a new EndToEnd module for §14.2) in the testGroup.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/programmable-tokens-testlib.cabal @ test-suite other-modules :83-88; library exposed-modules :50-53: Add new test modules to other-modules; if shared eval helpers move to the testlib, add the module to exposed-modules and plutarch deps to the library stanza (currently the library lacks plutarch — eval helpers can only live in the test-suite unless deps are added).
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchainScripts.hs @ issuance cases :1517-1524; protocolParamsDatum :186-188 (+ ~18 use sites, :916); scenarioBackend :57-110 (ssbProgrammableMintSpec :113-116): §14.3 gates: every issuance BenchCase arg list changes to the 2-param policy + new redeemers; params datum fixtures change; new scenarios needed (DelegateTransfer, BurnOnly, DelegateSeize, register+mint, 50-recipient airdrop, three-policy, late-witness-index) per spec §14.3. EvalKind (ScriptRunner.hs:53-63) may need new constructors if new standalone scripts appear.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchainAikenScripts.hs @ whole exe (1810 lines) + exe/BenchmarkOnchain/ScriptFixtureIds.hs, ScriptHelpers.hs, TxD29Fixture.hs, MainnetDexFixture.hs: Aiken ca7864f baseline must keep evaluating the SAME scenarios for like-for-like §14.3 comparison — shared fixture ids/helpers must stay backend-agnostic; any new shared scenario needs an Aiken counterpart built from Aiken's own redeemer shapes.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/examples/regulated-stablecoin/test/unit/Wst/Test/UnitTest.hs @ :68-76 (and src/examples/aiken/haskell/test/Wst/Aiken/Test.hs:40-41): Downstream consumers of the same ProgrammableTokens.Test harness (incl. the only current assertFailingTx red mockchain test at :73). They break at Phase 4 with the offchain builder changes — outside programmable-tokens-test but in the same cabal project; a missed update breaks `cabal build all`.

Risks:
- Redeemer-map ordering: buildBalancedScriptContext ledger-sorts redeemers (Builder.hs:504-523) but plain buildScriptContext does not; Dual-Arm index-based redeemers (witness indices into ref-inputs/withdrawals) are sensitive to the builder's insertion-sort of reference inputs by TxOutRef (Builder.hs:113-117) — fixture TxOutRefs must be chosen so intended indices survive sorting (existing convention: paramRef 'aa00' < dirNodeRef 'bb00' < dirNode2Ref 'bb01', Global.hs:504-513).
- Output-order traps: withOutput prepends and buildBalancedScriptContext does NOT reverse outputs while buildScriptContext DOES (Builder.hs:487 vs :521) — several existing fixtures encode comments about reversed order (Global.hs:889-898, 922-931); new Local full-scan fixtures must not inherit stale ordering assumptions.
- The §14.1 red suite must use raw-eval isLeft, not mockchain: convex balancing runs scripts during fee estimation, so deliberately-failing shapes need either evalScript-level tests (current idiom) or SubmitFailingTx + assertFailingTx (only demonstrated in the regulated-stablecoin example, UnitTest.hs:73) — collateral-consuming failing txs on the mockchain need the failing-submit path.
- compileNoTracing duplication (4 copies) means a Config change (e.g. tracing for debugging red tests) must be made in 4 places; consider consolidating before writing the new suite — but note the testlib library stanza lacks plutarch deps, so consolidation requires cabal changes.
- protocolParamsDatum is list-encoded via hand-rolled BI.mkList ToData (ProtocolParams.hs:58-62) — golden encoding changes in Phase 1 will NOT be caught by type errors in fixtures that pass raw BuiltinData; only runtime eval failures will surface mismatches, so update Gen.hs + all fixture datums together.
- Registration proof moves from a pany scan (Issuance.hs:134-136) to indexed nodeRefIdx witnesses — DirectoryMint's registration-authorization tests and Minting's registryNodeRefBuilder overlap here; §14.1 items 3-4 need both wrong-index and pre-insert-view red shapes, which the current builder supports only via careful TxOutRef ordering.
- Benchmark co-witness enumeration (ScriptScenario.scenarioEvalSpecsFromCtx) keys off script hashes/credentials in the ctx (sseProgLogicBaseHash etc.); new Dual-Arm scenarios with new script hashes silently produce EMPTY co-witness lists (understating tx totals) if ScriptScenarioEnv is not extended — a wrong benchmark, not a build error.
- Examples suites (regulated-stablecoin, aiken example) compile against ProgrammableTokens.Test and the offchain Env/BuildTx API; they are outside the named test package and easy to miss — they will break the workspace build at Phase 4.

Test touchpoints:
- Run current suite: cabal test programmable-tokens-test (needs --extra-lib-dirs=/usr/local/lib for libsodium VRF); test entry test/Spec.hs main.
- §14.1 red suite plugs into: new test/ProgrammableTokens/Test/<DualArm>.hs module using compileNoTracing + applyArguments + evalScript isLeft (pattern ProgrammableLogicGlobal.hs:386-417), contexts from ScriptContext.Builder (buildBalancedScriptContext for balanced shapes, buildScriptContext for minimal ones), registered in Spec.hs:12-21 + cabal other-modules :83-88.
- §14.2 functionality suite plugs into: ProgrammableTokens.Test mockchain actions — mockchainSucceedsWithTarget (Test.hs:124), deployDirectorySet (Test.hs:145), issueProgrammableTokenTx (Test.hs:224) extended with Dual-Arm variants; assertions expectSingleton/expectN (Test.hs:75-83), Query.registryNodes/programmableLogicOutputs usage pattern in DirectorySet.hs:52-56; red mockchain shapes via SubmitFailingTx + assertFailingTx (Test.hs:93-98, example at examples/regulated-stablecoin UnitTest.hs:73).
- §14.3 gates plug into: benchCases list + mkCase (BenchmarkOnchainScripts.hs:48,1517-1524), scenarioBackend (:57-110), EvalKind (ScriptRunner.hs:53-63), runScriptBenchmark (ScriptRunner.hs:83); Aiken counterpart in BenchmarkOnchainAikenScripts.hs sharing ScriptFixtureIds/ScriptHelpers/ScriptScenario; run via cabal run benchmark-onchain-scripts / benchmark-onchain-aiken-scripts.
- Property tests: QuickCheck via tasty-quickcheck (pattern Global.hs:354-368); generators in lib/ProgrammableTokens/Gen.hs (genGlobalParams :27 must grow with the 4-field datum).
- Existing tests that must stay green as regression floor (spec §14.2 item 13): all 42 — DirectoryMint 2, DirectorySet 4, ProgrammableLogicGlobal 31, ProgrammableLogicMinting 5 (the latter rewritten, not deleted).
- Downstream harness consumers to re-run after offchain changes: src/examples/regulated-stablecoin/test/unit/Wst/Test/UnitTest.hs and src/examples/aiken/haskell/test/Wst/Aiken/Test.hs.

### Offchain transaction-builder mapping for the dual-arm-custody issuance redesign (spec §6, §10, §11.7) — every site that constructs issuance/global/seize redeemers or issuance transactions in src/programmable-tokens-offchain, src/examples, plus the two builder sites in src/programmable-tokens-test that compose issuance txs

Key facts:
- Current issuance redeemer is a static one-field newtype: SmartTokenMintingAction Credential (src/programmable-tokens-onchain/lib/SmartTokens/Contracts/Issuance.hs:36). It is constructed exactly once offchain, in issueProgrammableToken (src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/ProgrammableLogic.hs:64) and passed to mintPlutus at lines 85 and 95 — a build-time-static redeemer. The new 4-constructor MintRedeemer (Local/DelegateTransfer/DelegateSeize/BurnOnly, spec section 6) carries post-balancing indexes, so the mint witness must become body-dependent; no mint-with-txbody helper is currently used anywhere in the repo (only addWithdrawalWithTxBody + raw TxBuilder, pattern at ProgrammableLogic.hs:258-260).
- Registration-mode selection already exists in issueProgrammableToken: key dirNodeData == issuedSymbol -> mint + BuildTx.addReference (uIn udat) (ProgrammableLogic.hs:82-89, the future RegisteredByReferenceInput, index via findIndexReference post-balancing) vs insertDirectoryNode (ProgrammableLogic.hs:96, the future RegisteredByOutput, index of the prepended insertedNode output, Directory.hs:136).
- The minting-logic withdrawal (source of mintingLogicWdrlIdx) is added by invokeMintingStakeScript (ProgrammableLogic.hs:115-129, addScriptWithdrawal hsh 0) in the core package and independently by addIssueWitness (src/examples/regulated-stablecoin/lib/Wst/Offchain/BuildTx/TransferLogic.hs:374-380). The ledger canonically sorts withdrawals by reward account, so mintingLogicWdrlIdx/globalWdrlIdx must be computed from the final sorted withdrawal list post-balancing, not insertion order.
- Global redeemer TransferAct { plgrTransferProofs, plgrMintProofs } (src/programmable-tokens-onchain/lib/SmartTokens/Contracts/ProgrammableLogicBase.hs:983-996) is built in exactly two places: transferProgrammableToken (ProgrammableLogic.hs:209-213, proofs via proofNodeIndex/findIndexReference at 245-249, minted symbols walk at 251-256, params ref input added at 222 but its index is NOT in the redeemer today) and hand-rolled in FullTransactionBenchmarks.hs:1143-1146 (plgrMintProofs = []). Both must gain paramsRefIdx and switch mintProofs to MintProof = Member | NonMember nodeIdx (spec section 11.3).
- SeizeAct is built only via mkSeizeActRedeemerFromAbsoluteInputIdxs, defined in the ONCHAIN module ProgrammableLogicBase.hs:1058-1062 (relative variant at 1035-1043) and consumed by seizeProgrammableToken (src/examples/regulated-stablecoin/lib/Wst/Offchain/BuildTx/ProgrammableLogic.hs:142-146; params ref added at 155 without index). SeizeAct gains paramsRefIdx appended as final field, so both smart constructors change signature.
- Params UTxO location: Query.globalParamsNode (src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Query.hs:179-185) = UTxO at dsProtocolParamsSpendingScript payment credential holding the protocolParamsPolicyId NFT. Directory nodes: Query.registryNodes (Query.hs:57-61) = UTxOs at dsDirectorySpendingScript holding directoryNodePolicyId tokens; node choice via registryNode with MustBeEqual/MustNotBeEqual/GreaterThanOrEqual against the derived issued symbol (Query.hs:89-105). CBOR anchor: Query.issuanceCborHexUTxO (Query.hs:169-175). Every consumer passes these UTxODat values in; the new redeemers additionally need their post-balancing reference-input indexes.
- Issuance template application sites that pin the parameter change (protocolParamsCS :--> mintingLogicHash, spec section 5): Scripts.programmableLogicMintingScript (src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Scripts.hs:123-137, currently progLogicCred + directoryNodeCS + mintingCred-last), Env.programmableTokenMintingScript (src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Env/TransferLogic.hs:59-64), and the CBOR-split derivation issuerPrefixPostfixBytes (src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/IssuanceCborHexRef.hs:41-56) which must compile the same prefix and still split on the last-applied mintingLogicHash placeholder.
- First-output/single-name assumptions to drop: current onchain Issuance checks the first output (Issuance.hs:172), which is why issueProgrammableToken + paySmartTokensToDestination keep a single recipient output and why one-tx multi-recipient mint is forbidden today. Under Local full-scan there is no output-position rule; builders may emit multiple names/recipients (airdrop, CIP-68). No airdrop builder exists in this worktree yet — Local is the designated witness for it (spec section 10.1). insertDirectoryNode's prependTxOut ordering (Directory.hs:136-137) becomes a performance preference (indexes early), never an assumed position.
- Pure-delegation sites needing no redeemer work (recompile only): Wst/Server.hs endpoints (310-319, 326-353), Wst/Client.hs, Wst/Offchain/Endpoints/Deployment.hs wrappers, all Endpoints-driven benchmark cases (FullTransactionBenchmarks.hs:194-207, 231, 326, 342, 359, 405), UnitTest.hs scenarios (Endpoints only), aiken example Wst/Cli.hs:139-151 and Wst/Aiken/Test.hs:82,117 (they pass only the token-specific minting-logic redeemer through registerCip143PolicyTx's polymorphic redeemer parameter), Wst/Aiken/Offchain.hs:26-37 (blueprint->TransferLogicEnv only), Wst/Offchain/BuildTx/ExampleTransfer.hs (all undefined stubs). Stake-cert-only builders unchanged: registerTransferScripts (ProgrammableLogic.hs:149-174), registerProgrammableGlobalScript (43-50), ReferenceScripts.hs.
- Latent guard to update: issueProgrammableToken compares the queried params datum against the env-derived one (glParams == uDatum paramsTxOut, ProgrammableLogic.hs:71-73) — comparison type widens to the 4-field datum. mintProtocolParams (BuildTx/ProtocolParams.hs:27-53) writes the current 2-field datum from Env.globalParams (Env/Directory.hs:169-174); both move to the 4-field datum with the section-12 derivation order inside mkDirectoryEnv (Env/Directory.hs:135-137 currently derives base/global from protocolParamsPolicyId).

Change sites:
- src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/ProgrammableLogic.hs @ issueProgrammableToken, lines 57-103 (redeemer built at 64, mintPlutus at 85/95, ref-node branch 82-89, insert branch 90-96): (a) Replace static SmartTokenMintingAction mint redeemer with the 4-constructor MintRedeemer; compute mintingLogicWdrlIdx (sorted withdrawal position), paramsRefIdx (findIndexReference of uIn paramsTxOut), and RegistrationWitness (RegisteredByReferenceInput idx of uIn udat in the ref-branch; RegisteredByOutput idx of the inserted node output in the insert-branch) inside a post-balancing TxBuilder closure — mintPlutus cannot express this; needs a body-dependent mint witness. (c) Drop the implicit single-recipient/first-output coupling; support per-policy witness selection (Local default, DelegateTransfer/DelegateSeize/BurnOnly per spec section 10.1).
- src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/ProgrammableLogic.hs @ invokeMintingStakeScript, lines 115-129: Still adds the minting-logic withdrawal, but its final sorted position is now load-bearing (mintingLogicWdrlIdx); expose/compute the index from the final tx body rather than assuming insertion order.
- src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/ProgrammableLogic.hs @ transferProgrammableToken, lines 187-235 (redeemer 209-213, params ref 222, proof helpers 200-207 and 237-256): (b) Add paramsRefIdx (post-balancing index of the params reference input added at line 222) to TransferAct; convert plgrMintProofs from covering-node indexes to MintProof = Member | NonMember nodeIdx — minted programmable policies get nullary Member (no ref-input needed), non-programmable get NonMember with covering-node ref index; mintProofReferences (line 205) then only references non-member covering nodes.
- src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/Directory.hs @ insertDirectoryNode, lines 80-137 (refs 132-133, mint 135, prependTxOut 136-137); insertNodeArgs 51-63: Register-and-mint arm: the inserted node's OUTPUT index feeds RegisteredByOutput and must be computed post-balancing, not assumed from prependTxOut ordering; node key derivation (insertNodeArgs via programmableTokenMintingScript) changes with the two-parameter issuance template; params/cbor-hex reference inputs stay but their indexes may now also be witnessed.
- src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Endpoints.hs @ registerCip143PolicyTx lines 86-115; transferTokens lines 136-167: Composition sites: registerCip143PolicyTx (queries at 107-109: issuanceCborHexUTxO, registryNodeForReferenceOrInsertion, globalParamsNode) composes issueProgrammableToken + paySmartTokensToDestination + invokeMintingStakeScript inside balanceTxEnv_ — must guarantee all MintRedeemer indexes resolve in the final balanced body; transferTokens inherits the TransferAct paramsRefIdx/MintProof change.
- src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/ProtocolParams.hs @ mintProtocolParams, lines 27-53 (datum at 46): Write the new 4-field params datum and hardened anchor output (spec sections 4/11.1); datum value comes from Env.globalParams which must be rebuilt per the section-12 derivation order.
- src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/BuildTx/IssuanceCborHexRef.hs @ issuerPrefixPostfixBytes lines 41-56; mintIssuanceCborHexNFT lines 70-99 (globalParams use at 75-76): Recompute the CBOR prefix/postfix split for the two-parameter issuance template (protocolParamsCS applied, mintingLogicHash still last); replace the directoryNodeCS/progLogicCred inputs with protocolParamsCS.
- src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Scripts.hs @ programmableLogicMintingScript, lines 123-137: Change applied parameters from (progLogicCred, directoryNodeCS, mintingCred-last) to (protocolParamsCS, mintingLogicHash-last); ripples into every policy-id derivation.
- src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Env/TransferLogic.hs @ programmableTokenMintingScript lines 59-64; programmableTokenPolicyId lines 67-69: Feed protocolParamsCS instead of directoryNodeCS+progLogicCred into the template; policy IDs consumed by Query.registryNode, Deployment.seizeMultiCredentialAssetsTx and Endpoints.transferTokens all change (full redeploy).
- src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Env/Directory.hs @ mkDirectoryEnv lines ~120-158 (base/global derivation 135-137); globalParams lines 169-174: Adopt the section-12 acyclic derivation order and the single public env constructor invariant; globalParams becomes the 4-field datum; base validator must keep compile-time global+seize credentials derived from the same scripts stored in the datum.
- src/programmable-tokens-offchain/lib/ProgrammableTokens/OffChain/Query.hs @ globalParamsNode 179-185; registryNodes 57-61; registryNode 89-105; issuanceCborHexUTxO 169-175: Query mechanics unchanged (NFT-at-script-credential lookups) but datum type widens to 4 fields and the derived issued symbol used in registryNode changes with the new template; these UTxOs are the objects whose post-balancing ref-input indexes become paramsRefIdx/nodeRefIdx.
- src/examples/regulated-stablecoin/lib/Wst/Offchain/BuildTx/TransferLogic.hs @ issueSmartTokens 230-236; addIssueWitness 374-380; transferSmartTokens 238-271; seizeSmartTokens 290-319; multiSeizeSmartTokens 341-372; addSeizeWitness 455-461: issueSmartTokens inherits (a) via issueProgrammableToken and its own withdrawal via addIssueWitness (index source for mintingLogicWdrlIdx); transferSmartTokens inherits TransferAct change; seize wrappers inherit SeizeAct paramsRefIdx; seize+mint-of-same-policy (DelegateSeize with seizeRedeemerIdx into txInfo.redeemers) is a NEW builder capability with no current site — must be added if that flow is exercised.
- src/examples/regulated-stablecoin/lib/Wst/Offchain/BuildTx/ProgrammableLogic.hs @ seizeProgrammableToken, lines 71-163 (redeemer via mkSeizeActRedeemerFromAbsoluteInputIdxs 142-146; index helpers 124-139; params ref 155): (b) Pass paramsRefIdx (index of paramsTxIn among reference inputs, currently added without index at 155) through the updated mkSeizeActRedeemer* constructors; firstSeizeContinuationOutputIndex (132-139) already post-balancing — keep; note the constructors live in onchain ProgrammableLogicBase.hs:1035-1062 and change signature.
- src/examples/regulated-stablecoin/lib/Wst/Offchain/Endpoints/Deployment.hs @ deployFullTx 48-72; issueSmartTokensTx 75-103; transferSmartTokensTx 130-163; seizeCredentialAssetsTx 171-212; seizeMultiCredentialAssetsTx 214-249: deployFullTx is the genesis composition (mintProtocolParams + initDirectorySet + registrations) and must adopt the section-12 order/4-field datum; the others are query+compose wrappers that recompile against the new builder signatures (queries at 98-100, 156-160, 209, 246).
- src/examples/regulated-stablecoin/exe/full-transaction-benchmarks/FullTransactionBenchmarks.hs @ oneOutputPerInputTransferTx, TransferAct construction lines 1143-1146 (witness 1148-1153, refs added 1174-1180); import lines 62-65: Hand-rolled TransferAct { plgrTransferProofs=[proof], plgrMintProofs=[] } must gain paramsRefIdx (params ref added at 1174) and the MintProof type; directoryProofToTokenProof/directoryProofNodeForSymbol helpers follow the transferProgrammableToken changes.
- src/programmable-tokens-test/lib/ProgrammableTokens/Test.hs @ issueProgrammableTokenTx, lines 224-256: Same composition as registerCip143PolicyTx (issueProgrammableToken + paySmartTokensToDestination + invokeMintingStakeScript at 252-255); recompiles against (a); used by DirectorySet tests and the aiken example tests.
- src/programmable-tokens-test/test/ProgrammableTokens/Test/ProgrammableLogicGlobal.hs @ TransferAct literals 140, 148, 163, 175, 190, 202, 214, 222, 230, 718; mkSeizeActRedeemerFromRelativeInputIdxs 748, 774, 806, 829, 852, 881, 914, 964: Every direct redeemer literal gains paramsRefIdx / MintProof tags; these are the tests pinning the frozen constructor layout (golden encoding test to be added per spec section 6).

Risks:
- No body-dependent mint-witness helper exists in the Convex.BuildTx surface used here — mintPlutus takes a static redeemer. The MintRedeemer indexes (withdrawal position, ref-input positions, output position, redeemer-map position for DelegateSeize) all require final-body closures; the only proven pattern is addWithdrawalWithTxBody / raw TxBuilder (ProgrammableLogic.hs:258-260, TransferLogic.hs:451-453). A mint analogue must be written or upstreamed, and the balancing loop must stay fixed-point stable when change-output insertion shifts indexes (spec section 10.2: recompute after final balancing, never fall back to 0).
- Withdrawal ordering trap: the ledger sorts withdrawals canonically by reward account, so mintingLogicWdrlIdx/globalWdrlIdx computed from builder insertion order will be wrong whenever another stake script hashes lower — must read the sorted withdrawal list of the final body. Same class of trap for txInfo.redeemers ordering behind DelegateSeize's seizeRedeemerIdx.
- mkSeizeActRedeemerFromAbsoluteInputIdxs/RelativeInputIdxs live in the onchain module (ProgrammableLogicBase.hs:1035-1062); their signature change ripples into Wst example builder and 8+ test call sites simultaneously — cannot be staged.
- Policy-id cascade: changing the issuance template parameters changes every derived programmableTokenPolicyId, the directory node keys, the CBOR split bytes, and the params datum contents at once; any offchain site still deriving with the old 3-arg application (Scripts.hs:123, Env/TransferLogic.hs:59, IssuanceCborHexRef.hs:52) silently produces scripts that hash differently from on-chain registration — the equality guard at ProgrammableLogic.hs:71 only catches the params datum, not template drift.
- DelegateTransfer/DelegateSeize/BurnOnly have NO existing builder paths (today every mint, including mixed shapes, goes through the single Local-like path with the global validator independently invoked); mixed transfer+mint and seize+mint flows need new witness-selection logic per spec section 10.1, and burn flows (none exist offchain today) need BurnOnly.
- Dropping the first-output rule enables multi-recipient/multi-name mints, but paySmartTokensToDestination-based flows and the section 10.6 MUST (inline stake credential on every new base output) need explicit builder enforcement once outputs multiply — programmableTokenReceivingAddress currently guarantees it only for the single destination path.

Test touchpoints:
- src/examples/regulated-stablecoin/test/unit/Wst/Test/UnitTest.hs — Endpoints-driven scenarios: issuance 107-115, transfer 128-145, blacklist 155-188, blacklisted-transfer 206-244, seize 253, multi-seize 295; registerTransferScripts helper 308-315. All exercise the new redeemers end-to-end via mockchain.
- src/programmable-tokens-test/test/ProgrammableTokens/Test/ProgrammableLogicGlobal.hs — direct TransferAct/SeizeAct redeemer literals (lines 140-230, 718, 748-964) pin the global/seize redeemer encoding; all must add paramsRefIdx and MintProof tags.
- src/programmable-tokens-test/test/ProgrammableTokens/Test/DirectorySet.hs:51 — Test.issueProgrammableTokenTx with () minting-logic redeemer covers the register-and-mint (RegisteredByOutput) arm.
- src/examples/aiken/haskell/test/Wst/Aiken/Test.hs:82,117 — registerCip143PolicyTx with Integer redeemer proves the polymorphic token-logic redeemer path survives the MintRedeemer change.
- src/examples/regulated-stablecoin/exe/full-transaction-benchmarks/FullTransactionBenchmarks.hs — issueSmartTokensTx case 222-233, seize cases 326-359, max-quantity 405, and the hand-rolled oneOutputPerInputTransferTx (1143) are the section 14.3 bench gates; the direct TransferAct there breaks at compile time on the redeemer change.
- New golden encoding test required by spec section 6: MintRedeemer constructor indices (Local=0, DelegateTransfer=1, DelegateSeize=2, BurnOnly=3) and RegistrationWitness (RegisteredByReferenceInput=0, RegisteredByOutput=1), plus the frozen TransferAct/SeizeAct layouts from section 11.3 — no existing file covers this.

### Benchmark harness mapping for §14.3 gates — Plutarch harness (BenchmarkOnchainScripts.hs), Aiken harness (BenchmarkOnchainAikenScripts.hs), shared BenchmarkOnchain/* libs, benchmark-onchain-compare.sh, vs Aiken ca7864f reference

Key facts:
- ARCHITECTURE: Both harnesses are cabal executables (src/programmable-tokens-test/programmable-tokens-test.cabal:112 `benchmark-onchain-scripts`, :138 `benchmark-onchain-aiken-scripts`, :163 `benchmark-onchain-functions`) sharing BenchmarkOnchain.{ScriptRunner,ScriptScenario,ScriptFixtureIds,ScriptHelpers,Formatting,MainnetDexFixture,TxD29Fixture,Compile,CardanoScriptHelpers}. Each BenchCase = (name, primary EvalSpec, full-tx ScriptContext); runCase (ScriptRunner.hs:105-128) evaluates the primary spec plus every witness the scenario walker derives from the SAME ScriptContext (ScriptScenario.hs:50-101 walks txInfoInputs/txInfoWdrl/txInfoMint and maps credentials/CSs to backend hooks), filtering specs whose EvalKind equals the primary. Totals are per-transaction sums of all script witnesses — this is what makes runs 'ledger-valid like-for-like full transactions'.
- SCENARIO CATALOGUE (identical names both sides; Plutarch list at BenchmarkOnchainScripts.hs:1484-1557, Aiken at BenchmarkOnchainAikenScripts.hs:1580-1810): programmableLogicBase (base spend view of globalTransferCtx); programmableLogicGlobal.TransferAct (1 base input w/ 5 tokens -> 2 base outputs + change, params+dirnode refs, ctx :321-361); .TokenDoesNotExist (:363-399, non-programmable token + covering node); .MixedMany (:401-471, 2 non-prog + 3 prog policies, 4 node refs); .ManyTokens50 (:560-597, 50 names/1 policy wholesale move); .ManyOutputs20 (:599-642, 1 input -> 20 recipient base outputs); .ManyPolicies10 (:644-708, 10 policies + 10 node refs, positional proofs tied to ref-input sort order); .Spend5/10/15/25/50/100/120Utxos (mkGlobalTransferManyCtx :493-537); .MixedOwners5 (:1143-1186, 3 pubkey + 2 script stake owners); SeizeAct1/5/10/20/50/100/150 (mkGlobalSeizeCtx :710-739, N base inputs each 1 token, N stripped outputs + residual); SeizeAct1.ExternalScriptAnd50PubKeyInputs (:839-876); SeizeAct2.PartialSeizeWithNoise (:772-837); directoryNodeMinting.InitDirectory (:878-908); .InsertDirectoryNode (:910-988, registry insert + registered-asset mint whose issuance policy is NOT evaluated); programmableLogicMinting.Mint (:990-1006, 1 pubkey funding input, minted-to base output first, registry-node ref, NO params ref today); .Mint.BusyTx20Outputs (:1199-1224, 19 filler pubkey outputs); .Mint.TenUnrelatedWithdrawals (:1231-1253); .Mint.TopUpExistingTreasury (:1095-1136, mint + global TransferAct [1] [1]); .Burn (:1008-1043, burn -1 + global TransferAct [1] [1] + registry-node ref); .Burn.Redeem10Utxos (:1058-1088); protocolParamsMinting (:1255-1271); issuanceCborHexMinting (:1273-1289); programmableLogicBase.Tx.c3111df7.Spending + programmableLogicGlobal.TransferAct.Tx.c3111df7.NightAdaDex16Swaps (mainnet DEX replay :1291-1430); programmableLogicBase.Tx.d29ce2a9.Spending/.Stake (TxD29 replay :1436-1481).
- AIKEN SCRIPT LOADING: no runtime plutus.json read. AikenProgrammableTokenScripts.hs:138-176 embeds the ca7864f compiled CBOR hex of each validator (always_fail, protocol_params_mint, issuance_cbor_hex_mint, issuance_mint as programmableLogicMintingScript, programmable_logic_base, programmable_logic_global, registry_mint as directoryNodeMintingScript, registry_spend as directoryNodeSpendingScript) as string literals; compile-time params are applied at Haskell runtime via Convex.Aiken.Blueprint fromCardanoApiScriptToProgram + Plutarch applyArguments (applyDataArguments :111-119). Aiken repo /home/gumbo/iohk/cip113-programmable-tokens is checked out AT ca7864f; its plutus.json is the source of the embedded hex. Aiken V3 validators take only [ctx] at eval (aikenMintArgs/aikenRewardArgs/aikenSpendArgs, BenchmarkOnchainAikenScripts.hs:50-57); Plutarch side passes compile-time params as EVAL args instead (mkCase esArgs).
- SIZE COMPARISON: ScriptRunner.hs:143 records erSize = SBS.length (serialiseScript esScript) per EvalSpec; surfaced in the 'Per-script breakdown' Size column (:276-296). benchmark-onchain-compare.sh (repo root) runs both exes, joins Transaction-totals and breakdown tables BY EXACT case-name string, and extract_sizes (:375-417) dedupes first-seen size per script name and prints the gate line 'Plutarch never larger than Aiken on any counterpart script: YES/NO' (a > 0 && p > a flags LARGER violation). New scenarios pair automatically only if both mains use byte-identical case-name strings.
- SIZE-GATE ASYMMETRY (existing): Plutarch esScript is the compiled template WITHOUT compile-time params applied (params go in esArgs), while Aiken esScript HAS params baked in via applyDataArguments — Plutarch measured sizes slightly understate deployed size relative to Aiken's applied size. The two-param issuance redesign shrinks this delta but the asymmetry itself is inherited.
- CONFIRMED LATENT DEFECT — under-applied base validator: mkProgrammableLogicBase now takes TWO credentials (global, seize) + ctx (ProgrammableLogicBase.hs:714), and the walker backend correctly passes 3 args ([globalStakeCred, seizeCredBench, purposeCtx], BenchmarkOnchainScripts.hs:72-77), but three top-level catalogue entries still pass only 2 args: 'programmableLogicBase' (:1486 [globalCred, ctx]), 'programmableLogicBase.Tx.c3111df7.Spending' (:1525-1532), 'programmableLogicBase.Tx.d29ce2a9.Spending' (:1541-1548). Under-application yields a lambda, which evalScript' 'succeeds' on without running the body — checkpoint outputs show the giveaway constant 560,100 CPU / 3,600 mem (pure machine steps, identical across different contexts) vs Aiken's 5.8M. These rows currently report a fake 10.41x win and MUST be fixed before the §14.3 gate is trusted.
- STALE BACKEND HOOKS (currently dead code, live under the new scenarios): Plutarch ssbProgrammableMintSpec (BenchmarkOnchainScripts.hs:111-116) passes [progLogicBaseCred, mintingLogicHash, ctx] — missing the directoryNodeCS arg the current 4-arg mkProgrammableLogicMinting signature needs (top-level cases at :1517-1522 pass 4 args). Aiken ssbProgrammableMintSpec (BenchmarkOnchainAikenScripts.hs:118-123) applies sseDirectoryPolicyCS (0x18) as registry CS while the top-level cases (:1741 etc.) use directoryNodeCS (0x11). Both hooks are only reachable when a context mints mintingPolicyCS with a DIFFERENT primary — exactly what seize+mint, register+mint, and three-policy-mint scenarios will do.
- ISSUANCE REDESIGN DELTAS THAT DRIVE FIXTURE CHANGES (spec §5/§6/§11): script params become (protocolParamsCS, mintingLogicHash); redeemer becomes MintRedeemer = Local{mintingLogicWdrlIdx,paramsRefIdx,registration}|DelegateTransfer{+nodeRefIdx,globalWdrlIdx}|DelegateSeize{+nodeRefIdx,seizeRedeemerIdx}|BurnOnly{mintingLogicWdrlIdx} with RegistrationWitness = RegisteredByReferenceInput|RegisteredByOutput; params datum grows to 4 fields (directoryNodeCS, progLogicCred, globalLogicCred, seizeLogicCred); global TransferAct becomes {transferProofs, mintProofs :: [MintProof = Member | NonMember nodeIdx], paramsRefIdx}; SeizeAct appends paramsRefIdx as final field. Aiken ca7864f fixtures are the frozen baseline and DO NOT change; only new twin scenarios are added there. Aiken issuance_mint (validators/issuance_mint.ak) delegates to PLGlobal when its redeemer names the same registry node for BOTH TransferAct (TokenExists node_idx == RefInput index) and ThirdPartyAct (registry_node_idx == index), so Aiken twins exist for both delegate gates; Aiken burns still require the registry proof (harness burn fixtures use RefInput 1) while the Plutarch BurnOnly path legitimately drops the node ref input — a real design win the like-for-like tx shapes will now show.
- NEW §14.3 SCENARIOS REQUIRED (both harnesses unless noted): Mint.Busy50Outputs (parametrize the BusyTx builder: 49 filler outputs, ~105 ada funding); CIP-68 two-name mint (one policy, ref-NFT + user-token names to two different base outputs; Plutarch Local, Aiken RefInput proof); 50-recipient airdrop MINT (mint qty 50 fanned to 50 base outputs — distinct from existing ManyOutputs20 which is a TRANSFER; Local full-scan vs Aiken no_escape is the margin); three-policy mint (3 issuance policies with distinct minting logics — needs new fixture CSs/logic hashes, 3 registry-node refs, 3 minting-logic withdrawals, walker env + mintEvalSpec extension in BOTH backends so the two non-primary policies are evaluated); explicit DelegateTransfer transfer+mint (reshape TopUpExistingTreasury or add new; Aiken side already delegates via RefInput+TokenExists); burn-only + transfer+burn (reshape existing Burn/Burn.Redeem10Utxos: Plutarch drops directoryMintingNodeRef, issuance uses BurnOnly, global mintProofs [Member]); seize+mint via DelegateSeize (seized policy == minted policy: mint programmableTransferCS inside mkGlobalSeizeCtx-shape tx, seize SeizeAct.directoryNodeIdx == issuance nodeRefIdx, seizeRedeemerIdx must index the seize entry in txInfoRedeemers — builder redeemer-map ordering becomes load-bearing; Aiken twin: ThirdPartyAct + issuance RefInput); seize+burn (issuance BurnOnly + seize); register+mint (evaluate the issuance policy in the InsertDirectoryNode tx — currently skipped on BOTH sides — Plutarch Local{RegisteredByOutput}, Aiken OutputIndex proof, either as new primary case or via a walker branch for insertedCs); best-case vs late-position witness-index variants (params ref at ref-input position 0 vs sorted-last — needs a late-sorting paramRef TxOutRef id since ref inputs are insertion-sorted by TxOutRef, e.g. 0xee prefix vs current 0xaa; late minting-logic-withdrawal variant complements existing TenUnrelatedWithdrawals).
- FUNCTION-ISOLATION HARNESS: BenchmarkOnchainFunctions.hs (exe benchmark-onchain-functions, SimpleRunner-based) already carries the §14.3 encoding-rule decision cases: decision.a.baseFwd.* (scan vs indexed base forwarding, :667-674), decision.b.params.* (params-lookup cost at ref positions 0/2/4 + bakedFloor, :675-678), decision.c.local.* (full-scan vs indexed custody at 2/20/50 outputs, :679-688). Any new hot-path encoding change must land its aggregate-win case here first.
- CHECKPOINT/PROOF FLOW: doc/bench-checkpoint-*.txt files are saved outputs of benchmark-onchain-compare.sh; §14.3 self-baseline regression allowance is checked by diffing against the latest committed checkpoint. Formatting.hs supplies the pipe-delimited tables the compare script's awk parses — changing table layout breaks the comparer (columns are positionally parsed: totals cols 1/2/3/5/7, breakdown cols 1-6).

Change sites:
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchainScripts.hs @ benchCases :1517-1522 and ssbProgrammableMintSpec :111-116: Change every programmableLogicMinting arg list from [progLogicBaseCred, directoryNodeCS, mintingLogicHash, ctx] to the new two-param form [protocolParamsCS, mintingLogicHash, ctx]; fix the backend hook to the same (it is stale today — 3 args vs the current 4-param signature) because seize+mint / three-policy / register+mint scenarios make it live.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchainScripts.hs @ :1486, :1525-1532, :1541-1548: FIX under-applied mkProgrammableLogicBase primary specs: add the missing seize-credential argument (3 args total: global cred, seize cred, ctx). Today these evaluate a lambda and report ~560K CPU / 3.6K mem without running the body; txD29 needs a placeholder seize cred chosen consistently with its ctx.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchainScripts.hs @ programmableMintCtx :990-1006, programmableMintBusyTxCtx :1199-1224, programmableMintManyWithdrawalsCtx :1231-1253: Replace redeemer (ScriptCredential mintingLogicHash) with Local{mintingLogicWdrlIdx, paramsRefIdx, RegisteredByReferenceInput i}; ADD the protocol-params reference input (absent today — the redesign makes the params lookup mandatory on Local); keep mintingRegistryNodeRefBuilder (:228-234) as the registration witness; recompute indices against ref-input TxOutRef sort order (paramRef 0xaa sorts before directoryMintingNodeRef 0xbb).
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchainScripts.hs @ programmableMintTopUpCtx :1095-1136: Becomes the DelegateTransfer gate: issuance redeemer DelegateTransfer{mintingLogicWdrlIdx, paramsRefIdx, nodeRefIdx, globalWdrlIdx}; global redeemer TransferAct gains mintProofs::[MintProof] ([Member] for the minted policy) + paramsRefIdx; the first-output-holds-mint ordering comment becomes obsolete (Local/Delegate have no first-output rule).
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchainScripts.hs @ programmableBurnCtx :1008-1043, programmableBurnRedeem10Ctx :1058-1088: Issuance redeemer -> BurnOnly{mintingLogicWdrlIdx}; DROP the directoryMintingNodeRef reference input (BurnOnly needs no registration; the global's Member mint proof becomes a nullary tag needing no ref input); global TransferAct [1] [1] -> {[1], [Member], paramsRefIdx}.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchainScripts.hs @ every TransferAct construction: :325, :367, :405, :501, :565, :611, :680, :1014, :1063, :1100, :1162, :1395, :1438: Append paramsRefIdx and retype the second field [Integer] -> [MintProof] (transfer-only scenarios keep mintProofs = []); ManyPolicies10 (:680) keeps positional proofs but note its index comment (:662-668) — adding/reordering ref inputs shifts indices.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchainScripts.hs @ seize redeemers :714, :774, :842-848 (mkSeizeActRedeemerFromAbsoluteInputIdxs call sites): SeizeAct gains paramsRefIdx appended as final field — extend mkSeizeActRedeemerFromAbsoluteInputIdxs (ProgrammableLogicBase.hs:1057-1061) with the new parameter and update all three fixture call sites.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchainScripts.hs @ protocolParamsDatum :186-188, txD29ProtocolParamsDatumData :1440-1446, protocolParamsMintCtx :1255-1271: Params datum 2 -> 4 fields (add globalLogicCred = globalCred, seizeLogicCred = seizeCredBench; txD29 list gains 2 entries with fixture creds); protocolParamsMintCtx must satisfy the hardened §4.1 anchor (exact value, no stake cred, fully-decoding 4-field inline datum, ScriptCredential-only fields) or the scenario fails.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchainScripts.hs @ directoryInsertCtx :910-988, esp. registeredAssetRedeemer :924 and :949-951: The registered-asset mint redeemer (currently old-style ScriptCredential) becomes Local{..., RegisteredByOutput outIdx}; for the register+mint gate, additionally evaluate the issuance policy for insertedCs (new primary case or walker branch). computeRegisteredCs (:163-173) must track any change to the CBOR-split reconstruction for the new two-param template (protocolParamsCS lands inside the stored prefix; mintingLogicHash stays last).
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchainScripts.hs @ new fixtures + benchCases additions (append near :1517-1522): Add Mint.Busy50Outputs (parametrize :1199 builder), CIP-68 two-name mint, Mint.Airdrop50Recipients, ThreePolicyMint, explicit DelegateTransfer case if TopUp is kept separate, SeizeAct.MintSeized (DelegateSeize; seized == minted policy programmableTransferCS; SeizeAct.directoryNodeIdx == nodeRefIdx; seizeRedeemerIdx indexes txInfoRedeemers), SeizeAct.BurnSeized, RegisterAndMint, and best/late witness-index variants (late-sorting params ref TxOutRef, e.g. 0xee-prefixed, since ref inputs are insertion-sorted by TxOutRef).
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchainAikenScripts.hs @ twin fixtures + benchCases :1580-1810; ssbProgrammableMintSpec :118-123: Add byte-identical-named twins for every new scenario using ca7864f encodings: aikenRefInputProofData/aikenOutputIndexProofData (:263-269) for issuance, aikenTransferActRedeemerData (:247-249) with TokenExists node idx for DelegateTransfer twins, aikenThirdPartyActRedeemerData (:254-256) for seize+mint/burn twins (issuance RefInput index == ThirdPartyAct registry_node_idx). Existing Aiken fixtures stay frozen. Fix the backend hook's registry CS (uses sseDirectoryPolicyCS 0x18; top-level cases use directoryNodeCS 0x11) before it goes live for non-primary mint evals; extend it to select per-CS minting-logic creds for the three-policy twin.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchain/ScriptScenario.hs @ ScriptScenarioEnv :13-35, mintEvalSpec :91-101, ssbProgrammableMintSpec type :46: Extend the env with the extra minting policies/logic hashes needed by three-policy mint, the seized-policy mint (programmableTransferCS), and register+mint (insertedCs), and add mintEvalSpec branches so the walker evaluates those policies when they are not the primary; hook signature may need a per-CS logic-hash argument.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchain/ScriptFixtureIds.hs @ whole module (:67-260): Add fixture identities: second/third minting policy CSs + logic hashes (avoid taken bytes 0x10-0x1e, 0x21-0x22, 0x40, 0x44, 0x46, 0x55, 0x60+i policy range, keep canonical sort), their registry-node refs (0xbb-space; watch the ManyPolicies 0xbb 0x20+i range and issuanceInitRef = 0xbb 0x21 collision zone), CIP-68 token names, airdrop50/seize-mint input refs, and a late-sorting params ref for the late-witness-index variants.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-onchain/lib/SmartTokens/Contracts/ProgrammableLogicBase.hs @ :983-998 (redeemer type + makeIsDataIndexed), :1057-1061 (mkSeizeActRedeemerFromAbsoluteInputIdxs): Harness-facing surface of the companion change: TransferAct {transferProofs, mintProofs :: [MintProof], paramsRefIdx}, SeizeAct + paramsRefIdx final field, new MintProof type exported for fixture construction — both bench mains import these constructors (BenchmarkOnchainScripts.hs:25).
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/programmable-tokens-test.cabal @ :112-160 (both benchmark exe stanzas): Any new shared fixture module (e.g. a Cip68Fixture or extended ScriptFixtureIds split) must be added to other-modules of BOTH benchmark executables or one side fails to build.
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/benchmark-onchain-compare.sh @ :83-94 and :375-417: No code change required for new scenarios (name-keyed join pairs them automatically), but the §14.3 gate reading depends on it: exact name parity between the two mains, and the size gate line at :415. Only touch if table formats change (awk parses Formatting.hs column positions).
- /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/src/programmable-tokens-test/exe/BenchmarkOnchainFunctions.hs @ decisionBenchCases :665-688: Home for any pre-gate isolation measurements the implementation needs (encoding-rule compliance); existing decision.a/b/c cases already cover base-forwarding, params-lookup position cost, and Local full-scan vs indexed — extend only if a new hot-path encoding question arises.

Risks:
- The three under-applied base-spend primary specs make the current 'Plutarch total CPU/mem lower than Aiken for every shared scenario' gate partially fraudulent (fake 10.41x rows); fixing them will visibly worsen those three rows against the committed self-baseline checkpoints — the regression allowance must account for this correction, not treat it as a regression.
- Index-bearing fixtures are fragile: reference inputs are insertion-sorted by TxOutRef inside the context builder (comments at BenchmarkOnchainScripts.hs:662-668), withOutput PREPENDS (multiple fixtures rely on composing filler outputs first), and DelegateSeize's seizeRedeemerIdx depends on txInfoRedeemers AssocMap ordering, which no current fixture controls — a builder helper pinning redeemer-map order may be needed or the seize+mint fixture will be positionally wrong in a way that fails only at eval time.
- Walker/back-end divergence is silent: stale ssbProgrammableMintSpec arg lists (both files) evaluate to under-applied lambdas that PASS, so a new scenario exercising the hook can go green while measuring nothing; every new scenario needs a sanity check that its per-script breakdown budgets are non-trivial.
- Scenario pairing is by exact string; a one-character name mismatch between the two mains silently drops the row into the unpaired '-' section of the comparison instead of failing the gate.
- The Aiken baseline is frozen embedded hex; if anyone regenerates it from a different commit of /home/gumbo/iohk/cip113-programmable-tokens (currently at ca7864f) the comparison target changes invisibly — the hex literals in AikenProgrammableTokenScripts.hs:145-176 are the actual baseline, not the checked-out repo.
- Plutarch sizes are measured on the unapplied template (params in esArgs) while Aiken sizes include applied params — the 'every counterpart script serializes smaller' gate is slightly generous to Plutarch; worth an explicit note or an applied-size measurement when the new two-param issuance template is gated.
- Burn/BurnOnly reshaping changes tx shape asymmetrically by design (Plutarch drops a ref input Aiken still needs); reviewers may challenge like-for-like-ness — the spec explicitly sanctions it, so document it at the fixture.
- protocolParamsMintCtx must be upgraded in lockstep with the hardened anchor policy (§4.1) or the protocolParamsMinting scenario fails; conversely a fixture that still passes with a 2-field datum after the validator change means the hardening was not actually enforced.

Test touchpoints:
- Run gates with: bash /home/gumbo/iohk/wsc-poc/.claude/worktrees/new-session-3c417d/benchmark-onchain-compare.sh (builds + runs both exes, emits per-scenario CPU/Mem x-ratios, Mem % vs the 14M tx limit for the 1.43x target, and the size-gate YES/NO line); needs /usr/local/lib libsodium via --extra-lib-dirs per the build-env memory note.
- Individual harnesses: cabal run benchmark-onchain-scripts / benchmark-onchain-aiken-scripts (cwd src/programmable-tokens-test); failure logs print per-EvalKind on any witness failure (ScriptRunner.hs:119-127) — every scenario must show [PASS] with ALL witnesses succeeding, and new scenarios' primary budgets must be non-trivial (>~1M CPU) to prove the body ran.
- Isolation pre-gate: cabal run benchmark-onchain-functions — decision.b.params.* rows are the §14.3 contingency-1 tripwire for Mint.Simple params-lookup cost; any new encoding change needs its aggregate case added here before landing.
- Self-baseline: diff new compare output against the latest doc/bench-checkpoint-*.txt (most recent: bench-checkpoint-with-size.txt / bench-checkpoint-seize-optimized.txt lineage) to enforce the no-regression-beyond-allowance clause; commit a new checkpoint at each safe point.
- After fixing the under-applied base args, verify the three base-spend rows jump from ~560,100 CPU / 3,600 mem to realistic body costs and still beat Aiken's ~5.8M/14.5K.
- Golden encoding tests (spec §6/§11.3) for MintRedeemer constructor indices and the frozen TransferAct/SeizeAct layouts live in the onchain test suite, not the harness — but the harness fixtures are downstream consumers: any golden change invalidates every redeemer literal cited in the change sites.